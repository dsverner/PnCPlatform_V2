# Decision log — PnCPlatform_V2

Every decision taken on this project, in one sequence, numbered from 1.

This is a **record, not a plan**. A decision is never deleted: one that no longer holds is marked
superseded, with what replaced it, because the question an auditor asks years later is *why is it
built this way* and the answer must survive the answer changing.

**This log does not continue PnCPlatform's 258.** Where a V2 decision agrees with a predecessor
decision, it cites it — `agrees with PnCPlatform #45` — rather than inheriting it. Carry-forward
is deliberate re-justification.

**Sources:** `R1`–`R7` are the interview rounds in `docs/interview/ROUNDS.md`. `Spec review` is
the owner's mark-up of requirements v0.1. `Schema review` is `docs/review/SCHEMA-REVIEW.md`.
`Plan` is the approved plan for the first commit, 2026-09-11.

| # | Decision | Why | Source |
|---|---|---|---|
| 1 | `PnCPlatform_V2` is the **delivery repository**. `C:\Projects\PnCPlatform` at 0.10.5 becomes a reference corpus, consulted deliberately and inherited nowhere | Owner: *"I want a full review of the schema based upon an exhaustive interview of the desired outcomes."* | R1 |
| 2 | The rebuild driver is **comprehensibility and end-state fit**, not defect | Owner: *"This project is very large and important, we need to make sure that the starting architecture sets us up for that end result and not just a phase 1 outcome."* Marked *"I can no longer hold it in my head"* | R1 |
| 3 | The platform's primary identity is the **engineering work environment**. When it conflicts with other identities, this one wins | Owner: *"at the end of the day, I am contracted to replace the existing engineering environment."* | R2 |
| 4 | Four answers must never fail: **point in time · proof of obligation · impact · reconstruction**. Change-and-rationale, estate and authorisation were left unmarked | Owner's selection. The unmarked three are not dropped; they are not must-never-fail | R2 |
| 5 | **Impact** is the answer whose failure would be a serious professional problem | Owner's single pick | R2 |
| 6 | The **tension** between decisions 3 and 4 — a work environment wants mutability, a record wants immutability — is carried explicitly, not resolved quietly | Stated to the owner in R3; resolution is decision 23 | R3 |
| 7 | All **fourteen work steps** are real and distinct: request · scope · study · vendor tool · rationale · check · approval · issue · apply · readback · test · return to service · baseline · drawings | Owner ticked all fourteen. Nothing descoped on this basis | R3 |
| 8 | All **eight impact triggers** must be traceable: standard · advisory · system change · template · primary plant · device swap · error found · misoperation | Owner ticked all eight | R3 |
| 9 | The estate is **modest**: 50–150 stations, 2k–10k devices, 10k–100k settings records, 15–25 yr history, 25–75 users, <10 concurrent, 100–500 changes/yr, 25–50% microprocessor | Owner's dials. Consequence: a complexity problem, not a volume problem | R4 |
| 10 | **Procedures are authored definitions, never code.** The engine interprets versioned, approved definitions | Owner: *"We need to create a process engine that will allow us to create/edit etc. all the various procedures that need to be followed in the group… There can be no hard coded procedures."* Agrees with PnCPlatform #45 | R4 |
| 11 | **Two deliberate layers**: a workflow governs the lifecycle of a thing; a procedure governs the doing; one invokes the other at a step | Owner's pick from three shapes. The predecessor had two mechanisms that were never joined | R5 |
| 12 | The procedure vocabulary is **twelve capabilities**: ordered steps · data capture · branching · guards · roles per step · sign-off · evidence · parallel · sub-procedures · due dates · hold points · deviation | Owner's selection | R5 |
| 13 | **In-flight work is pinned** to the procedure version it started on | Owner reversed his R5 answer once the conflict with decision 4 (point in time) was put to him | R6 |
| 14 | **Iteration over a set** is needed | Owner reversed his R5 answer. Reinforces PnCPlatform #60 (the settings-issue package is the approval subject) | R6 |
| 15 | The **ownership boundary** — fourteen domains, each OURS, THEIRS or SPLIT — is as tabled in `REQUIREMENTS.md` §6 | Owner's grid, R6, with five conflicts reconciled in R7 | R6, R7 |
| 16 | **Work orders are split**: Cascade owns the scheduled order; the platform owns the Work Request. Agrees with PnCPlatform #36 | Owner chose to keep #36 over his first answer of OURS | R7 |
| 17 | **Assets are split**: the platform owns P&C engineering facts; SAP keeps purchase, stock, cost, warranty | Owner chose P&C-only. Respects the proposal's exclusion of enterprise asset-management replacement | R7 |
| 18 | **Test records are split**: the tool keeps the raw file; the platform owns the result, readings and acceptance. Agrees with PnCPlatform #42 | Owner reversed THEIRS once the conflict with decision 4 (proof of obligation) was put to him | R7 |
| 19 | **Drawings are split**: the drawing system holds the file; the platform records what each sheet depicts, by drawing key. Agrees with PnCPlatform #53 | Owner reversed THEIRS once the conflict with decision 5 (impact) was put to him | R7 |
| 20 | **Wiring is ours, shape only in Phase 1** | Owner. Resolves the inconsistency between "unsure" and "everything below the panel is ours" | R7 |
| 21 | **Carry forward** the proven subsystems; **design the procedure engine fresh** | The review found the failing part is 7 tables and 124 rows against 1 182 939 rows that work. Owner chose this over clean-sheet and over in-place replacement | Schema review |
| 22 | The procedure layer being replaced is exactly: `config.TestPlanStep`, `TestPlanReading`, `work.WorkflowInstance`, `WorkflowTransition`, their registries, and the `Program.Workflow` / `Program.TestPlan` definition kinds | The two layers were never joined; 2 of 13 capabilities present; never activated | Schema review |
| 23 | **Work is mutable until a procedure step commits it; committed facts are immutable thereafter.** The engine is the boundary between drafting and record | Resolves decision 6. Owner marked FR-2.2 *keep* | Spec review |
| 24 | **Identity is the person; the account type is a platform grant.** AD authenticates; the seven account types are not directory groups | Owner: *"users will authenticate as themselves and then within the application they will be tied to the account types."* | Spec review |
| 25 | **Two budgets**: architectural weight may be spent freely at this scale; the interface may not be slow | Owner: *"while true, the application must be responsive for usability's sake."* | Spec review |
| 26 | **Non-microprocessor devices are first-class**, and the predecessor schema already satisfies this | Owner's dial (25–50% microprocessor) plus measurement of the characteristic model. An earlier claim to the contrary was withdrawn | R4, Schema review |
| 27 | **Stack: .NET + PWA on SQL Server**, as PnCPlatform | Owner's choice from three. Deployment, IIS configuration and QA environment already exist and work | Plan |
| 28 | **Authoring: the owner now, P&C engineers later.** The first editor is an expert surface; the model must admit a friendly authoring UI later without being reshaped | Owner's choice. Closes OQ-1 | Plan |
| 29 | **The first commit is documents only.** No code, no scaffold, no DDL | Owner's choice. Establishes the record before anything is built against it | Plan |
| 30 | **Phase 1 replaces the legacy application's functionality — not its structure or its stack** | Owner, mid-review | Plan |
| 31 | Legacy `D`-prefixed rows (2 361) are **dropped at cutover** with a counted, recorded reason | Owner: *"D in the OLD_NO signifies that a work request was deleted prior to completion, in our cutover, these can be dropped as they have no significance."* | Plan |
| 32 | Cutover is by **full-table content-hash diff**, not by watermark | The header table has no usable date (0 of 8 409) and no unique key (894 duplicated IDs); a watermark misses in-place edits; the whole database is 48 706 rows. Adopts the owner's import-now, delta-later strategy and rejects its obvious implementation | Plan |
| 33 | `CDATE` is the **calculated date**; `VDATE` is the **verified date, when it went into service**. These two dates matter; `IDATE` is not yet characterised | Owner's correction | Plan |
| 34 | Legacy state is decoded from **both** the `OLD_NO` prefix and the document filename rotation (`A9999` → `M9999_12345` → `P9999_12345` / `A9999`), and **state never survives into filenames** in the platform | Owner's description of the rotation, corroborated in the data | Plan |
| 35 | This log starts at 1 and **cites** PnCPlatform decisions rather than continuing them | Carry-forward is deliberate re-justification, not inheritance | Plan |
| 36 | The **decision-card convention** for asking the owner anything is carried forward unchanged, with one observed refinement: prefer propositions to accept/reject/amend over requests for narrative | Owner's standing instruction from PnCPlatform, 2026-09-08. Refinement from seven rounds of empty free-text boxes | Plan |
| 37 | `docs/reference/CARRY-FORWARD-MAP.md` is **maintained, not written once**. Anything imported gets a line | It is the working answer to NFR-3 | Plan |
| 38 | A procedure definition is **one JSON document per version**, authoritative in `config.DefinitionVersion.PayloadText`, **projected to rows on approval** for query | Owner's pick from three. The predecessor's `{"steps":"see config.TestPlanStep"}` pointer is why a procedure could not be versioned, diffed or approved as one thing. Agrees with PnCPlatform #78 | Design |
| 39 | Control flow is **block-structured** — nested `sequence · parallel · choice · foreach · repeat · call · hold · step`. No arbitrary edges | Owner's pick. Cannot express deadlock or an unreachable step by construction; diffs cleanly; renders as an outline for a later friendly editor | Design |
| 40 | A sub-procedure's version is **pinned when the root instance starts**; the whole tree is one version set (`process.InstanceVersionSet`) | Owner, after the alternative was shown to add silent drift. *"Which procedure did this request follow?"* has one answer per instance | Design |
| 41 | Approval of a new procedure version **lists every affected running instance for a person to rule on**, singly or in bulk — Keep · Migrate from current step · Cancel and re-raise — each with a reason (`process.InstanceMigration`) | Owner: *"if I have 1000 outstanding work requests… will those changes not fall through the cracks?"* Nothing moves silently; every instance ends with a cited version and a cited decision | Design |
| 42 | **Every committing step produces a `record.Record`.** The step instance holds the draft, then the pointer | Owner's pick. The record model is the platform's existing immutable object — bi-temporal acceptance, evidence links, obligations consume it. One evidence chain | Design |
| 43 | Guards, preconditions, branch conditions, foreach sets, validations and workflow guards are **grammar-1 expressions**, checked against the fact catalogue and evaluated three-valued, using `src/PnC.Formula` unchanged | One expression language in the platform. `FORMULA-GRAMMAR.md` §7 already prescribes an AST under `"when"` for workflow guards. Agrees with PnCPlatform #248 | Design |
| 44 | Step **due dates are derived on read** from a cadence and an anchor step — never stored | Owner, 2026-09-10 (`CALCULATION-ENGINE-DESIGN.md` §8): *"Allowing the application to 'calculate' the due date is actually a much more straight forward and correct method"* | Design |
| 45 | A new **`process` schema** hosts both layers' runtime and the projections. `WorkflowInstance` / `WorkflowTransition` are re-homed from `work`. The seven predecessor procedure tables are not imported | One place to look (NFR-3). The layer being replaced is not entangled with carried tables | Design |
| 46 | **Authoring v1** is the existing JSON editor with `procedure.schema.json` enforced live and every expression checked through `/formula/check`. The authored document carries expressions as text; approval stores the canonical AST. **The schema is the contract a later friendly editor targets** | Owner: authors now, engineers later. Nothing in the model changes for the friendly editor to exist | Design |
| 47 | The join runs **in both directions**: a workflow state's `onEnter.startProcedure` and a transition's `requires.procedure`; a step's `advances` fires a transition | FR-2.1 in full. The predecessor had no column joining its two layers | Design |
| 48 | A **deviation always requires a reason and always produces a `record.Finding`**; a step declares whether it allows `skip`, `vary`, both or neither | The record stays honest about what was not done as written | Design |
| 49 | A step's **`precondition`** is the guard primitive; an **Unknown** result holds the step for a person and names the unknown facts, never proceeds and never silently blocks | Kleene semantics as `FORMULA-GRAMMAR.md` §5; missing data is shown, not hidden | Design |
| 50 | The build step **forks on the device**: a settings file for a microprocessor relay, typed settings (tap, time dial, instantaneous…) recorded as characteristics for anything else. Both commit as revisions in the settings book | Owner's pick (repairs card R1) after the review found the example demanded a file from every device, impossible for the 50–75% of the estate without one. Delivers FR-3.4 in the procedure, not only the schema | Repairs |
| 51 | A readback difference resolved by **raising a corrective change ends that relay's branch as `Superseded`**; the other members continue; the request can close. Mechanism: `step.branchOutcome` | Owner's pick (R2). Without it the branch waited forever on a precondition that could never be true (review C1). Every step with a precondition now sits in a branch some outcome can end | Repairs |
| 52 | **Committing a step creates the settings-book entry**: a `ConfigurationFileRevision` or `DeviceSettings` commit writes the `document.Revision`, `ConfigurationFile` and package item; a `Readback` commit writes the readback revision and the comparison. **Step 1 produces the package** (`step.produces`) | Owner's pick (R3); package creation left to my default, which was step 1. The settings book is a consequence of the procedure, never edited by hand | Repairs |
| 53 | Field work is **captured offline and committed at check-in**, with the technician as `CommittedByActorId`, the reviewer as `AcceptedIntoPlatformByActorId`, `OccurredAt` = the capture instant, and competency and segregation evaluated against the technician. Witnesses re-authenticate on the field device | Owner's pick (R4); witness mechanism left to my default. Keeps the field pack capture-only (FR-7.1) while letting field steps become records | Repairs |
| 54 | Conditions are evaluated **on every commit and on a scheduled sweep** (default 15 min) for anything reading `@at`. `@at` is the evaluation instant | Owner's pick (R5); interval left to my default. A time-based hold with nobody logged in releases on time | Repairs |
| 55 | A Ready step is **claimed by the first person to open it**; others in the role see it read-only; the claimant may release it; the responsible engineer may take it over with a logged reason. The draft is writable only by the claimant, readable by the claimant and the responsible role | Owner's pick (R6). Two technicians on one draft had last-writer-wins with no warning (review H2). Also answers OQ-15 | Repairs |
| 56 | Every **open legacy change lands at the `COMPLETION` block** with branch states from the legacy tracks and steps 1–12 recorded as *migrated — not performed in this platform* | Owner's pick (R7). The legacy data cannot say whether approval, issue or application happened; anything else is invention | Repairs |
| 57 | **The FLOC tree is the model; the legacy Location / Protected Asset / Protection Function style is its default view, kept indefinitely.** Navigation by station, panel, scheme and device type (FR-7.2, binding) is the same tree browsed | Owner (R8, in his words): *"The legacy style is simply a view of the FLOC mechanism that we are moving to… Protection Engineers and Techs will always operate off of Location/Protected Asset/Protection function and that really should just be a particular view on the new FLOC segmentation."* | Repairs |
| 58 | The **legacy software completion track is dropped** from the procedure. Its 1 906 non-NA rows migrate as dated notes on the request, never discarded | Owner: *"the software track was never implemented and can be dropped."* The data shows 1 572 Complete and 334 In Progress with notes back to 2005 — used inconsistently, not never. Ruling stands; the history is kept as notes. `LEGACY-SYSTEM.md` §6 | Repairs |
| 59 | The 17 legacy chains where an archived CR exceeds the active CR migrate **as the letters say** — `A` active, `P` history — and become **findings on day one** for a person to rule on | Owner's pick (open-questions card D1) over an automatic VDATE rule or a pre-cutover review. Base 0069, where the 2025 in-service record is marked archived, is the example | Open questions |
| 60 | The 2 rows keyed `2440` — a model number typed into `OLD_NO` — are **dropped with a counted reason** | Owner's pick (D2). Two rows, one SEL-2440 at Belledune, 2018 | Open questions |
| 61 | **Every device's settings are held as a file** — the vendor's native file where one exists, a **text file in name=value form** where not — **parsed against a per-device template** naming the settings the device has, and read through **one uniform accessor**, `device.settings.<key>`. The legacy `SET1` column is the text format and seeds the templates. Settings become generic and user-configurable once a file type has a reader | Owner (D4, in his words): *"all devices store there settings either in a text file for devices that have no native file type or in the devices native file type where possible. Then the application will create the necessary code to parse the settings based upon a device template (for example X('wdg1')…). In that way, all devices would have the same retrieval procedure X()."* Supersedes the typed-fields half of #50; the fork stays. Agrees with the grammar's `device.settings.*` facts and PnCPlatform's reader-per-file-family design | Open questions |
| 62 | The 1899-12-30 sentinel means **unknown**; migrates as no date | Owner's pick (D3). 54 calculated and 75 in-service dates | Open questions |
| 63 | **The engine is built inside Phase 1.** The excess over the contracted hours is the owner's investment in phases 2–5; documentation packages are delivered at acceptance | Owner's pick (P1): *"I am adding work to this first phase to make the other phases more streamlined."* Estimate 287–470 h before #64 | Open questions |
| 64 | **The application is rewritten.** Only `PnC.Formula` and the deploy tooling are carried from the predecessor's code; the schema carry-forward (#21) is unchanged | Owner's pick (P2) **against the recommendation to carry the code**. Cost recorded: bring-up 72–124 h instead of 12–24; Phase 1 as scoped roughly 347–570 h | Open questions |
| 65 | The order of phases 2–5 is **decided with the client after Phase 1 acceptance**; shape-only schema covers both orders | Owner's pick (P3) | Open questions |
| 66 | **One engineer role, scoped per person**: `PCEngineer` with grants for Transmission, Distribution, Generation·Hydro, ·Belledune, ·Coleson; `PCTechnician`; `Administrator`; `PCApprover` as a separate grant. Visibility is strictly per scope | Owner's pick (P4) with the concern that scopes be *"completely separate"* — met by the grant, since read scope equals write scope (FR-6.2; PnCPlatform #38, #59) | Open questions |
| 67 | The engine's **published fact names are accepted** as proposed | Owner (P5) read three authored conditions and found the style right | Open questions |
| 68 | **Reads of a step's draft are audit-logged**, like reads of evidence | Owner (P6). Closes OQ-15; extends PnCPlatform #65 | Open questions |
| 69 | **A hold past its maximum raises an obligation**, not only an overdue item | Owner (P6). Closes OQ-17 | Open questions |
| 70 | The predecessor's **eight draft `Program.Workflow` definitions are discarded**; the two new workflow documents supersede them | Owner (P6). Closes OQ-16 | Open questions |
| 71 | The owner works **more than forty hours a week** for Phase 1, unquantified; start date not given | Owner (P7): *"I am willing to work more hours, to get it done properly."* | Open questions |
| 72 | Closed by the data, not by ruling: **`IDATE` is dropped** (NULL in all 14 211 rows); **pre-numbering rows need no ordering rule** (CRs below 1 000 sort first; 334 of 617 are single-row chains) | Measured 2026-09-11 against `dbRelayManagement_Legacy` | Open questions |
| 73 | The V2 development database is **`PnCPlatform_V2_DEV`** on 10.10.70.25. The predecessor's `PnCPlatform_DEV` and `_QA` are never written to | A new name so the reference corpus stays intact. 18 databases listed on VM01 this session; none carries `_V2_` | Workflow |
| 74 | **The build runs in nine waves, W0–W8**, each leaving an observed increment on DEV behind a gate that reuses the carried tooling: carry the foundation → API skeleton → identity and scopes → definitions and the `process` schema → the interpreter → authoring and the second procedure → parity screens and the FLOC view → migration → QA, rehearsal and acceptance. `docs/workflow/PHASE-1-WORKFLOW.md` | Owner approved the sequence 2026-09-11. Order follows dependency: every gate needs auth, so identity precedes the engine; screens are queries over `process.*`, so the engine precedes screens; real data lands after the screens exist to show it | Workflow |
| 75 | Waves are **strictly sequential for a solo owner except W6 (screens) and W7 (migration)**, which may interleave | Neither depends on the other; both depend on W4 | Workflow |
| 76 | The carried tooling — the SQL project and dacpac build, `deploy.py`, `generate.py`, `check_generated.py`, `smoke.py`, `record_release.py`, `package_release.py`, `rehearse_relocation.py`, the extensibility gate — is **carried as-is under #64's tooling exception**, each with a `CARRY-FORWARD-MAP.md` line | Verified this session: the generator produces every temporal view and write procedure from the deployed catalogue, so the `process` schema's twelve tables get theirs without hand-writing; `deploy.py` already runs the generate pass; the smoke is re-runnable on a populated database | Workflow |
| 77 | **W0 carried the foundation from the predecessor at commit `e99a32d`, byte-identical**: `docs/schema/ddl/` (the SQL project, `Roles.sql`, the publish profile, `tools/`, and its `CONVENTIONS.md` / `PROCEDURES.md` / `STEPS.md`), `docs/schema/gate/`, `docs/schema/grammar/` and `docs/schema/FORMULA-GRAMMAR.md`, `src/PnC.Formula` and `PnC.Formula.Conformance`, `tools/package_release.py`, `tools/rehearse_relocation.py`, `tools/README.md`. `grammar/` and `FORMULA-GRAMMAR.md` were not in the W0 list and are carried because `smoke.py` imports `grammar/formula.py` and decisions #43 and #49 cite the grammar document | Verified by `diff -rq` against `C:\Projects\PnCPlatform` on 2026-09-11: no file differs; the only predecessor-only entries are `bin/`, `obj/` and `__pycache__/`. Each import has a `CARRY-FORWARD-MAP.md` line | W0 |
| 78 | **The seven replaced tables are removed with everything over them**: 24 files (the tables, registries, generated views and write procedures) and three hand-written procedures — `work.StartWorkflow`, `work.Transition` (the interpreter over `WorkflowInstance` / `WorkflowTransition`) and `record.RecordTestResult` (which read `config.TestPlanStep.IsRequired`). `record.TestResult.TestPlanStepRowId` and `record.TestReading.TestPlanReadingRowId` **keep their columns and lose their foreign keys**; W3 re-points them at `process.ProcedureStep` and the step's capture | The workflow (#74) fixes 295 tables, so the two record tables stay; a foreign key into a table that no longer exists cannot. Dropping the columns would lose the join the design needs; keeping them unconstrained for three waves costs nothing on an empty database. `generate.py` reported 0 written, 0 stale after the removal — the generated files matched the deployed catalogue exactly | W0 |
| 79 | **`smoke.py` takes the engine's location from `PNC_ENGINE_DIR`** and, until W4 delivers the V2 interpreter, the gate runs its 41 engine-driven lines through the predecessor's built `src/PnC.Engine.Cli` pointed at `PnCPlatform_V2_DEV`. Eight checks were removed with the seven tables (six on the workflow interpreter, two on `IsPartial`); **the smoke total is 244**, from 252 | The predecessor's engine left the database on 2026-09-10 and lives in `PnC.Api`, which #64 rewrites; without an engine the 41 lines cannot run and the schema gate would lose the fact-catalogue checks for four waves. A deviation from #76's *as-is*, recorded here; the override is one line | W0 |
| 80 | **The release-fact smoke check is fresh-database safe**: on a database with no succeeded deployment it asserts that `platform.release` and `platform.baseline` are **Unknown**; otherwise it asserts the latest succeeded deployment as before | The first `deploy.py --fresh` on `PnCPlatform_V2_DEV` failed this one check (243 of 244): the check expected a text value where the engine honestly answered Unknown, because `record_release.py` runs after the smoke. The predecessor's `_DEV` had nine succeeded deployments, which is why it never showed there. A second deviation from #76, recorded | W0 |
| 81 | The extensibility gate runs against **`PnCPlatform_V2_GATE`** (dropped and recreated on every run), never the predecessor's `PnCPlatform_GATE` name | Same reason as #73: the V2 footprint on VM01 is named `_V2_` throughout. Re-run 2026-09-11: nine checks PASS; `RESULT.md` differs from the predecessor's only in the run line | W0 |
| 82 | Of what the W0 copy brought beyond the list: **`docs/schema/SCHEMA-DESIGN.md` is carried** as the citation target of 378 DDL headers; **`MIGRATION-PLAN.md` and `docs/schema/migration/` stay uncommitted until W7**, when they are re-copied under their own decision; **the proposal document is committed at `docs/contract/PNC Platform Proposal.docx`** | Owner's picks (W0 card D3a–D3c, 2026-09-11). D1 (keep the two record columns, #78), D2 (the predecessor's engine until W4, #79) and D4 (the `Standard` workflow seed stays until W3) were accepted as recorded | W0 card |
| 83 | **V2 release numbering restarts at 0.1.0.** The sqlproj's `DacVersion` is 0.1.0; the 0.10.16 row recorded by the first W0 deploy stays in `platform.Release` as history | Owner's pick (W0 card D5) over continuing the predecessor's 0.10.x. Observed after redeploy: `platform.vRelease` holds 0.10.16 and 0.1.0, each with one succeeded deployment to `PnCPlatform_V2_DEV` | W0 card |
| 84 | **The API is a generic dispatcher over the catalogue** — `POST /api/v1/{schema}/{procedure}`, `GET /api/v1/{schema}/{view}`, the catalogue read from `sys.*` at start with `OBJECT_DEFINITION` parsed for defaults; thin handlers; **a procedure's `THROW` is the API's 409 in the procedure's words**; no rule in C# | `docs/design/API.md` §0, §3, §6 (decisions A, C). Agrees with PnCPlatform §2.3 and its `Catalog.cs`, consulted 2026-09-11, not copied. Verified: 497 procedures and 375 views catalogued on `PnCPlatform_V2_DEV`; `config.ApproveDefinitionVersion` on an unknown id answers 409 `rule` 50030 *Unknown definition version.* | W1 |
| 85 | **Identity is Windows via IIS, one enabled `security.User` by `UserPrincipalName`, and attribution is the database's**: the request's connection sets `SESSION_CONTEXT('UserPrincipalName')` (and the delegation / sponsored-person headers), `personnel.ResolveActor` attributes the act, and **`ActorId` / `MigrationRunId` are never bound from a client** — a body carrying either is refused. The DEV header `X-PnC-Dev-User` needs `Auth:Mode=Development` **and** the environment name `DEV`; **the host's default environment is `Production`**, so neither condition is reachable by omission | API.md §2 (decisions B, G). Agrees with PnCPlatform §2.2, §4.2. The Production default is a V2 hardening (`docs/review/API-W1-SECURITY.md` #2), probed: with the local Development-mode file present and no environment variable, the header answers 500 `auth_mode` | W1 |
| 86 | **The permission map is `api-permissions.json` in the release; explicit `null` and unmapped hand-written procedures are not callable; the map is validated against the catalogue at start.** One `security.fHasPermission` call decides; refusals are logged `AccessRefused` before the 403 | API.md §4–§5 (decisions D, E). Agrees with the predecessor's `PermissionMap` and §2.4; the start-up validation is V2's. All 33 hand-written procedures and the 3 generated append procedures (`migration.Run_Append`, `platform.Release_Append`, `platform.Deployment_Append`) have explicit entries, 13 of them `null`; the schema→class assignments are **W1 defaults reviewed in W2** (W1 card). Verified: `personnel.ResolveActor` → 404 `not_callable`; ReadOnly write → 403 with an `AccessRefused` row | W1 |
| 87 | **Read logging is driven by `config.ReadLoggedClass` through `audit.LogRead`**, loaded with the catalogue — a row switches a class on, never a release. **POST bodies must be `application/json`** (415 otherwise) so a cross-site form cannot drive a Windows-authenticated user's procedures | API.md §5–§7 (decision F; hardening #1 in `API-W1-SECURITY.md`). FR-6.4; PnCPlatform #65. The predecessor hard-coded the one logged class in C#. Verified: a form-encoded POST answers 415 | W1 |
| 88 | **W1's UI is the hand-written shell** (`wwwroot/`, no framework, no inline script or style under `default-src 'self'`); the UI framework is a W5/W6 ruling. **`PnC.Api.Smoke` is rewritten** to the W1 surface: 19 checks, exit 1 on any failure | API.md §8–§9 (decisions H, I). Observed 2026-09-11 in Chrome on the build laptop: the shell renders health and *not signed in* without an identity; 19 of 19 smoke checks pass against the local API | W1 |
| 89 | **The V2 API runs as a second IIS site on VGS-VM02, port 8443, against `PnCPlatform_V2_DEV`**; the predecessor's site at 443 stays until W8 rebuilds the QA VMs. `VGSOT\svc-pncapi` holds `app_execute` on `_V2_DEV` as it does on `_QA` | Owner's pick (W1 card D1). Also accepted: D2 the `Auth:UpnSuffix` mapping until W2; D3 the permission map's W1 defaults, reviewed in W2; D4 the catalogue visible to every signed-in user; D5 the SQL-link certificate handled in W8. Verified 2026-09-11 (read): `PnCPlatform_QA` holds the Windows user `VGSOT\svc-pncapi` in `app_execute`; `_V2_DEV` does not yet | W1 card |
| 90 | **Development mode outside the DEV environment refuses to start**, not only to answer | Owner's T1 result: `set ASPNETCORE_ENVIRONMENT=DEV` in PowerShell sets nothing, so the host came up as *Production* with Development mode and every API call would have answered 500 `auth_mode`. That is the fail-closed behaviour working, but a host that cannot serve should not start. `src/PnC.Api/README.md` gives the PowerShell form | W1 card |
| 91 | **The owner installs the second IIS site on VGS-VM02 by hand from `docs/runbook/VM02-SECOND-SITE.md`**; this session verifies from the laptop afterwards. The service account's `app_execute` membership on `PnCPlatform_V2_DEV` was granted by the owner; **two gate identities are seeded** on `_V2_DEV` through the procedures as the SYSTEM actor — `VGS01@vgsot.internal` (Administrator, Global) and `VGS99@vgsot.internal` (ReadOnly, Global), each person noted as a W1 gate fixture | Owner's picks (W1 card round 2: G1 done, G2 owner, G3 seed; submitted 2026-09-11 20:09Z). Verified by query 2026-09-11: `app_execute` on `_V2_DEV` holds `VGSOT\svc-pncapi`; `security.vUser` × `vGrant` shows the two rows with their grants. W2 replaces the fixtures with the real identity model (#66) | W1 card |
| 92 | **W1 is done: the gate was observed from VGS-VM07 as VGS01 and VGS99 against the second site on VGS-VM02** — `/health` with the release, `/me` as both, the catalogue, a view read as ReadOnly, a write refused as ReadOnly (an `AccessRefused` row attributed to VGS99) and allowed as Administrator (a person attributed to VGS01, soft-deleted after). Two facts of the OT host now stand in the runbook for every later install: **the catalogue the app sees is the service account's, not `db_owner`'s** (the `platform` schema's procedures are invisible to `app_execute`), and **a site on a custom pool identity needs `useAppPoolCredentials`** for Kerberos with the service-account SPN | Observed 2026-09-11/12 (`PHASE-1-WORKFLOW.md`, W1 gate record; `docs/runbook/VM02-SECOND-SITE.md`). The install ran through the Proxmox guest agent as `claude-rw@pve` once the owner allowed it, superseding #91's *by hand*; the by-hand script stays valid | W1 |
| 93 | **Five active account-type roles** — `Administrator`, `PCEngineer`, `PCTechnician`, `PCApprover`, `ReadOnly` — with the predecessor's fourteen deactivated (soft, reversible by a row); **the permission matrix of `IDENTITY.md` §3 seeded** as defaults (Archive and Administer stay the Administrator's); **the five scopes of #66 are `Division` nodes under one `Owner` node**, seeded as data, and an engineer's grant is `NodeSubtree` on a division | `docs/design/IDENTITY.md` §3–§4 (decisions A–C). Observed on `PnCPlatform_V2_DEV` 2026-09-12: seven active roles (five plus the two schema mechanics), PCEngineer 30 / PCApprover 15 / PCTechnician 14 permissions, six scope nodes. The matrix is the W2 card's proposition; an amendment is a row change | W2 |
| 94 | **Read scope is enforced on lists in the database**: `security.fReadableSubjects` (inline, set-wise, one family per call: Node, Asset, Record, WorkRequest, Scheme) filters every list read of a view that has a subject column; `security.fHoldsPermission` (any scope) gates the list; the API contributes only the subject column and family, derived from the catalogue (`sys.sql_expression_dependencies` → the base table's registry) and the map's typed subject keys. **The carried `fHasPermission` subtree predicate was wrong** — a node's `Path` holds its ancestors only, so `LIKE sn.Path + '%'` matched sibling subtrees — and is corrected in both functions | `IDENTITY.md` §5 (decision D); `docs/review/API-W2-SECURITY.md` #1–#2. Verified by the W2 smoke (47 checks): the Hydro engineer sees the Hydro asset, station and placement and not Transmission's; a single read and a write outside scope answer 403 with `AccessRefused` rows; Administrator and ReadOnly see both. Scoped reads cost ~30 ms on the fixture. The predecessor decided lists on the class (its `ApiEndpoints.cs:75`), which #66's *completely separate* forbids | W2 |
| 95 | **The directory SID is the user's durable key**: written to `security.AlternateKey` (`ActiveDirectorySid`) on the first Windows sign-in of a user found by name, logged `Administrative`, and preferred on every later request | `IDENTITY.md` §2 (decision E). DEV mode has no SID and never writes one. **Observed in Windows mode 2026-09-12**: the three gate accounts' SIDs in `security.vAlternateKey` (`ActiveDirectorySid`, kind and value read by query) and one `sid-registered` row each in `audit.vActionLog` **Amended by the W2 card (D1 *refuse*, submitted 2026-09-12 11:08Z): a user found by name who already carries a different SID is a re-created directory account and is refused (401 `identity_changed`) until an Administrator confirms the person; the new SID is never registered beside the old one** (b0cc815). Windows-mode verification stands as recorded under #97 | W2 |
| 96 | **The permission matrix of `IDENTITY.md` §3 is accepted as the seed, on the owner's condition that the application lets it be changed.** Today an Administrator changes it over the API: `security.RolePermission_Upsert` (`Grant.Modify`) and `RolePermission_Deactivate` (`Grant.Archive`) are callable and audited; the W6 administration screen puts a face on them. **Found while recording this: the seed re-activated any mapping an Administrator had deactivated on the next deploy** (it tested `IsActive = 1`); from 0.2.1 the seed adds a mapping only where no row exists at all, so a deactivation survives every deploy | W2 card M1 *accept*, note: *"This is fine for now, as long as the application will allow changes."* (submitted 2026-09-12 11:08Z). The API's classification of `_Upsert` / `_Deactivate` is `PermissionMap.ForProcedure`; the seed rule is `PostDeploy/Seed_security_RolePermission.sql`. **Owed:** a check in the API smoke that a deactivated mapping is honoured and survives a redeploy (W6, with the screen) | W2 card |
| 97 | **Three dedicated gate accounts on `vgsot.internal`** — `pnc-gate-admin` (Administrator, Global), `pnc-gate-ro` (ReadOnly, Global), `pnc-gate-hydro` (`PCEngineer`, `NodeSubtree` on *Generation · Hydro*) — created on the Samba DC (VGS-VM05) with `samba-tool` through the guest agent, passwords generated by the session and held only in the predecessor's gitignored `dev.local`; seeded as platform users on `PnCPlatform_V2_DEV` through `Person_Add` (system account, noted as a gate fixture), `User_Add` and `Grant_Add` as the SYSTEM actor. **Gates run from VGS-VM02 unattended** with these accounts (`tools/ot/gate_runs.py`: a one-shot scheduled task per account, deleted after); the owner's own accounts are no longer gate fixtures | W2 card A1 *yes* (standing authorisation, submitted 2026-09-12 11:08Z). Verified by query 2026-09-12: `security.vUser` × `vGrant` holds the three rows with those grants; `samba-tool user list` shows the three accounts. **Run by the session 2026-09-12 after the owner widened its standing permissions** (guest-agent commands, the `_V2_*` databases, the gate accounts): three `SMOKE PASS` lines (31 / 9 / 6 passed, 0 failed) and the three SIDs registered (#95). Two facts of the host stand with it: **the gate accounts hold `SeBatchLogonRight` on VM02** (granted with `secedit`, without it the task never starts) and **the run's files live in `C:\Users\Public`**, the accounts having only read and execute on the site folder. The smoke's role-list check was corrected on the way: a `PCEngineer` is refused `security/vRole` (no `Grant.Read`), which is the platform right and the check wrong. The Windows-mode Hydro run does not exercise the scope checks (one identity per process); they stand on the DEV-header run of the gate record | W2 card |
| 98 | **A VM whose guest agent stops answering commands may be rebooted through Proxmox by the session** (`tools/ot/pve.py reboot <vmid>`), after a ping and a trivial exec have both failed, and the reboot is recorded in the gate record | W2 card A2 *yes* (submitted 2026-09-12 11:08Z). First use 2026-09-12: VGS-VM07 (VM 107) answered pings but timed out on every command; after the reboot the agent ran commands within a minute | W2 card |
| 99 | **Incident, 2026-09-12 08:35: this session published the V2 schema over the predecessor's `PnCPlatform_DEV`** by running `deploy.py` without `--database` — the V2 copy of the tool still defaulted to the predecessor's database name. Drop-objects-not-in-source removed seven predecessor tables (the TestPlan and Workflow tables and their registries) and the V2 seeds deactivated its fourteen roles; 1.2 M rows survived. **Remedy, on the owner's ruling (incident card R1, submitted 12:05Z): point-in-time restore to 08:35:00** from the 01:02:45 full and the log chain to 08:45:34, after a copy-only backup of the damaged state; run by the owner because the session's classifier refused every database write. **Rule from it: every V2 tool defaults to `PnCPlatform_V2_DEV`, and `deploy.py` refuses a target outside `PnCPlatform_V2_*` unless `--allow-any-database` is passed** (e82688c) | Verified after the restore by read-only query: all 302 predecessor DDL tables present, no V2-only object, no object modified that day, the predecessor's nineteen roles active. `PnCPlatform_V2_DEV` was never touched (no object modified that day; the gate users present). The card's figure of 158 added tables was unreliable — the predecessor's database already holds ~161 tables beyond its DDL folder — and is not carried here | W2 incident |
| 100 | **The `process` schema is built in W3 as PROCEDURE-ENGINE §4 lists it** — twelve tables, `Versioned` for the four projections and seven instance tables (own registries), `AppendOnly` for `WorkflowTransition`; `DueAt` is never a column (#44). **Rules in SQL, grammar in C#** (agrees with #84): `process.ValidateProcedureDocument` / `ValidateWorkflowDocument` hold the structural checks of design §8 as THROWs in the rule's words (50120–50135); `process.ProjectProcedureVersion` derives the four projections; `process.ApproveProcedureVersion` approves (segregation, #1) and projects in one transaction. The API does only what the grammar library can: JSON-Schema validation (`SchemaCheck`, the subset the two schemas use — no library added, API.md §10), parse, type-check against the live catalogue plus the document's own declared names, canonicalise (`DocumentCompiler`) | `docs/schema/ddl/process/`; `src/PnC.Api/Definitions/`. `work.WorkflowInstance` / `WorkflowTransition` were never imported: `process` is designed new (CARRY-FORWARD-MAP §3). Verified on `PnCPlatform_V2_DEV` 2026-09-12: 34 tables (11 Versioned + 1 AppendOnly + 11 registries), generator clean, schema smoke **268 PASS** (24 new) | W3 |
| 101 | **The engine's facts of PROCEDURE-ENGINE §7 enter `compliance.vFactCatalogue` in W3, not W4**: `step.state/outcome/committed_at/committed_by/capture` (parameters `id`, `field` — `pass=`/`member=` arrive with the interpreter), `branch.outcome`, `procedure.outcome`, `package.revisions`, `package.revision_count` as source `Engine` (no reader until W4); `work.outage_required` / `work.outage_window_start` as `Fixed` facts read by `fFixedFactValue` from the work request's outage columns. **`procedure.<name>` and `input.<name>` are not catalogue rows**: they are declared by the document (a `produces.as`, an input) and checked against it (50135); `ValidateProgramFacts` passes those prefixes for a `Program.Procedure` | Without the rows `config.AddDefinitionVersion` refused every canonical procedure document (`ValidateProgramFacts` walks `$.fact`). A deviation from the wave plan, recorded. Verified: the SETTINGS_CHANGE document stores; `vProcedureFactUse` indexes 17 uses across 16 blocks and the technician alias | W3 |
| 102 | **The three example documents are loaded through the API and kept** (`process.Add*Version` is idempotent on the canonical hash, `@Existing = 1`), not seeded in PostDeploy; the gate is the load. **`DRAWING_REVISION` exists from W3 as a definition with no version** (`Seed_config_Procedure_Placeholders.sql`) because `SETTINGS_CHANGE` calls it and a `call` must name a `Program.Procedure` definition (50126); W4's pinning will require an Effective one, W5 authors it. The pre-W3 `Standard` workflow seed is **Retired** (soft; a fresh database no longer seeds it) — the workflow documents now follow `workflow.schema.json` | Plan Q1 option (a). Verified by query 2026-09-12: `SETTINGS_CHANGE` v1, `SETTINGS_LIFECYCLE` v1, `SETTINGS_CHANGE_REQUEST` v1 Effective on `_V2_DEV`; `Standard` v1 Retired; `DRAWING_REVISION` 0 versions | W3 |
| 103 | **The design example needed one correction to be a valid grammar-1 document**: `person.training_current` takes a `module` parameter in the catalogue (PROCEDURES.md #35, built 2026-09-06) and the checker requires every listed parameter, so the technician alias now reads `person.training_current[module='PC_FIELD_SETTINGS'] = true`. **`PC_FIELD_SETTINGS` is a placeholder module code** — no training module is seeded; until W7 seeds them the fact evaluates Unknown and the person is refused (FORMULA-GRAMMAR §5, fail closed). **FR-3.1's fourteen steps are fifteen `step` blocks plus one `call`** in the document ([4] has two variants — the electromechanical fork — RESOLVE_DIFFERENCE is unnumbered, [14] is the call), so the gate asserts 15 projected steps, 4 `advances`, 1 `call` | Found by the API's type check on the first load (400 `document_invalid` at `$.roles.technician.requires`); the document's description records the change. The count was checked against the file (15 `"block": "step"`) before the gate was amended | W3 |
| 104 | **Two electromechanical settings templates are seeded for W4's fixture as `Transform.SettingsParse` definitions bound through `ref.FirmwareVersion` ('n/a')** — the existing mechanism of #61, no new table: `SETTINGS_TEXT_BDD15B` (`WDG1`, `WDG2` A 2.9–8.7; `SLOPE` % 15–40; `HARMONIC_RESTRAINT` %) and `SETTINGS_TEXT_CYL` (`COMPENSATOR` Ω 1.4–10; `INST` A), every setting `IsCatalogueFact`; models `BDD15B` (CGE) and `CYL` (Westinghouse), technology Electromechanical, asset type `ProtectiveRelay`. Field sets from the legacy `SET1` text, profiled read-only 2026-09-12 (BDD15B family 89 rows, CYL 26 rows); **the final per-model field sets are W7's card** | Verified: `compliance.vFactCatalogue` lists six `device.settings.*` facts with their units; both templates Effective, bound to their model's firmware row | W3 |
| 105 | **A fourth gate account, `pnc-gate-approver` (Administrator, Global)**, under #97's standing authorisation, because the Author/Approve segregation needs a second Administrator to observe approval; `gate_runs.py` runs Administrator → Approver → ReadOnly → Hydro, the Administrator run authoring a fresh `W3_GATE_APPROVAL` Draft each run and the Approver run approving it. **W3 is done on the evidence in the gate record**: DEV-header smoke 73 PASS; Windows mode on VM02 as 0.3.0: admin 49, approver 8, read-only 10, hydro 6 PASS, 0 FAIL; `W3_GATE_APPROVAL` v3 Effective with author `pnc-gate-admin` and approver `pnc-gate-approver` | Created on the DC with `samba-tool`, seeded as a platform user through the procedures, `SeBatchLogonRight` on VM02 by `secedit`, SID registered on first sign-in (#95). Verified by query | W3 |
| 106 | **The interpreter: decisions in C#, writes in procedures** (agrees #84, #100; the predecessor's `RuleEvaluation` pattern, consulted). `src/PnC.Api/Engine/Interpreter.cs` walks an instance's block tree against its pinned canonical document, evaluates every expression with `PnC.Formula` over the live catalogue (`DbFactReader` → `compliance.fFactRead`, one call per read) and advances the run through `process.*` procedures — one per state change, each its own transaction: `StartWorkflow`, `StartProcedure` (pins the version set, materialises the root), `MaterialiseBlock` (a foreach member / a repeat pass), `SetBlockState`, `SetStepState`, `OpenHold` / `ReleaseHold`, `ClaimStep` / `ReleaseStep` / `TakeOverStep` / `SaveDraft` / `WitnessStep`, `CommitStep`, `Transition`, `CompleteInstance`. **`Advance` is idempotent**: run after every commit, on the sweep and on demand (§4.1), it changes only what the state and the facts say must change | Atomicity where it matters (the commit) is one procedure; the structural advance is a sequence of small, re-runnable writes, so a crash mid-advance is repaired by the next evaluation. Verified on `PnCPlatform_V2_DEV` 2026-09-12: one `SETTINGS_CHANGE` run end to end over three fixture devices (the W4 gate record) | W4 |
| 107 | **The engine's facts are read in SQL**: `compliance.fFactRead` gains `Engine` branches over the `process` tables — `step.state/outcome/committed_at/committed_by/capture[id, pass?, member?]`, `branch.outcome[id, member?]`, `procedure.outcome`, `package.revisions/revision_count` — with §7's resolution scope: the interpreter supplies the current member and pass when the expression omits them, so *the current member and the current pass first, then the instance* holds. **A parameter name ending in `?` is optional** in the catalogue (`Checker` and the reader honour it). `step.capture` is typed by the document after the read; `procedure.<name>` and `input.<name>` are the run's own bindings (`Produced` / `Inputs`), never a database read | The predecessor's `DbFactReader` was consulted, not copied. Verified: `person.training_current[module='PC_FIELD_SETTINGS'] = true` holds the technician's steps until the attendance exists; `step.outcome[id='CHECK'] = 'Pass'` drives the APPROVE precondition and the repeat; `work.outage_window_start <= @at` releases the hold on the sweep | W4 |
| 108 | **Vocabulary reconciled to the deployed CHECKs, not the design prose**: `document.ConfigurationFile.CaptureKind` is `Designed` / `AsLeftReadback` (the design wrote *Design* / *Readback*); `FileKind` gains **`SettingsText`** (the text file of #61) and `document.SetInService` accepts it; `record.Record.SubjectKind` and `SecondSubjectKind` gain `WorkRequest`, `SettingsIssuePackage`, `ProcedureInstance` | Found by the explore of 2026-09-12: §5.1 named values the CHECKs refuse, and no subject kind admitted a record about a request, a package or a run. Widening a CHECK is a schema change; renaming the deployed values would have touched every carried row | W4 |
| 109 | **The settings-issue package is a Document of the seeded class `SettingsIssuePackage`** (`CharacteristicSchema.DocumentClass`) and a Revision; `procedure.package` binds the Revision RowId; **every configuration file is a revision of the device's `DeviceConfiguration` document** (one per device, created on its first file); **evidence files are a revision of an `Evidence` document per commit**, linked `EvidenceFor` the step's record. The lifecycle of a produced entity starts from the workflows the procedure's `advances` name whose subject kind is the produced kind | `document.File` hangs off a Revision (no record FK), and a package is *a revision whose class is the package* (STEPS.md reconciliation). Seeded in `Seed_config_DocumentClasses.sql`. Verified: 3 package items, 6 configuration-file revisions, 5 evidence links on the gate run | W4 |
| 110 | **Segregation is instance-wide**: `CommitStep` runs `security.CheckSegregation` for this step's `signoff.action` against every distinct prior committer of an attested step in the instance, subject `ProcedureInstance`; **version 2 of `DefaultSegregation`** adds `Calculate/Check`, `Check/Approve`, `Calculate/Approve` on `ProcedureInstance` (WarnAndLog, the design default). **`Apply/Test` is not a pair**: FR-3.1's technician applies and tests the same relay | §3: *any of three engineers who calculated any of eleven devices is barred from the check*. The seeded rules covered none of the procedure's actions. Verified: the Administrator who calculated is refused APPROVE (409, segregation) and the second Administrator approves; the technician tests what they applied | W4 |
| 111 | **The sweep is a hosted service in the API** (`SweepService`, `PeriodicTimer`, interval `Engine:SweepMinutes`, default 15, 0 disables), unattributed — `personnel.ResolveActor` attributes its writes to the platform's System actor. `POST /api/v1/process/sweep` runs it now (Administrator). A hold past `maxDuration` is released `Expired` and logged; **the obligation of #69 waits for the rule engine**, which V2 does not carry yet (`RunRule*` were never added) | The design's *platform setting* has no store; the host carries it, recorded. Verified: the outage hold released on the sweep (`ReleaseBasis Condition`) with nobody signed in as the request's engineer | W4 |
| 112 | **`DRAWING_REVISION` has a one-step placeholder version 1** seeded through `process.AddProcedureVersion` so a root run can pin it (a callee with no Effective version refuses the start, 50141); W5 authors the real procedure as version 2. **`SETTINGS_CHANGE`'s `CHECK` now advances the lifecycle's `Check` (on Pass) and `APPLY` its `Apply`** — the lifecycle declared both and no step fired them | Plan Q5. The example's gap was found on the first run: APPROVE's `Approve` has no path from `Calculated`. The document's description records the change (extends #103) | W4 |
| 113 | **The text reader is `process.ParseSettingsText`**: name=value pairs split on commas and newlines, names matched to the model template's `SettingCode` case- and space-insensitively (`HARMONIC RESTRAINT` → `HARMONIC_RESTRAINT`), the leading number of a value taken as its decimal (unit words after it ignored), range-checked against the definition; unmatched names listed in `ParseError` (`ParseStatus` Parsed / Partial / Empty). **A native (vendor) file is stored `NotParsed`** — no reader exists; the SEL-421 fixture model carries an empty `SETTINGS_NATIVE_SEL421` transform so the binding holds | #61. Verified: the BDD15B and CYL text files parse to 6 settings read as `device.settings.*`; the SEL-421 file is stored NotParsed | W4 |
| 114 | **A witness is a second signed-in person attesting from their own session within 15 minutes** (`WitnessStep`: the session's resolved actor on the step, the attestation logged with its instant; `CommitStep` requires it fresh and by a different person). Never a typed name | SPEC-PANEL M7's alternative; the design's *re-authenticates on the same device* has no mechanism under Windows authentication without a credential prompt the API cannot raise (plan Q1). Verified: RETURN_TO_SERVICE committed by the Administrator, witnessed by the second Administrator | W4 |
| 115 | **Deferred commit is a check-in**: `POST step-instances/{id}/checkin` names the technician by user principal name; the technician's actor is `CapturedByActorId` and `CommittedByActorId`, the session actor `AcceptedIntoPlatformByActorId`, `OccurredAt` the capture instant (`CaptureTimeQuality` 2), competency and segregation evaluated for the technician. **Captured fields stay on the step** (`Draft`, read as `step.capture`): `record.CharacteristicValue` rows need a characteristic template per record kind, which none carries yet | §5.2; the pack's own attestation is FR-7.1's. Verified: the CYL's APPLY committed as `FieldPack` with `smoke.tech` as committer and `smoke.admin` as accepter | W4 |
| 116 | **The migration list is derived, never stored**: `process.vMigrationList` lists every Running or Held instance whose version set pins a version now superseded by a newer Effective one (root or callee) and has no `InstanceMigration` ruling for it. Rulings are `InstanceMigration_Add` rows; the re-plan of §6 step 4 is estimate row B6', deferred | #41. Verified: after a v2 of `SETTINGS_CHANGE` is approved, the half-run second instance is listed (root) and the completed first is not; both stay pinned | W4 |
| 117 | **A step's `advances` is idempotent on the target state**: a transition fired by a step whose target state the instance already holds is a no-op (a foreach member's step advancing a package-level lifecycle fires it once per device) | Found on the first run: three APPLY commits fired `Apply` three times. A person's transition is never a no-op (50170) | W4 |
| 118 | **Read scope for 'Any'-subject views**: a row whose subject kind is outside the five scoped families (a settings-issue package, a procedure or workflow instance) is readable under a Global grant only (IDENTITY.md §5); `fReadableSubjects` is unchanged. The `process` instance views and the engine endpoints decide on the work request the run belongs to (`WorkRequest.*`); step actions on `Record.Modify` | Plan Q4: until W6 maps instances to nodes. Found when `vWorkflowInstance` answered no rows for a package even to the Administrator | W4 |
| 119 | **No UI framework in Phase 1.** The authoring screen is hand-written JavaScript under the shell's CSP (`script-src 'self'`; no inline, no CDN): `definitions.html`, `definitions.js`, the shared `pnc.js` (identity header, JSON calls), `styles.css`; the service worker's shell list names every file and its cache key changes with them | #88 deferred the ruling to W5/W6; the screen needed nothing a library gives (a textarea, a list, five calls). Agrees with #100 "no library". W6's parity screens are the next test; recorded so it can be overturned there | W5 |
| 120 | **The editor is the expert surface of #46 / §8: JSON text, checked by the API's dry run.** `POST /api/v1/definitions/documents?dryRun=true` runs the same compile as a save (schema, grammar, types, canonical text) and then the database's structural rules (`process.ValidateProcedureDocument` / `ValidateWorkflowDocument`), stores nothing and answers every problem with its JSON path (a rule as `rule 5012x`, in its own words); any signed-in person may dry-run, storing needs `Definition.Modify`, approving `Definition.Approve`. A stored version is read back with **every expression printed as grammar text** (`GET …/documents/{versionRowId}`, `PnC.Formula.Printer`, the parser's inverse), so the author edits what was written, not the AST. `/me` carries the permission codes of the roles in force so the screen can say what it will refuse; the API's own check decides. The expression bench (`/formula/check`) stays for one expression | §8 asked for the schema live and the structural checks at save; one dry run gives both live, in one mechanism, with nothing duplicated in the browser. Verified: the edited `SETTINGS_CHANGE` checked, saved as v21, its author's approval refused 409 by the segregation rule, approved by the second Administrator with 15 steps projected — in Chrome | W5 |
| 121 | **`DRAWING_REVISION` v2 carries exactly the legacy documentation track's content**: IDENTIFY_DRAWINGS (drawing references, note → `ScopeDecision`) then RECORD_REVISION (document link, revision label, issued-on date, note → `DrawingUpdate`), the engineer's role, over the work request. Authored in the definitions screen; its text kept at `docs/design/examples/drawing-revision.procedure.json`; loaded and approved by the smoke before the run so the run pins it. **The seed loads the one-step placeholder only while `DRAWING_REVISION` has no Effective version at all** — found on the 0.5.0 deploy, which stored the placeholder again as v3 and approved it, retiring the authored v2 (and again as v5 when the first guard sat before a `GO`; `RETURN` leaves only its batch); a deploy must never overturn what a person authored and approved | The owner's recorded words on drawings stop at a distinct step (FR-3.1), split ownership — the drawing system holds the file, the platform the depiction by drawing key (FR-5.2) — and the legacy fields status, note, date, link (LEGACY-SYSTEM §8). Anything richer (depiction rows by drawing key, a check step, reconciliation findings) is invented until the owner rules; put on a card with this v2 as the example. Verified: the child run committed both steps and completed inside the SETTINGS_CHANGE run, pinned to v2 | W5 |
| 123 | **Most settings changes revise no drawing** (owner, W5 card A/B): `DRAWING_REVISION` v3's IDENTIFY_DRAWINGS commits `Done` with the drawing numbers as text, or `NoneAffected`, and RECORD_REVISION sits in a `choice` taken only on `Done`. Drawing references stay text in Phase 1; the pattern drawing revisions follow "will need to be discussed and documented at some point" — a later card, not now | The owner's words: "most 'setting' changes will not have associated drawing revisions. They will have rationale document changes but not necessarily drawing revs"; "for most, if not all of the changes in stage 1, there will be no drawing revisions". A false precondition at the root would wait forever (Interpreter, the C1 reading), so the skip is a choice, authored, not code | W5 |
| 124 | **No drawing check step now; the drawing office is outside the platform** (card C, D): RECORD_REVISION records what they did — the link, the revision label, the date; no role for them. A check step is a later document edit | Owner: "at the present time, no check is fine. This is the type of thing that the application will need to be able to handle without any coding changes" — it does: the step is a document edit, as #123's choice was | W5 |
| 125 | **The date on the drawings step is the revision date, not an issue date** (card E): `revisedOn`, "the date the revision was made" | The owner read the legacy date as the mark-up date, not the drawing system's issue | W5 |
| 126 | **PCEngineer may author and approve definitions** (card F): the matrix seed gives `PCEngineer` `Definition.Modify` and `Definition.Approve`; the segregation rule still requires a second person to approve what one authored (#110 Author/Approve on DefinitionVersion). **A definition has no node, so the engineer's grant must be Global** (`fHasPermission`: unscoped subjects are covered only by Global, decision 59 / #118): a subtree-scoped engineer holds the code and is still refused — observed on DEV with the Hydro engineer (403 *not held for this subject*) and a Global `smoke.engineer` who authors | Owner's choice among *Administrator only*, *engineers draft, Administrator approves*, *engineers both*. Amends #28's "the owner authors now" and the IDENTITY.md §3 matrix; a seed row, no code | W5 |
| 127 | **The parity screens are hand-written SQL read models plus hand-written pages.** Each screen-shaped query is a view under `<schema>/Views/` granted to `app_execute` (the `vPlacedAsset` pattern): `document.vSettingsRecord`, `document.vParsedSettingNamed`, `work.vChangeRequestStatus`, `location.vFloc`, `location.vFlocScheme` — catalogued by the dispatcher, permission from the schema or a prefix line, read scope from the view's first subject column in the catalogue's preference order (so a view names its subject column deliberately and never names a nullable one ahead of it). The pages are vanilla JavaScript under the CSP (`settings`, `request`, `setting`, `floc`; helpers in `pnc.js`): **#119 re-tested on four screens and kept**. STEPS.md step 18 lists them, with `vPlacedAsset` and `vNodeTree` that W0 carried unrecorded | The dispatcher joins nothing and filters by equality only; a view is the cheaper answer and stays the database's. Nothing in the browser knows a rule | W6 |
| 128 | **The settings grid's grain is one row per designed configuration-file revision**, with `GridState` read from the revision: *Active* (`InServiceFrom` set, `InServiceTo` null), *Archived* (`InServiceTo` set, or the revision/package Superseded or Withdrawn), *Withdrawn* (never in service and the device's member branch left the change — Superseded), else *Outstanding*. A fourth state beyond the legacy toggle, reachable by its own button | §10 rows 1–3 map the package's lifecycle to the toggle, but a package is per change and the grid is per device: the W4 run's CYL left the change while its package went InService, and by package state it would read Active. Legacy dropped such rows (its `D` prefix); here they stay readable. Pending the owner's W6 card (fold into Archived?) | W6 |
| 129 | **CDATE is the design revision's `PreparedAt`** (the Calculate sign-off's commit) **and VDATE is `InServiceFrom`**, set at BASELINE from the RETURN_TO_SERVICE commit instant (§10 row 7); a withdrawn device has none. *Set Verified Date* is that step: claim, a second person's witness (#114), commit — or the check-in of a field-captured date (#115) | #33 (the owner's correction of the two dates); the engine already writes both | W6 |
| 130 | **Parity defaults pending the owner's W6 card: two completion tracks and no user-administration screen.** The software track shows *not modelled — see notes* (#58); accounts are the directory's and roles are grants (estimate §4 option 4). A branch whose block is still *Pending* reads *Not Started*, Running/Held *In Progress*, Skipped-NotApplicable *NA* | Estimate row D says "three tracks" and "user administration"; W6's text and #58 say two; found when the half-run request's tracks read In Progress before the run reached COMPLETION | W6 |
| 131 | **Four `Program.WorkType` keys seeded — SETTINGS_CHANGE, SETTINGS_ADD, SETTINGS_DELETE, SETTINGS_VERIFY —** each bound to `SETTINGS_CHANGE_REQUEST` (§10 row 6); the actions offer them; W7 maps the legacy Type onto them | No work type was seeded (the W4 fixture made one per run); an existing key is never overturned by a deploy. Pending the card | W6 |
| 132 | **NFR-2 is measured by the smoke**: a warm call of each list a screen makes, asserted under one second, the figures in the gate record | REQUIREMENTS NFR-2 *ordinary screens settle within a second*; DEV holds fixture data only (12 stations, 33 positions), so the figures are a floor, re-measured after W7 | W6 |
| 133 | **#118 residual**: the parity views carry `DeviceEntityId`, `WorkRequestEntityId` or `NodeEntityId` and scope correctly for a subtree engineer; `process.vWorkflowInstance` (subject *Any*) stays Global-only for package subjects and no screen reads it directly | Plan Q7: a scoped workflow-instance view is deferred until a screen needs one | W6 |
| 134 | **Definitions and reference data are readable class-wide by any role that carries `Definition.Read`, whatever the grant's scope** (`fHasPermission`: the one exception to *unscoped subjects are Global-only*); definition writes and approvals stay Global-only. IDENTITY.md §5 amended | Owner, W6 card H: found by the VM02 Hydro gate run — a division-scoped engineer was refused `config/vDefinition` (403) and saw empty action-type and model lists | W6 |
| 135 | **W6 card answers on the screens**: a device whose member left its change is not offered by the grid's toggle (the legacy dropped such rows; `GridState` *Withdrawn* stays in the view — card A); the change-request window shows the two modelled tracks and no software card (B; #58); the four action types stay as named (D); a *none affected* drawings outcome reads *Complete* with the outcome shown (E); the legacy columns stay the grid's default (G) | Owner, W6 card, 2026-09-12 | W6 |
| 136 | **Two items scheduled by the W6 card**: a **file-download endpoint in W7** (F — needed to open migrated rationale documents; every open a logged read, FR-6.4) and a **grants screen in W8** (C — people with their roles and scopes, add and revoke), amending #130's default | Owner, W6 card | W6 |
| 137 | **Of the predecessor's migration toolkit (#82), the machinery is carried and the loaders are not**: `common.py` (Run / Provenance / idempotence / flags), `run_rehearsal.py` (one loader now), `catalogue_sources.py`, `SOURCES.md`, the owner's `LEGACY-FIELDS.md` and `mappings/*.csv` rulings; the predecessor's loaders and rehearsal reports stay in its repository. `MIGRATION-PLAN.md` is rewritten as V2's rule ledger (v2.0) | The predecessor's rules contradict V2 in three places (SET1 as NativeSettings vs #61; the two tracks dropped vs #58; the OLD_NO prefix never read, its own recorded defect) and load the predecessor's databases; re-copying them would import the disagreement | W7 |
| 138 | **The importer (`legacy_import.py`) writes only through deployed procedures with `@MigrationRunId`**, one provenance row per target row, idempotent on the source row hash, flags never repairs; the hand-written engine procedures that insert — `StartWorkflow`, `StartProcedure`, `MaterialiseBlock`, `WriteEvidence`, `WriteConfigurationRevision`, `Transition` (its onEnter start) — gained `@MigrationRunId = NULL` passed through to the generated `_Add`s, and `WriteConfigurationRevision` a file-kind override and a status for migrated revisions. A migration run's `Transition` skips the person's role check (no person acts) and stamps the run | MIGRATION-PLAN §1; #85 (the API never binds the run id: the importer is not an API client) | W7 |
| 139 | **The FLOC shape of migrated data**: one Station per legacy location under the division its most frequent `USERNAME` group maps to (Hydro → Generation · Hydro, CCove → Generation · Coleson, Gen → Generation · Belledune, Dist → Distribution, Eng → Transmission — defaults), one placeholder Building per station, one Panel per distinct `(LOCATION, EQUIPMENT)` (the legacy protected-asset level, 1 773), one DevicePosition per base number named from `FUNCTIONS`, the relay Installed there; an ANSI code in parentheses in `FUNCTIONS` → a ProtectionFunction node with a commissioned function, other text → a node-function label | Panels and positions are not in the source; the tree needs Station → Building → Panel → DevicePosition (`Seed_ref_LocationNodeTypeParent`); `EQUIPMENT` (1 773 per station) is what the legacy grid called the asset column. Pending the owner's W7 card | W7 |
| 140 | **Revision chains**: per base in ascending CR, each A/M/P row → a `SettingsText` configuration-file revision from `SET1` (#61), at CDATE (else VDATE, else the capture date with quality 2); **A → Issued, in service from VDATE, open; P → Superseded, in service from VDATE until the next revision's; M → Draft**; an in-service date that does not advance along the CR order is set one second after the prior period's start, quality 2, flagged (several revisions share a VDATE — the chain orders by CR, §5); one `ConfigurationFileRevision` record per revision carries the work request and the overflow columns as its summary; `LegacyRecordNumber` per distinct OLD_NO, `LegacyChangeRequestNumber` and `SapWorkOrder` on the request | CUTOVER-STRATEGY §5 rows for A, P, `SET1`, CDATE, VDATE (#62); the grid's `GridState` (#128) then reads Active / Archived / Outstanding from the revision alone. Found on the first run: `SetInService` refuses a period that does not start after the prior (50247) — 21 of 50 revisions on the fixture chains | W7 |
| 141 | **Open changes land at COMPLETION through `process.LandMigratedInstance`**: the request workflow started and moved to InProgress by the migration run, the procedure pinned and materialised as any run's, every block and step before COMPLETION Skipped with the outcome **`Migrated`**, COMPLETION Running, its DOCUMENTATION / DATABASE branches from the two legacy tracks (Complete → Completed/Done · NA → Skipped/NotApplicable · Change In Progress or missing → Running, which the sweep then continues live); the software track's non-NA rows are dated notes on the request (#58); no settings-issue package (its lifecycle state is not in the source) | #56 (the owner's ruling) made mechanical; nothing about steps 1–12 is invented. Verified on the first 40 chains: the tree reads MAIN Running, nine blocks Skipped/Migrated, COMPLETION Running with both branches Completed/Done for a Complete/Complete request | W7 |
| 142 | **Findings and drops**: a seeded finding category `MigrationReconciliation`; the 17 chains where an archived CR exceeds the active CR → a Major, Open finding on the device with both CRs (#59); the 2 361 D rows and the two `2440` rows are counted in the reconciliation and never written (#31, #60); header and track rows of dropped or unknown chains counted, duplicates folded | The reconciliation is the gate: every input row under exactly one rule | W7 |
| 143 | **Legacy users become persons, not accounts**: `Users` → `personnel.Person` (name split, level in notes), no `security.User`, no password; a request's `Reqested By` matched by name, else kept as text and flagged | Identity is the directory's (FR-6.1); CUTOVER-STRATEGY §5 "Persons, not accounts" | W7 |
| 144 | **The file download endpoint `GET /api/v1/files/{fileRowId}`** (the W6 card's F, #136): `Document.Read` on the file's document, bytes from `document.FileStore` by stream id, the row's SHA-256 verified before anything is sent (500 `integrity` otherwise), a redacted or redaction-pending file refused 403, an archive-tier file 404, **every open a logged read** (`document.File` in `config.ReadLoggedClass`; `audit.LogRead`, FR-6.4); the setting display's file names link to it | SCHEMA-DESIGN §8.1 "compared on every read"; the only route that returns bytes rather than JSON | W7 |
| 145 | **Read models over the real estate: correlated lookups read the base tables in the filtered indexes' own form, and a listed view is ordered and paged in the API, not in SQL.** Measured on the migrated estate (11 600 revisions, 6 900 positions): an `OUTER APPLY … TOP (1)` against a generated current view (ROW_NUMBER over the entity) is evaluated over the whole table per outer row — a page of the grid 30 s, an offset page 108 s, the FLOC view 28 s; the same lookup on the base table with `ValidTo IS NULL AND IsDeleted = 0` (the indexes' predicate) seeks, and the whole grid reads in 1.1 s; but any ORDER BY / OFFSET / TOP / SELECT INTO over the view still cost 10–120 s, while the plain SELECT ran a parallel plan. So `Api:MaterialiseBeforePaging` names the read models the dispatcher reads whole and orders and pages in memory; `Api:MaxTake` is 10 000 so a screen reads its whole list in one call. Three indexes added (`record.Record` by second subject, `process.BlockInstance` by member, `location.NodeFunction` by node). Whole-estate lists now answer in 2–3 s, a station's in 1.4–1.6 s, a single entity under a second | NFR-2 asks for a second; W6's figures were on fixture data (#132). What remains for W8: a sargable station on the read models (the whole view still materialises before a station filter applies) | W7 |
| 146 | **The engine's clock is the database's.** Every instant the API passes into the engine — a transition's guard evaluation, a step commit, the sweep — is `SYSDATETIMEOFFSET()` read from the session (`SqlSession.NowAsync`), never the host's clock. Found by the W7 API smoke over the migrated estate: the build laptop ran 1.2 s behind the SQL Server; `package.revision_count` read at the host's "now" did not yet see the three package items the database had written at its own "now", and Approve was refused as false — the API-side twin of #79 | A fact written by the database must be visible to a guard evaluated a moment later, whatever the host's clock; the platform's one clock is the database's (#5) | W7 |
| 147 | **A scoped list read carries `OPTION (RECOMPILE)`.** The dispatcher adds the hint whenever a read is joined to `security.fReadableSubjects` (every list read of a scoped view). Observed on DEV: the Hydro engineer's read of `asset.vAsset` timed out at 30 s while the same statement run directly answered in 0.2 s; clearing the database's plan cache made the API's read answer in 0.2 s too, so a cached plan compiled for another readable set (or before the estate was loaded — UNVERIFIED which) was serving the wrong one. The recompile costs ~0.1–0.2 s a call on the migrated estate, measured | The readable set is the grant's, from 55 assets under a subtree to the whole registry under Global; one cached plan cannot fit both, and a 30-second timeout on a technician's list is worse than a tenth of a second on every list | W7 |
| 148 | **The W7 card, round 1 (owner, 2026-09-13), applied to the importer.** *H — a superseded record whose change-request number disagrees with its header:* the SETTINGS row's CR stays the change (the chains keep their order); the header's type, requester and SAP order and the two track rows are taken from the relay's own rows (the latest CR at or below the SETTINGS CR, else the earliest above), flagged `HeaderUnderOtherCr` / `TrackUnderOtherCr`; and **every A or P row with a track row is landed** through `process.LandMigratedInstance` like an M row, so a historical change reads with its legacy track states; the sweep completes the runs whose branches are terminal and `legacy_import.py --close` then closes their requests (a separate call, after the sweep, so the rehearsal's second pass stays at zero). A landing is keyed by row (`Landing:<OLD_NO>/<CR>`: an OLD_NO repeats within a base — P0005 carries eight CRs); a request whose rule changed is revised in place under a new provenance row. *C — station numbers two locations share:* "an error in the data that the user must mitigate" — the station left without a number gets a `MigrationReconciliation` finding (2). *D — technology:* kept as assumed, correctable in the model list; the P&B / Agastat CNT 35-96 timer is Static (its seven label variants in `device_model.csv`). *F:* the calculated date stands in for a missing VDATE, marked unknown (as built). *G:* a legacy change request covers exactly one relay (verified: 11 848 CRs, none with two SETTINGS rows) — one request per row, as built. *B:* the FLOC shape kept. *A — divisions:* the owner reads the tree as ownership (Transmission / Generation / Distribution, then the company) and not as the USERNAME groups; which stations belong to which owner is not in the source, so the mapping stays as loaded and flagged and the next card carries a station-by-station markup; users work across all three, which the grants model already allows. *E:* the first templates are SEL-221F, SEL-311C and SEL-551 — a W8 item | The owner's rulings on the loaded estate; each applied where the source supports it and put back on a card where it does not (A) | W7 |
| 149 | **W8 round 1 — the shape of QA and the cutover rehearsal.** `PnCPlatform_V2_QA` is created on VM01 by `deploy.py --database PnCPlatform_V2_QA --fresh --package dist/<version>` (the same package as DEV: `platform.Release` versions are global, each database carries its own `platform.Deployment` row); the gate accounts become its users through `tools/ot/gate_users.py` (the hand seeding of #97 made a script); VM02's site is **repointed** to QA, not rebuilt — the workflow's §4 assumption (VM02 and VM07 rebuilt clean) is overturned to *reused*, because a VM rebuild is infrastructure this session cannot perform (card). The cutover rehearsal's "client's copy" is `dbRelayManagement_Legacy_Cutover`, made by `docs/schema/migration/make_cutover_copy.py` (the eight tables copied whole, then one row added, one changed, one deleted per keyed table — direct writes to a scratch legacy copy, never to a platform table); `cutover_diff.py` finds exactly them (keyed tables report the change; whole-row tables report it as an add and a delete, as §3 keys allow); the delta is applied by the importer's `--source-db` re-run, adds and changes through its idempotent rules, deletes reported for a person (Q8). Found on the way: the diff suffixed duplicate natural keys in row order, so a copy that returned them permuted read 300 unchanged track rows as changed — duplicates are now suffixed in hash order | CUTOVER-STRATEGY §3; PHASE-1-WORKFLOW W8; the sole path this session has to VM01 and VM02 | W8 |
| 150 | **The grants screen** (`/grants.html`, #136 amending #130): people with an account (`security.vUser` × `personnel.vPerson`), their grants (`security.vGrant`) with the scope node named, add through `security.Grant_Add` (role from `security.vRole`; Global, or NodeSubtree with a division or station), revoke through `security.Grant_Revise` with `RevokedByActorId` and a reason — the row stays. A grant names who granted it, so `/api/v1/me` now carries the session's own **`actorId`**, resolved by `personnel.ResolveActor` from the session context, never an id the page picks. One person holds several grants — under all three owners where their work spans them (W7 card A). Reading needs `Grant.Read`, changing `Grant.Modify`: the Administrator's alone (IDENTITY §3) | Owner, W6 card C; W7 card A | W8 |
| 151 | **A landed track's date is the legacy row's.** `process.LandMigratedInstance` takes `@DocumentationAt` / `@DatabaseAt` and a Complete branch runs and completes at that instant; the importer passes the track's `Date`. Landings made before (DEV, 2026-09-13) keep the capture instant — a landed run is not re-shaped; QA's fresh load carries the dates | Found on the request window 2026-09-13 (P0002 read 2026-04-22 for a 2008 completion) | W8 |
| 152 | **The geographic map is a gap, put to the owner, not built silently.** SOW §5.7 places a graphical map of terminals, substations and generating stations *in scope, on the acceptance path*, and acceptance criterion 7 is its display and navigation — yet no requirement, wave, design document or decision mentions it, and the legacy source holds no coordinates (LOCATIONS is a name and a group). W8's card carries the three honest shapes with examples: a provincial outline with station pins from a coordinates file NB Power supplies (offline, in OT); a tile map, which the OT hosts cannot reach; or Phase 2 with a documented rationale under the SOW's own rule that nothing is dropped silently | `docs/contract/sow-baseline.md:95-100, 111-120`; found in W8 planning | W8 |
| 153 | **The W8 card, round 1 (owner, 2026-09-13), applied.** *A — station owners:* the owner marked all 231 stations Transmission / Generation / Distribution; the markup is `mappings/station_owner.csv`, the importer places a station by it (a fresh load), and `apply_station_owners.py` moved the loaded ones through `location.MoveNode` with provenance (DEV and QA: 158 moved, 73 already placed) — a single **Generation** division under NB Power replaces the three seeded guesses (Hydro / Belledune / Coleson stay, empty but for the smoke fixture); the owner named two more owners — **Industrial** (a division: substations industrial customers own) and the merchant generators **Caribou Wind Farm** and **TransAlta** (Owner nodes with a Generation division each) — seeded empty, because which stations are theirs is not in the source; and asked whether a user can correct the tree: the Locations screen now moves a station to any owner's division and adds a division, under `Node.Modify`. *B — templates as found:* SETTINGS_TEXT_SEL_551 (73 names), SETTINGS_TEXT_SEL_311C (116), SETTINGS_TEXT_SEL_221F (32), every name the legacy text carries spelled as SEL's software spells it (the owner: no translation between this platform and the manufacturer's), Decimal where every value was a number, else Text, no units or limits yet; `reparse_settings.py` re-parses the migrated files; a fresh load reuses a seeded model. *C — the map:* a provincial outline with station pins, after acceptance, from coordinates NB Power supplies. *D:* "rebuild" meant the application deployed on its platforms, not the VMs — QA as made is the intent. *E:* `/health` from VM07 as expected; the admin share was unreachable, so the smoke is now a read share `\vgs-vm02\pncsmoke` for domain users. *F:* the database package and technical architecture first, Word files generated from the Markdown plus PDF, draw.io as the approved equivalent of Visio. *G:* deletes reported, a person soft-deletes | The owner's rulings; each applied where the source and the platform allow it and seeded empty where the owner's own hand is needed | W8 |
| 154 | **A materialised read model takes its equality filters in memory** (#145 addendum). Found on QA at 0.8.2 by the gate runs: the grid's *Archived* list timed out at 30 s on VM02 while *Active* answered in 1.6 s; reproduced in SQL under both logins — `vSettingsRecord` with `GridState = 'Archived'` pushed into the statement and every column selected ran 42–54 s, the whole unfiltered view 0.6–1.7 s. So for a view in `Api:MaterialiseBeforePaging` the dispatcher keeps only the read scope in SQL and applies the query string's equality filters to the rows in memory (text and identifiers ordinal-ignore-case, numbers by value, `null` as null); the Archived list then answers in 2.4 s through a local host (measured against DEV — the probe host meant for QA had read DEV, `appsettings.Local.json` outranking its environment override; found 2026-09-13 22:10, #156). The parser fix for a repeated setting name and this ship as 0.8.3 | Measured; the class of #145 — the optimizer over this view is not to be trusted with a predicate on a computed state | W8 |
| 155 | **W8 round 2 answered (owner, 2026-09-13 evening, by chat — the card's saves stopped after its first item, recorded into its store by the session).** T1 the station move on QA *worked as described*; T2 the smoke from VM07 as VGS01 `SMOKE PASS: 81 passed, 0 failed, 10 skipped`; T3 the SEL template names *kept as found*; T4 the documentation *proceeds as answered in round 1* — the database documentation package and the technical architecture first, Word generated from the Markdown plus PDF, draw.io diagrams as the approved equivalent of Visio | The owner's replies, verbatim where quoted | W8 |
| 156 | **A migration defect found while choosing the review rows, and the answer to it: QA is reloaded fresh before anyone reviews it.** The importer's revisions stage keyed a base's row detail (SET1 text, CDATE, VDATE, the overflow columns) by OLD_NO alone; a base's P rows share one OLD_NO (1 265 bases, 3 579 rows), so every superseded revision of such a base carried the *last* P row's text and dates — chain 3939 read 2006-08-14 for a revision whose VDATE is 2001-01-16, chain 0005's nine revisions all 2022-09-13. The A and M rows, the requests, tracks and landings were right; the reconciliation totals could not see it (one row per source row was written, with the wrong content). Fixed by keying on (OLD_NO, CR). A loaded revision is not re-shaped (#148), so the fix takes a fresh load: `PnCPlatform_V2_QA` is recreated from `deploy.py --fresh` and reloaded before the owner's review; DEV likewise. The W7 gate's chain check (40 chains) looked at states and counts, not at each revision's dates against the source — a check on the dates of every chain is added to the reconciliation (`verify_chains.py`; on DEV before the reload: of 11 563 revisions 1 552 carry another row's text and 970 another row's in-service start) | Found 2026-09-13 20:5x; the owner asked whether the application was ready to review — it was not, in this respect | W8 |
| 157 | **W8 round 4 answered (owner, 2026-09-14, card `cards/w8-ux-round4`): the legacy shape on the platform's screens, with progressive disclosure.** The owner stopped the W8 review because the V2 screens were "nothing like" the legacy program; the four reference applications (the legacy C++Builder program, the predecessor V1, pnc-platform, Dev_Final) were then built and run on the build laptop so every screen could be compared against the running original, not a description. Rulings: main screen = a location list first, then collapsed groups, rows only under an opened group ("no devices are shown until the user selects which equipment they want… always the most user-friendly path"); the record page shows the settings text in full; the change request's tracks side by side with read-only status rows; the legacy right-click commands (Set Verified Date, Request Change, New Setting) as a context menu; the location report as a page that is its own preview; the theme follows the system. **No legacy record numbers on screen** ("Axxxx, Mxxxx and Pxxxx… I want to migrate away from this"): the migrated asset name's bracketed base number is hidden on screen (`settings.js`, `LEGACY_NO`) and still matched by the text filter; the numbers stay as alternate keys. **No SET1 / SETTINGS2 on screen**: those fields are going away; the record shows the filed settings text. **No schema change in this pass unless an issue is found and discussed first** | The owner's answers on the card and by chat, 2026-09-14; verified against the running legacy program (`C:\Projects\legacy-sm\Setting_Management_CB12`, rebuilt with RAD Studio 13) | W8 |
| 158 | **W8 round 5 answered (owner, 2026-09-14 18:57Z, card `cards/w8-ux-round5`, rebuilt around Dev_Final's `pilot/hardening-2026-06-12` branch at the owner's request): the settings book groups by functional scheme; the one view exception.** B10 *scheme groups*: "the functional-scheme group is really the Equipment field of the old database except that it is more appropriately named… a RAS is not a physical thing that needs protecting… the concept is just clearer". Done without a table or column change: the migration raises one `scheme.Scheme` per (station, EQUIPMENT) text, typed by a seeded `Program.SchemeType` `LEGACY_EQUIPMENT_GROUP` (`Seed_config_SchemeType_Legacy.sql`), and makes every migrated position a member through its protection-function nodes and its installed asset (role `Member`, seeded into `ref.SchemeMemberRole`) so a position whose FUNCTIONS carried no ANSI code belongs too (`legacy_import.schemes_stage`); the panel stays as the physical placeholder (#139). **The exception the owner approved in plan mode**: `document.vSettingsRecord` gains `SchemeEntityId, SchemeName` — a view revised, no table touched — so the grid reads its group in one pass instead of a browser-side join. Also: B1 *lines that expand* to the card; B2 *field filters and a grouping choice*; B11 *a curation screen* (`schemes.html`: rename, move, merge into, new, add the unplaced — through `Scheme_Revise`, `SchemeMember_Revise`, `AddSchemeMember`; an emptied scheme is retired, never deleted); B4 *compare two revisions*; B5 *a request queue with counters, the stage bar read from the workflow definition, the notes block*, and a badge on an in-service row whose device has an outstanding request; B6 *a workflow templates list and a form over the definition*; B7 *an asset page*; B8 *device types and setting templates as screens*; B3 *the rationale as a definition with sections* (releases 0.9.1–0.9.2, planned); Export CSV and the rationale PDF in Phase 1. **Later waves** (B9, B12): the operations dashboard, IEC 61850 / SCL templates, NERC CIP / security perimeters / training, notifications and subscriptions, the logic viewer / wiring / drawing defaults, vendors and dashboards, and maintenance policies with work orders ("extremely important in later phases when maintenance, compliance activities etc. are being looked at") | The card's answers read back from its store; the plan of 2026-09-14 (releases 0.9.0 → 0.9.2) | W8 |
| 159 | **Documents move to the FILESTREAM file store in a later wave (W9), not in 0.9.x.** The legacy Documentation button opens a Word file by folder path; until then the record's Documentation button opens the files linked to the record (`setting.html`), and the request's *Go to document* opens the documentation track's link | Round 4 minor screens; the owner's B3 ruling makes the rationale a definition first | W8 |
| 160 | **An application-configuration form (the legacy Program Setup dialog's successor) is a later wave.** Connection and folder settings are the host's (appsettings, `server.ini`-style files are not carried); nothing on screen until there is a setting a user, not an administrator, changes | Round 4 minor screens | W8 |
| 161 | **B13 stays open for discussion: a "data completeness" screen after migration.** The owner: "the new db is much more data rich than the obsolete db which results in the new db having a lot of 'holes' that will need to be filled in afterwards and it would be very valuable to have a place where users could see all of the places that need to be mitigated". Proposition for round 6, shown as an example: a page per station reading `migration.vPositionCatalogue` / `vStationCatalogue` dispositions and the `FLAG:` kinds in `migration.vProvenance.Notes` (21 kinds on the 2026-09-13 load), with counts and the rows to fix — the P&C group's "ongoing cleanup" worklist (B7 note). Not built until ruled | The owner's B13 note; the migration views that exist today (no flag table) | W8 |
| 162 | **The API's read scope is a join to the readable set, not an IN (subquery) predicate.** `SqlSession.QueryViewAsync` joins the view to `(SELECT DISTINCT SubjectEntityId FROM security.fReadableSubjects(…))` for the five scoped families; the `Any` family keeps the IN form with its Global-grant clause (#118). Measured on the fully loaded DEV estate on 2026-09-14 with typed parameters as the API binds them: the IN form 100–170 s for the whole settings book once the migration's landings had filled the process tables (the optimizer pushed the set into the view's correlated lookups — the same view read unscoped in 1.7 s), the join form 1.9 s; the station read 1.2 s, the FLOC view 1.6 s, the request list 3.9 s through the API afterwards. The read scope's meaning is unchanged (IDENTITY §5): the same rows, decided by the same function | NFR-2 (a screen within a second, a search within two); the W8 round-2 timing checks (#154) in the smoke | W8 |
| 163 | **The front end is React (supersedes #119, #127's "hand-written pages" half); direct saves with audit on an outstanding record; the reference applications are the floor; the cards are retired.** The owner reviewed 0.9.0 on 2026-09-15 beside the legacy program and Dev_Final and found it "nowhere near" either: thin read-only pages, every write routed through the engine. Rulings, in the terminal (the card mechanism is retired — rulings and discussion happen here, and behaviour is reproduced from the reference applications' screens and code, asked about only where the two disagree): (a) the legacy program and Dev_Final are the *low bar* — every function either has is duplicated, and more; (b) an outstanding record or request is edited by **direct saves with audit** (fields, the settings text, the three track statuses with dates and notes, the request header, the verified date); the engine raises and completes — it does not sit between a person and a field; (c) the legacy fields with no column (class, use, responsibility, element, line type, number of relays, CT/PT ratios) are **characteristics** (`config.CharacteristicDefinition` + `document.CharacteristicValue`) — "I don't see any value in indexing, grouping etc. on them"; (d) **React 19 + TypeScript + Vite + Tailwind 4 + react-router 7 + TanStack Query 5** in `src/PnC.Web`, modelled on Dev_Final's `web/`, built by `npm run build` into `src/PnC.Api/wwwroot/app` (ignored in git; `package_release.py` runs `npm ci && npm run build` and lists the lockfile's packages in `sbom.json`), served under `/app/` by `MapFallbackToFile("app/{*path:nonfile}")` under the unchanged CSP (no inline script or style; React's `style={}` goes through the CSSOM). The plain pages stay until each screen is ported; the shell links to them. Build order accepted by the owner: 1 shell and shared parts, 2 settings book, 3 record page with editing, 4 change request raise / tracks / complete / withdraw and the queue, 5 new setting and verified date, 6 print, 7 asset registry, 8 curation / device types / templates, 9 files per revision, 10 workflow templates editor, 11 work orders / maintenance / operations / notifications, 12 IEC 61850 / logic viewer / hardware / wiring | The owner, 2026-09-15: "the functionality isn't anywhere near the same as either the obsolete windows application or the Dev_Final"; "direct saves with audit, and switch to React"; "start item 1". #119 was recorded "so it can be overturned" | W8 |
| 164 | **The legacy A, M and P are one procedure, and the migration's one request per legacy row stands.** Owner, 2026-09-15: A = the in-service settings, M = the change request against the in-service device (Mxxxx_cr, made by "request change" on the A), P = the archived settings (Pxxxx_cr); completing the request makes the A the P and the M the A. Verified in the source database (2026-09-15, read-only): a settings row's `Change Request ID` is stamped when the row is created and never reused (A 5 641 / M 357 / P 5 850 rows = 11 848 distinct numbers; 0 of 2 356 chains put an A's number on a P), and an A's number is the number of the M that became it (147 A rows' headers still say M). So every A and P row was an M with its own request once — the migration's one `work.WorkRequest` per legacy number (R-08) is right, and "the A this request retired" is the device's previous revision, shown on the work item, not re-keyed. Fixed on DEV 2026-09-15 (increment 5): an A or P row with no legacy track row now lands as a completed change with both tracks NA (`NoTrackRows`, 6 721 rows: A 5 636, P 1 085 — `RECONCILIATION-2026-09-15-DEV.md`), `run_rehearsal.py --close` runs the closing pass, and the pass closed 11 574 requests (queue: 11 580 Closed, 272 In progress, 5 Cancelled). **A second correction found by the pass**: 105 M rows whose two legacy tracks read Complete/NA had their requests closed too, though their revision never went in service — in the legacy program a request is complete only when its M becomes the A (the dropped software track was that third check, #58). `close_landed` now skips M-row landings; DEV keeps the 105 until its next fresh load (no reopen transition exists, and a migration correction must not fake one). Until then DEV's queue shows 272 open requests against 355 outstanding revisions | The owner's correction of my reading; the queries in the session; `CUTOVER-STRATEGY.md` §5 | W8 |
| 165 | **Screens are definitions: a small set of screen kinds in code, each bound by a `Program.Screen` document to its data, columns, commands and procedure; the work item and the step are generic.** The owner, 2026-09-15: the group must be able to create a procedure — a settings change, a CIP standard change, a transmission fault analysis — *and the screens that carry it*, without code per process or device; refined the same day to "several standard screen designs in code which the admin selects and binds". Built: `docs/design/screen.schema.json` (kinds `settingsBook`, `list`, `workItem`, `step`, `record`; commands `openScreen` / `raiseRequest` / `transition` / `openStep` / `url` with an equality `when`; no formula expressions); the definitions API keeps its document kinds in a table (`DefinitionEndpoints.cs`: procedure, workflow, screen — a new kind is a row plus a `ref.DefinitionKind` seed row); `GET /api/v1/screens` lists the Effective screens the person may open and the sidebar is built from them; `GET process/step-instances/{id}` returns a step's state, claim, draft and definition node in one read. The work-item kind draws any procedure instance's block tree (parallel branches as the tracks, holds, foreach members) and the workflow document's states; the step kind draws any step's capture fields, evidence, outcomes and sign-off and performs claim / draft / witness / commit / check-in. **The proof**: the owner's four-step procedure (`examples/settings-change-simple.procedure.json`, its two workflows and its work type) authored through the authoring API with no code, and one change on a Bathurst Terminal relay run end to end through the generic screens on DEV — the new revision Active, the old one Archived, the request Closed (`evidence/W8-fourstep-*.jpg`). What still needs code: a new vendor file parser, a genuinely new table, an integration, a report the first time. The hand-coded React state is kept at tag `react-handcoded-2026-09-15` / branch `keep/react-handcoded` so the path can be abandoned if it proves too complicated for users (owner's request). Supersedes the hand-written pages of #127 and #163's item order (items 3–5 and 10 became this) | The central requirement (no hard-coded procedures) applied to screens; the owner's rulings 2026-09-15 | W8 |
| 166 | **A text settings file for a relay whose model has no settings template is kept as filed, `ParseStatus = NoTemplate`, never refused.** `process.ParseSettingsText` threw 50181/50182 when the device's firmware or model named no `Transform.SettingsParse`; the four-step proof hit it on an MCGG22 (most of the electromechanical estate has no template yet — the migration stored its text unparsed). The legacy program kept any text; the platform keeps it too and parses it when a template exists. Applied to DEV by ALTER on 2026-09-15; ships with the next package | The references are the floor (#163); #61 (text settings parsed by a per-model template) is unchanged for models that have one | W8 |
| 167 | **The settings record's columnless legacy fields are characteristics of the revision, in one seeded schema; the record screen is plain code.** `CharacteristicSchema.RecordTemplate` definition `SETTINGS_RECORD` (`Seed_config_CharacteristicSchema_SettingsRecord.sql`): class, use, responsibility, bulk power element, protection group, element, line type, number of relays under *Classification*; the four main and four auxiliary CT ratios and the two PT ratios under *Instrument transformers*. The record screen (`src/PnC.Web/src/screens/RecordScreen.tsx`, screen kind `record`, definition `SETTINGS_RECORD` naming only its view and key) lays out whatever the schema carries by display group, reads a migrated revision's values from its record summary until they are migrated into values, and saves a value on an **Outstanding** revision directly (`document.CharacteristicValue_Add` / `_Revise`, audited) — the direct save with audit of #163; an in-service or archived revision is the record and is read-only. The screen carries the 0.9.0 record page's every function (facts, notes, parsed settings, the text as filed, files and records, Compare) and replaces it; with the work item and the queue in React, `settings.html`, `requests.html`, `request.html` and `setting.html` are retired (service-worker shell `shell-10`). Rule applied (owner, 2026-09-15): a one-off screen is React with its constants, not a parameter grammar — the screen definition names the data and the kind, nothing more | The owner's ruling that these fields are characteristics ("no value in indexing, grouping etc. on them"); #163; #165 | W8 |

| 168 | **The first device template: the SEL-221F carried to completion before any modern relay — the settings by function, a reader and a writer per vendor format proven by round trip, the template as the daily-value work.** The owner, 2026-09-16: engineers will not author JSON and procedures seldom change; what the staff use every day is the device — its settings grouped by function with the inputs the model has, the standard settings marked, the NERC/NPCC classifications, the compliance calculations and documentation ("everything needed to manage the device"). And the hard requirement: settings are edited **in the platform** and at implementation the platform **writes the manufacturer's native settings file** with no retyping; a vendor whose format cannot be written is excluded from procurement. Rulings: an aliases column on the setting definition; no standard settings for the 221F (deferred); the classification → requirement logic is generic (BCA → CIP, NPCC A-10 → Directory 4, the PRC-023 list → a loadability calculation from the settings) and how each is handled may be custom; the 221F's file is the name=value list in SET order. **Built (increment 1):** `config.SettingDefinition` + `DisplayOrder` (the SET order, the writer's order), `Aliases` (legacy spellings, matched case-insensitively) and `Format` (`decimal:N` / `integer` / `text` / `mask3`); `document.ParsedSetting` + `RawValue` (the value text as filed — what makes a platform-written file byte-identical on re-parse). `process.ParseSettingsText`: aliases, chained `A=B=C=value`, the `LOGIC SETTINGS:` marker, a malformed fragment (`PSVC=S 27VLO=40`, a separator missing) read at its first `=` and noted, a re-parse closes the prior reading in valid time. `process.RenderSettingsText` (new, the writer; `GET /api/v1/settings/{revision}/rendered`). `compliance.fSettingFactValue` reads `SettingsText` revisions too (every text-settings device's `device.settings.*` facts were NULL — a defect). **The template** (`tools/template_sel221f.py`, one source → `Seed_config_SettingsTemplate_SEL221F.sql` and `docs/design/examples/templates/sel-221f.template.md`, the artefact the owner reviews): 56 rows from the manual (IM 981207, `Z:\Archive-WorkingData-Cloud\Work\DATA\Manuals\SEL\221F-2-3-4_IM_19981207.pdf`) — ID, the 42 SET settings, BFIN1/BFTD (the -3/-4), the ten logic masks — grouped by the manual's Specifications headings (twelve groups; the Section-5 heading and page cites in each description; a first cut by the 22 Section-5 headings gave 22 tabs and was replaced), with unit, base, primary range, closed list (seven enumerations), ANSI function, aliases (`Z1`→`Z1%`, `RO`→`R0`, `XO`→`X0`, `Z3DP`→`Z3D`) and format; the manual's two internal inconsistencies (CTR 1–6000 vs 1–5000; 25DV 0–125 vs 0–150 secondary) recorded in the descriptions. Bound to the model codes `SEL-221F Z1-3=.125-64 OHMS` and `SEL-221F`; the `SEL-221S` codes are **not** bound (their texts carry 67ND and other names the 221F has not — a different relay, a finding, not a merge). `Seed_ref_AnsiFunction_Core.sql` gives the codes the template cites their C37.2 names (the migration had named them from legacy position text). **Migration:** the filed text is `SET1` + `", "` + `SETTINGS2` — the legacy split a list at 255 characters and #140 filed the head only (51 of the 53 Active 221F texts were truncated; the tail, with the logic masks, sat in the record summary). **The round trip** (`docs/schema/migration/roundtrip_settings.py`): for every revision `render(parse(text))` against the filed text — identical / values-equal (the differences listed: the legacy users typed 14 different orders and spellings) / mismatch; a platform-written file must be identical (the strict rule, increment 2). Before the reload, on DEV's 53 Active 221F texts: 19 identical, 34 values-equal, 0 mismatch. **The device sheet** (`DeviceSettings.tsx` inside `RecordScreen.tsx`, plain React per #167): what the relay does as function chips from the template's ANSI codes, the inputs from its ratio settings (the manual's words in the definitions), a tab per group with value / unit / range / out-of-range and the manual's words per setting, a SHOWSET-like listing, and the file the platform writes beside the file as filed; the generic instrument-transformer characteristics hidden when the template carries the ratios. Nothing per model in code: a template for another relay draws the same way. Data findings for the owner: the `SEL-221F` model code carries texts of other relays (ZD2L, Z3DG, TAP, T.D. — Partial); three texts are malformed at the source. Deferred to the next increments: editing in the platform and the M as a copy of the A filed as rendered text at the settings step (2); classifications and the generic obligation rules with the PRC-023 worked example, content from the owner (3). **Increment 2 (built 2026-09-16, evening):** `process.SetParsedSetting` (one value of an outstanding revision, read as the parser reads it — type, primary range, closed list — the prior row closed in valid time, audited with code/from/to; `ConfigurationFile.Modify` on the device over HTTP); `process.CopyRevisionAsDraft` (at the request step, every device named by a captured Device set gets a draft copied from its in-service revision, parsed, in the package — the legacy M from the A, Outstanding at once); `process.IssueRenderedSettings` + `process.RefileRevision` (a settings step committed with no file writes the draft's rows as the model's text and files it as the revision's Native file, replacing the copy's, then reads its own file back through the parser — the round trip in place; `evidence.required` is satisfied by the platform-written file; a vendor file attached over the copy re-files the copy, one outstanding revision per device per package); the edit cell on the device sheet. Proven twice: the smoke's #168 section on the BDD15B fixture, and the owner's four steps on Bathurst 0012 B-PROT SEL-221F through the screens (`evidence/W8-fourstep-221f-*.jpg`): request → revision 3 outstanding as a copy of revision 2 → Z1% edited 21→22 on the sheet (three audited edits) → rationale attached, settings step committed with no file: `A0523_875199 (2).txt`, 510 bytes, byte-identical to the rendered text, re-read Parsed → hold released → installed as the technician → completed: revision 3 in service from 13:12, revision 2 archived, the request Closed; the Active round trip then reads 1 identical (the platform's file), 52 values-equal, 0 mismatch. **The fresh DEV reload** (`RECONCILIATION-2026-09-16-DEV.md`): 437 358 rows in 7 304 s, second pass 0; every text SET1+SETTINGS2 (11 554 equal, 0 differ); 949 revisions parsed; the 221F round trip 181 revisions values-equal, 0 mismatch, 0 unparsed after template v4 (alias MT0), the parser's tolerant marker, the missing-separator rule (a space, a known name and '=' is a comma the legacy user missed — noted per record) and the refined chain rule (a misspelt single word in a chain is a typo, the other names keep the value); the round-trip tool compares the filed text with the stored rows, not with a re-parse of the rendered text, which had hidden a lost chain. Parser rules and their tests live in `ParseSettingsText.sql` / `roundtrip_settings.py`; the data findings (typos, foreign texts under the `SEL-221F` code, missing separators) are the records' own, listed in the reconciliation | The owner's direction and rulings of 2026-09-16; the manual; the DEV survey of the 221F texts; agrees with #61 (a per-model template) and extends it with the writer; supersedes the reading that data flows only from the device file to the platform | W8 |
| 169 | **A hand-written read model reads its lookups from the base tables in the current-row form, never through a generated current view; a scoped read of a materialised read model pins its join order.** Found on the reloaded DEV estate (2026-09-16): `document.vSettingsRecord` joined `work.vWorkRequest` and `process.vWorkflowInstance` (generated current views, ROW_NUMBER over the entity) and the optimizer evaluated the request view once per outer row — the whole WorkRequest table scanned and sorted 11 572 times, 137 million rows, 48 s for the view that had run in 1–6 s the day before (a plan the fresh statistics flipped). Rewritten as `OUTER APPLY … WHERE ValidTo IS NULL AND IsDeleted = 0 … ORDER BY RowSeq DESC` (the filtered indexes' own form, as the view's other lookups already were): 1.2 s. Then the API's scoped form of the same read (the readable set joined to the view, `OPTION (RECOMPILE)`, #158) still ran past 200 s in every free shape — join, `IN`, a table variable — because the optimizer drove the view's correlated lookups from the readable set (6 879 assets); `FORCE ORDER` (or a hash-join hint) kept the written order, the view first, and measured 1.7 s. `SqlSession` now adds `FORCE ORDER` to the recompiled scoped read of the views in `MaterialiseBeforePaging` (the hand-written read models); generated views keep the optimizer's freedom. The settings grid through the API: 1.6 s (500 rows). Also: a copy made at the request step belongs to its request before any record points at it — the view finds the request through the request-confirmation record that names the package (`prec`), so the work item lists the copy at once. **The record screen's sections are tabs (owner, 2026-09-16, on seeing the sheet): Settings, Classification, Notes, Text as filed, Files and records, Compare — nothing sits under the settings tabs whatever tab is chosen (it read as part of the settings). Follow-up recorded: a copy made at the request step shows an empty Classification because a migrated revision's class/use/responsibility live in its record summary, not in characteristic values (#167); migrating them into values is the fix, then `CopyRevisionAsDraft` carries them | The measurements of 2026-09-16 (actual plans read from `SET STATISTICS XML`); agrees with #158 (the join form) and refines it; the NFR-2 rule | W8 |
| 170 | **Applicability classifications belong to the primary asset a scheme protects, not to the relay; in this phase a person records them, a later phase derives them; the primary-asset registry is thin.** The owner, 2026-09-16, in three steps. (1) Whether a relay is a BES Cyber Asset for CIP is only partly about the device (a cyber device or not); the deciding criterion is the importance of the primary asset it protects in its station — the CIP-002 evaluation under the entity's procedure puts primary assets in scope and every other CIP standard follows; NPCC is the same shape: the A-10 study, repeated on an interval, lists the impactful busses and the directories apply to what sits at them. (2) The CIP-002 procedure and the A-10 derivation are a project of their own, wanted but not this phase: the values are set by a person for now, from the entity's own lists, in a way that leaves the derivation, its schedule and its reporting to slot in without rework. (3) The classification belongs to the primary asset the device is assigned to protect; transmission lines will come from the owner's TLM project (structure coordinates; Carson's equations still to do), transformers, busses and connectivity are not modelled yet, and modelling the power system is not this phase. **Built:** asset types Line, Transformer, Bus, Breaker, Generator, Capacitor, Reactor and System (the asset a RAS or other system-protection scheme protects — the owner: a system protection has no single primary element), class Primary, not devices, a line not routed until TLM supplies its route (`Seed_ref_AssetType_Primary.sql`); the PRC-023 classification kind beside BesStatus, CipImpactRating and NpccBulkPowerSystem (the seeded NpccA10 kind retired the same evening — the owner: "NPCC bulk power system and NPCC A-10 are the same thing; A-10 is the study document that determines whether a bus is declared as an NPCC BPS bus or not" — one kind, the study as its basis); `asset.vPrimaryAsset` (the asset, its station through its placement, its current classifications summarised); `asset.RecordClassification` (one current value per subject and kind, `Basis = Recorded`, the person and the instant, optionally the list or study as the reference document; a new value revises, an empty one withdraws; audited; `Asset.Modify` on the subject over HTTP); the `SCHEME` screen (members; "protects": pick a primary asset at the scheme's station or create one — a name, a type, the station, nothing more — with its zone role; `scheme.SchemeProtects_Add/_SoftDelete`) and the `PRIMARY_ASSET` screen (where it is, the schemes protecting it, the classifications in the standards' own words — CIP High/Medium/Low/None, BES/Not BES, BPS/Not BPS, Listed/Not listed — never a yes/no that would need re-encoding); the device sheet's Where panel shows "protects <asset> via <scheme>" with the inherited classifications and links to both. Shown on DEV: Line 0012 created at Bathurst Terminal from the 0012 B-PROT scheme's page, protected primary, CIP impact Medium and A-10 Impactful recorded by the engineer, the device sheet of 0012 B-PROT reading them through its scheme (`evidence/W8-scheme-protects-line-0012.jpg`, `W8-primary-asset-line-0012-classified.jpg`, `W8-record-protects-inherited.jpg`); the smoke's #170 section (a line at the fixture station, recorded, revised, withdrawn, the history kept, ReadOnly refused). **Not built, by the ruling:** the derivation (`Program.ClassificationDerivation` stays a kind row), the flow-down from station or bus to the elements at it, the CIP-002 procedure, the A-10 schedule; the estate's primary assets themselves (none migrated — the legacy data never held them; entered per scheme by the group, or created from the scheme names by rule once the owner confirms the naming on a sample, or imported from TLM when it joins). The obligation rules (BCA → CIP set, A-10 → Directory 4, PRC-023 → loadability) read the classification whatever its basis and are the next increment, with the requirement sets, thresholds and the line-rating source from the owner. **Same evening, from the owner's use of it:** (a) the primary asset's own fields are editable on its page (`asset.Asset_Revise`; "Line 0012" should have been L0012 — the group's L-number form); voltage classes 69/138/230/345 kV seeded. (b) **A primary asset has as many terminals as it has** — the owner: a capacitor or reactor one, a transformer two, a transmission line could theoretically have n; "three terminal lines will have two line numbers"; entered with a "+ terminal" button, a station and a voltage each (a transformer's terminals differ; the asset-level voltage column is not used for primary assets), and, on the right, the schemes protecting the asset **as a tree by terminal end** (the owner: add a terminal on the left, assign its protection on the right — which made the "protection location" mark redundant, so it went): a link names the terminal end it protects from (`scheme.SchemeProtects.AssetTerminalEntityId`; an older link is placed by the scheme's station, `scheme.vSchemeStation`); a scheme whose station is no terminal is listed as untied with a way to tie it. **NPCC is a bus test** (the owner, the same evening): "the A10 testing is a bus test, not an element test" — whether the 2103 protections at Bathurst are NPCC depends on the Bathurst 230 kV bus being declared impactful, the Eel River end on the Eel River 230 kV bus; lines and transformers are not BPS, the busses their terminals connect to are; this applies to NPCC only, BES status and the CIP rating stay on the element. So a terminal names the bus it connects to (`asset.AssetTerminal.BusAssetEntityId`, a Bus primary asset at that station), the NPCC BPS kind is recorded on busses only, a line or transformer shows what each terminal inherits from its bus (`asset.vAssetTerminalDetail`), and the relay's sheet reads the NPCC declaration through its scheme's terminal end: the first cut placed a primary asset at the station whose scheme created it, which showed Eel River on a line the Bathurst scheme also protects and confused; a placement is a device's (one Installed asset per node; a routed asset has none). Replaced by `asset.AssetTerminal` — terminals numbered 1, 2, …, each a station, one current row per asset and terminal number — added, changed and removed on the asset's page (each change saved and audited at once) and, at creation from a scheme, terminal 1 = that scheme's station; the read model shows the terminals and, as a cross-check, the stations of the schemes that protect it ("protected from" — a scheme at a station that is not a terminal is a data error). Node.Path is the ancestors' chain without the node's own id (the first station lookup matched every station). L0012 and L2103 given their terminals and their first-cut placements closed | The owner's three rulings of 2026-09-16 (recorded in memory as `feedback-applicability-from-primary-assets`); the schema's existing `asset.Classification` (subject Node/Asset/Scheme, Basis Recorded/Derived, reference document, determination) and `scheme.SchemeProtects`; agrees with #68 (classification carried) and defers its derivation | W8 |
| 171 | **Classifications and compliance, first cut: the obligation rules and the loadability formulas are definitions; the evaluator is code; the CIP rating is the station's, the BCA flag the device's, the ratings are hand-entered until the DMZ connector.** The owner's answers of 2026-09-16, one question at a time: (Q1) the BES Cyber Asset flag is set per device by a person for now — "there will eventually be a study of CIP-002 and a procedure to determine the classification of a terminal which will then result in every device in that terminal which is a cyber asset being given that classification"; the CIP impact rating lives on the station. (Q2) the CIP requirement set is the owner's starter list — CIP-004 R2/R4, CIP-005 R1 (with routable connectivity), CIP-006 R1, CIP-007 R1–R5, CIP-010 R1–R3, CIP-011 R1; the platform holds no record kind for these, the evidence is elsewhere. (Q3) PRC-023 R1: "we utilize first 1, then 2, then 13 and finally 12 (if all else fails)"; the criteria are the standard's (PRC-023-6, in force in NB from 2024-10-01; NB appendix PRC-023-6-NB-0 changes nothing in R1 and sends R5's list to NPCC; https://nbeub.ca/reliability-standards). (Q4) line ratings: "a ratings table the group keeps" in another group's database, not reachable now — "make room for the given values in our application and then create the connector later" (a DMZ connector, likely two-way, every transfer triggered from inside the OT). **Built:** `asset.AssetRating` (kind Continuous / FourHour / FifteenMinute / PracticalLimitation, season, amperes, source, `SourceSystem` = Recorded until the connector writes its own name; a Ratings panel on the primary asset); `ref.ClassificationKind.SubjectKinds` (which subjects a kind applies to) with the kinds BesCyberAsset (BCA / Not BCA) and ExternalRoutableConnectivity (ERC / No ERC) on devices and CipImpactRating on stations (a STATION screen; the primary asset no longer carries it); the facts `device.classification.<K>`, `device.station.classification.<K>`, `device.protects.classification.<K>`, `device.protects.bus.classification.<K>`, `device.protects.terminal.voltage`, `device.protects.name`, `device.protects.rating[kind]` (`compliance.fDeviceProtects`: the device's scheme, the Primary link first, the terminal end named or matched by station); grammar 1 gains `hypot`, `cos`, `sin`, `atan2`, the Ratio × X rule, `deg`, and names ending in `%` (both interpreters, conformance 153 passed); the standards in force in NB seeded with their requirements quoted (`Seed_compliance_Standards_NB.sql`, versions read from nbeub.ca on 2026-09-16: CIP-004-7, CIP-005-7, CIP-006-6, CIP-007-6, CIP-010-4, CIP-011-3, PRC-023-6, each `-NB-0`); the rules and formulas generated from one table (`tools/compliance_rules.py` through `tools/FormulaCompile`, the platform's own parser — each definition carries the text and the canonical AST): 13 CIP rules scoped `device.classification.BesCyberAsset = 'BCA' and device.station.classification.CipImpactRating in {'High','Medium'}` (CIP-005 R1 also `ExternalRoutableConnectivity = 'ERC'`), PRC-023 R1 scoped `device.protects.terminal.voltage >= 200kV or device.protects.classification.Prc023 = 'Listed'` (4.2.1.1 / the R6 list), R3 / R4 / R5 scoped on the criterion chosen; the formulas `prc023_zset` = Z3% × |R1 + jX1| along the line angle (SEL-221F manual 5-14), `prc023_line_angle` = atan2(X1, R1), `prc023_z30` = zset / cos(line angle − MTA) × cos(MTA − 30°) (manual 2-32: the mho circle through the origin, diameter along the MTA; SPP *Methods to Increase Line Relay Loadability* Fig. 2: Z30 = Z_MTA cos(MTA − 30°), Trip MVA = kV²/Z30, checked on its 345 kV / 88 Ω / 1 352 MVA / 766 MVA example), `prc023_trip_current` = 0.85 × V_LN / Z30, `prc023_load_current` = min(trip current, 50H) (50H trips in MTO/MTU per 5-20; 50P only supervises, 5-19; ground elements excluded by Attachment A 2.2), `prc023_criterion` = 1 if > 150 % of the highest 4-hour rating, else 2 if > 115 % of the 15-minute rating, else 13 if > 115 % of the practical limitation, else 12 if the reach ≤ 125 % of the line impedance with MTA = 90°, else None — a rating the platform does not hold skips its criterion; the evaluator `ComplianceEvaluator` (Preview writes nothing; Effective opens / closes `ObligationInstance` rows and records every fact read as `ObligationInstanceFact`; one `RuleEvaluationRun` per pass; candidates = devices with a classification or a protects link; a formula fact is evaluated from its definition on read) behind `POST compliance/evaluate` and a scheduled pass (`Engine:ComplianceMinutes`, default 60); the device sheet's Compliance tab (the device's own classifications, the inherited ones, the obligations with their facts, Evaluate now → Commit, the PRC-023 working). **Proven on DEV** (preview on relay 3445, SEL-221F at Eel River, in-service settings Z3% = 135, R1 = 6.57 Ω, X1 = 42.76 Ω, MTA = 81.26°, 50H = 3 790 A; L2103's terminal 230 kV): zset 58.40 Ω, line angle 81.26°, Z30 36.55 Ω, trip current 3 088 A, load current 3 088 A, criterion None — no rating is entered yet, so criteria 1, 2 and 13 are skipped and criterion 12 fails on the 135 % reach; hand check 1.35 × √(6.57² + 42.76²) = 58.40, 58.40 × cos(51.26°) = 36.54, 0.85 × 230 000 / √3 / 36.55 = 3 088. PRC-023 R1 true (230 kV); the 13 CIP rules Unknown until the relay's BCA flag and the Eel River station's rating are recorded (the owner enters both). Smoke #171 section (a rating entered, ReadOnly refused; BCA and station Medium recorded; preview writes nothing; commit opens 12 CIP + PRC-023 R1 after the terminal is made 230 kV; the facts trail; a second pass unchanged; the BCA withdrawn closes the 12; ReadOnly previews but cannot commit). **Stated, not derived:** the mho is the steady-state self-polarised circle (the memory-polarised expansion, manual 2-33, is not modelled); 50H is counted without decoding MTU/MTO; CIP scope is High/Medium stations (a BCA at a Low station has no CIP-004…011 obligation here — UNVERIFIED against the entity's procedure, one word in the seed if wrong); the rules are seeded once, a change is a new version by a person. Found and fixed on the way: `ValidateProgramFacts` refused a formula's own published name; `RuleEvaluationRun_Append` required an actor; the standards seed reused an output variable (the DEV rows of the first failed run carry shared ids until the next fresh load). | The owner's four answers (memory `feedback-applicability-from-primary-assets`); PRC-023-6 pp. 3–5, 14; NB appendix PRC-023-6-NB-0; SPP *Methods to Increase Line Relay Loadability* (2006) p. 3; SEL-221F IM 981207 2-8, 2-16/17, 2-32/33, 5-14/16, 5-19/20; `docs/schema/FORMULA-GRAMMAR.md` §4.5/§4.8/§7; agrees with #68 (classification carried) and #170 | W8 |
| 172 | **A read scope is a union of seekable branches, never one predicate with ORs.** Found while verifying #171 on DEV (2026-09-17): a scoped engineer's list took 19 s and the API timed out, so four W2 scope checks and the grid's NFR-2 timing failed; `security.fReadableSubjects` was untouched by #171 — the estate had simply grown past the point where its plan held. The cause is one shape repeated four times: `(a = b OR c LIKE d)` for the node subtree, and `x IN (…) OR x IN (…)` for the records, work requests and schemes. Neither form can use an index, so the optimiser scanned `location.Node` and spooled it once per candidate — 12 000 rows against 12 000 rows for a grant that reads 145 nodes. Each branch written on its own seeks (`IX_Node_Entity` for the node itself, `IX_Node_Path` for the prefix, `IX_SchemeMember_Member` for a member kind) and the union is the same set, proven by counting both ways before and after on the same grant: nodes 145, assets 49, records 252, schemes 16, work requests 62, and the administrator's global grant still reads the whole registries (12 577 / 7 055 / 12 249 / 1 801 / 11 927). **Measured on DEV**, one grant, warm: nodes 19.0 s → 0.28 s, assets 19.1 s → 0.33 s, records (which had not finished in six minutes) → 0.99 s; the smoke's warm screen timings back to grid 1 772 ms for 500 rows, request status 434 ms, floc by station 487 ms, tree roots 125 ms; smoke 266 passed, 0 failed. **The rule this sets:** a read-scope branch is written per readable set. An `OR` across two sources in a security predicate is a defect, not a style, because the cost is invisible until the estate is large and the failure then looks like a timeout, not a slow query. Agrees with #169 (a read model's plan is taken, not assumed) and with the architecture's separation of budgets: no architectural weight excuses an interface that does not settle in a second (the owner, 2026-09-17: "Usability requires that the speed of the application must be maintained"). | `security/Functions/fReadableSubjects.sql`; the plans and timings above, taken on `PnCPlatform_V2_DEV`; IDENTITY.md §5 (read scope is as strict as write scope) unchanged in meaning | W8 |
| 173 | **Applicability is the asset type's; the BES Cyber Asset flag is derived, not asked for; the CIP impact rating is the location's; and a standard a device is not bound by is not shown against it.** The owner, 2026-09-17, on using #171: (1) "Busses are not PRC-023 applicable as is shown, nor do busses have a rating"; (2) "the BES Cyber Asset determination would result from whether or not the device is a microprocessor based device and whether or not the primary asset that it is protecting is BES. This should not be a drop down. External routable connectivity is correct for now as a dropdown until we can take that to a network analysis module to determine it. From there the various cip requirements would be dictated by the classification (high, medium or low impact) of building that the device is in"; (3) "There doesn't seem to be a way of classifying a location with a high, medium or low CIP impact rating"; (4) "Once the various compliance classifications have determined that a particular device is not bound by a particular standard, it should not show up in any of the devices information forms". **Built:** applicability as reference data — `ref.ClassificationKind.AppliesToAssetTypes` (a JSON list of asset types; NULL = all) and `ref.AssetType.CarriesRating` (Line and Transformer, the circuits PRC-023 R1's criteria 1, 2 and 13 measure) — enforced in the procedures, not merely hidden on the screen: `asset.RecordClassification` throws 50232 for a kind that does not apply to the subject's asset type and 50234 for a kind that is derived, and `asset.RecordAssetRating` (new, replacing the generated CRUD over HTTP) throws for an asset type that carries no rating; a withdrawal is always allowed, so a value recorded before a kind was narrowed can still be taken back. The **first real `Program.ClassificationDerivation`**: `bes_cyber_asset` — `device.technology in {'Microprocessor', 'IEC61850'} and device.protects.classification.BesStatus = 'BES'` → BCA, else Not BCA, and Unknown while the protected element's BES status is unrecorded (the grammar's rule: all-Unknown yields Unknown, not the else) — evaluated by `ComplianceEvaluator` before the rules in every pass and written by `asset.DeriveClassification` with `Basis = Derived` and the derivation's version row; it never overwrites a value a person recorded, and an unchanged value writes nothing, so an hourly pass over the estate does not revise thousands of identical rows. The **CIP impact rating moves to the location**: `device.station.classification.<Kind>` is replaced by `device.location.classification.<Kind>`, the value of the nearest ancestor of the device's placement that carries one (`location.fNearestClassified`), so the building the device is in rules and the station serves while the migrated buildings are placeholders (DEV holds 250, one per station, named "Building (unknown — legacy has no buildings)" because the legacy database named none); the `STATION` screen becomes `LOCATION` for any node type, reachable from the sidebar, and the CIP rules' scope reads the new fact. **What does not bind is not shown**: the verdict list keeps the rules that apply and the ones still undetermined, and collapses the rest into one line. **Proven on DEV** by the smoke's #173 section, which is also the demonstration: the fixture's SEL-421 (Microprocessor) protecting a BES line is derived BCA and opens 12 CIP obligations plus PRC-023 R1, while the BDD15B (Electromechanical) beside it on the same line is derived Not BCA and not one CIP standard appears against it; a bus refuses both a PRC-023 listing and a rating; a person recording the BCA flag by hand is refused; the building's Medium rules over the station's Low. **Stated:** ratings are carried by Line and Transformer only (a capacitor or reactor has one in reality; PRC-023 needs the circuit's) and a derived BCA cannot be overridden by hand — the Recorded-wins path exists in `DeriveClassification` if the owner ever wants the control back. | The owner's four comments of 2026-09-17 with the bus screenshot; `ref.Model.Technology`'s four values (Microprocessor 355, Electromechanical 1 185, Static 10, IEC61850); FORMULA-GRAMMAR's `Program.ClassificationDerivation` host shape, seeded since W3 and until now never evaluated; corrects #171 | W8 |
| 174 | **A person builds the location tree in the application; equipment sits directly in a yard, because this client has no bays.** The owner, 2026-09-17, correcting #173's assertion that the CIP impact rating belongs to a location: "Location is too far up the FLOC tree, there are locations which contain multiple yards, each of which have a different impact rating. It is actually at the yard level of the FLOC that we set the impact rating." Working it through, he then answered his own question about how a relay in a control building reaches its yard: "what building houses the devices that protect what yards... this must already exist!!! For example lets say that TN-4134-BDG2-PNL10 (panel 10 in building 2) is the physical location of the T3 protection A...That link must already exist and doesn't that already provide the link necessary?" It does, and from the other side: a relay carries two links the platform already holds — **where it is** (its placement: position, panel, building) and **what it protects** (its scheme, the primary asset, that asset's terminal). The second carries the yard, and the only gap is that a terminal names the station rather than the yard within it; `asset.AssetTerminal.StationNodeEntityId` is a plain reference to any location node with no constraint forcing a Station, so a terminal can name TN-4403-Y230 as soon as yards exist. **The many-to-many he was worried about needs no declaration**: a building houses protection for two yards precisely because the relays in it protect assets in two yards, and one yard protected from two buildings is two relays with different placements — there is no building-to-yard table to maintain and therefore none to get wrong. **Bays:** "For the client that I am dealing with, the Bay concept doesn't exist. Transformers, PTs, CTs etc. would each be labelled onsite and then the FLOC would match that tag, so the FLOC for T3 would be TN-4134-Y230-T3 with no bay reference at all." So `ref.LocationNodeTypeParent` gains EquipmentPosition → Yard and JunctionBox → Yard, and the three Bay pairs are deactivated rather than deleted (no bay has ever been created in this estate); `location.AddNode` already honours IsActive, so the database itself now refuses a bay. **Built:** the LOCATION screen creates the tree — a type select offering only what `ref.LocationNodeTypeParent` allows under this node, a name, an optional subtype and notes, `location.AddNode`; after a success the type stays chosen and the name clears, because the owner is about to enter many siblings in a row; rename through `location.RenameNode` (new — the generated `Node_Revise` takes Path and Depth, which a client must never compute: a wrong Path detaches a subtree from every scoped read); withdraw through `Node_SoftDelete` behind a confirm. **Shown on DEV** at Eel River (station 4403): a yard created from the screen, its page offering Equipment position and no Bay and carrying the CIP impact rating control, T3 added inside it by name, the yard renamed, both withdrawn afterwards so the estate is as it was. **Not yet done, and named for the next increment:** a terminal cannot yet be pointed at a yard (the asset page offers stations only), so the rating does not flow to a relay until it can; and the FLOC code (TN-4403-Y230-T3) is a path of short codes that the platform cannot compose — a node has a name, and only a station has a number (`location.AlternateKey`, KeyKindCode StationNumber). | The owner's three messages of 2026-09-17; `ref.LocationNodeType` (Yard is one of 22 types, seeded since W2) and 0 yard rows in the estate before today; corrects #173 | W8 |
| 175 | **Every location node carries a code; the FLOC is composed from the codes and never typed.** The owner, 2026-09-17: "for each of the various levels, ie. Y230, T3 etc. can we have a field 'code' which contains the code used in the FLOC and then many a Name and/or Description etc. which allows the user to add more descriptive texts?" His FLOC is a path of short codes — TN-4403-Y230-T3 is the Transmission division, station 4403, the 230 kV yard, transformer T3 — and the last segment is the tag painted on the equipment. Ruled by him with the plan: a station's code **is** seeded from its station number, and the FLOC is **always composed**, never editable, because composing it is what keeps it true when a node moves. **Built:** `location.Node.Code` (the node's one segment; unique among a parent's children, `UX_Node_ParentCode`) and `location.Node.FlocCode` (the composed path, materialised exactly as `Path` already is, indexed for lookup); `Name` keeps its job as the descriptive label and `Notes` is the description, so a node reads `Y230 · 230 kV yard`. The rule lives in one function, `location.fComposeFloc`: no code → no FLOC; the parent has a FLOC → parent's FLOC + '-' + code; the parent has none → the code alone. So the chain is **the unbroken run of coded ancestors ending at the node**: NB Power carries no code, so it starts at the division (TN, TN-4403, TN-4403-Y230, TN-4403-Y230-T3), and when a code is withdrawn the platform **never invents the missing segment** — the transformer under a decoded yard reads T3, not TN-4403-T3. A code may hold neither the separator nor whitespace (`location.AssertNodeCode`, 50213). `AddNode`, `RenameNode` and `MoveNode` maintain it: a code change rewrites the FLOC of the node and its whole subtree, scoped by the `Path` prefix so the index seeks it (#172) and recomputed by a recursive CTE rather than a text-prefix swap, because under the restart rule a descendant's FLOC need not begin with its ancestor's old one. **An omitted code keeps the existing one and an empty string clears it** — `SqlSession` treats a JSON null as a parameter not given, so a screen saving a name must not wipe a tag; written into both procedure headers because the difference is invisible from outside. **A defect caught before it ran:** `Node_Revise` defaults `@Code` to NULL, so a move would have silently erased the code of the moved node and every descendant; `MoveNode` now carries codes through. **Seeded:** 250 of the 256 stations take their existing `StationNumber` as their code (the other six have no number); a station's FLOC stays NULL until its division is coded, which is correct and stated in the seed's header. **Shown on DEV**: the Transmission division given TN on the screen, which rewrote the FLOC of all 72 stations beneath it; a yard and a transformer entered at Eel River by code and name, composing TN-4403-Y230ZZ-T3; all proof nodes withdrawn afterwards. Smoke 270 passed, 0 failed, with five #175 checks: the chain composes, the separator and a space and a repeated sibling code are refused, a code change follows down the subtree, an untagged node has no FLOC and a decoded parent restarts the chain, and the migrated stations carry their numbers. | The owner's question and his two rulings of 2026-09-17; `location.AlternateKey` (StationNumber, the only code the platform held before today); the `Path` materialisation this follows; extends #174 | W8 |
| 176 | **A device is placed at a position, and a protection function joins a scheme — both from the screen; and the generic view read gains a contains search.** The owner, 2026-09-17: “I have gone into the locations tab, added a Building to Eel River, added a panel to the building... Now how do I associate a protection with this panel?”, then “build both”. Two writes existed as procedures with no screen. **Placing**: the DevicePosition page gains a *Placed here* panel — search for the relay, choose the kind (Installed / Spare / Stored / Removed / Condemned), place it; a relay recorded elsewhere is named before the move is made, because `asset.PlaceAsset` ends the old placement in the same transaction. A panel page says so rather than offering the write: a device is placed at a position, not at a panel. **Joining a scheme**: the scheme page gains a *Members* panel — kind, role (the fifteen of `ref.vSchemeMemberRole`), in-service, and a chooser scoped to the scheme's own station by two equality reads (`location.vFloc` by station for panel/position, then `location.vNode` by that position for its functions), never a walk of the tree. A scheme with nothing placed anywhere has no station yet, so the station is asked for once. Members stay functions, assets and channels and never devices (#7.2), so a relay swap leaves the scheme intact. **The contains search**: `SqlSession.QueryViewAsync` built `[Col] = @p` and nothing else, so the chooser could only match an asset's *exact* recorded name — unusable against names like `SEL-221F Z1-3=.125-64 OHMS [3445]`. A filter key ending in `~` now means *anywhere inside*: `Name~=3445` finds that relay. Text columns only (a number has no inside, 400 `not_searchable`); LIKE wildcards escaped, so a term with a per cent sign searches for the character; the materialised-view path carries the same flag. It is for a person typing into a search box, never for a report — it cannot seek an index | The owner asked for both; the chooser is useless without the search, which is why one primitive on the generic read beat a bespoke endpoint | #176 |
| 177 | **The CIP impact rating belongs to the building that houses the protections, not to the yard; #174's yard ruling is withdrawn and no code changes.** The owner, 2026-09-17: “the impact score isn't related to the yard! It is related to the building and how what the impact score is for the assets being protected by protections in that building. For example there could be multiple yards each with there own assets which by themselves would not be over the thresholds but if the building housing the protections for all the yards is a single building then that building will be assessed accordingly and each of the cyber assets in it as well.” Checked against the deployed mechanism before answering: `compliance.fFixedFactValue` resolves `device.location.classification.CipImpactRating` from the device's **Installed placement** and then `location.fNearestClassified`, which walks the node itself and seven ancestors — ProtectionFunction, DevicePosition, Panel, Room, Building, Station, Division, Owner — and takes the lowest level that carries a value. A rating on a building therefore beats the station's and reaches every device in that building, which is the owner's rule exactly. No yard special-casing exists anywhere in the classification path, so #174's yard ruling was recorded but never wired in; the increment it named (a terminal naming a yard so a yard's rating could reach a relay) is **withdrawn**, not deferred — it would have implemented the wrong rule. Two things do follow. The screens offer the rating on every location node, a panel and a device position included, which invites recording it below the level the rule supports; the rating is a building's (or a station's where the building breakdown is not modelled). And **the platform does not compute the aggregation**: it records the value the entity's own CIP-002 evaluation produced. The inputs are reachable — a building's panels reach their devices, and `compliance.fDeviceProtects` reaches what each device protects — so the rollup could be computed later, but nothing computes it today and no screen claims to | The reading rule was already right; what was wrong was a recorded ruling and a next increment built on it | #177 |
| 178 | **The legacy LOCATION is a building, not a station: Eel River is restructured into two stations and three buildings, and the pattern is the estate's.** The owner, 2026-09-17, working out why he could not give EEL RIVER TERMINAL a code: 4403 is the HVDC circuits and 4416 the terminal; the 230 kV protections sit in the HVDC control house, which is why they carry 4403, and the 138 kV protections sit in the terminal, which is why they carry 4416. The importer's own shape confirms the reading: it creates one Station AND one placeholder building from each distinct legacy LOCATION string, while the station number comes from a different column entirely (the dominant SETTINGS.ASSET across that location's rows). 231 stations, 231 placeholder buildings, one each, all named “Building (unknown — legacy has no buildings)”. So the numbered thing and the named thing were never the same thing, and Eel River is where that shows — corroborated by the panels `230 KV BUILDING INTERFACE A/B` at 4403 and `138 KV BUILDING INTERFACE A/B` at 4416, the two ends of the link between the buildings. **Now**: `TN-4403` Eel River HVDC with `TN-4403-BDG1` Eel River HVDC Control House (72 panels); `TN-4416` Eel River Terminal with `TN-4416-BDG1` Eel River 138 Bdg (48 panels) and `TN-4416-BDG2` Eel River 230 Bdg, the new building, moved there from the station node it was built under, which is retired. The owner's first plan named 4403's building “Eel River 230 Bdg” and deleted it after the move; the panel list shows that building is the converter station (filters F1—F6, synchronous condensers SC1—SC3 and their exciters, runback, VAR loss, converter transformers TC1—TC4, 345 kV and 13 kV plant), so deleting it would take the HVDC protections with it. **The move is therefore a panel move, not a building deletion**, and no name needs a “new” suffix stripped later. The owner then named the real structure — what he had called BDG1 at 4403 is a ROOM inside the converter building — and `ref.LocationNodeTypeParent` already allows Building → Room → Panel with the room optional. He ruled against using it for now (“I am not aware of any other location where we utilize a room so I would hesitate to start now”), so the 230 kV panels stay directly under the control house and the room stays available | The FLOC level was wrong, not the FLOC; correcting it is two renames and a move, because a node already carries Code and Name (#175) and MoveNode already rewrites Path, Depth and FLOC for a subtree | #178 |
| 179 | **The settings book lists buildings, and a location on a settings screen is the building's name.** The owner, 2026-09-17: “It would be helpful for the engineers and techs if the 'Locations' list box at the left, actually displayed the BDGx Names and filtered the data on those. That way there would be three 'buildings'”; “the lettering should be all capital letters as well”; and, asked whether the station should still be shown, “I don't believe that we need the location being displayed in the P&C Setting bars any longer, Building Names will be enough and is more straight forward for the users.” So: `document.vSettingsRecord` gains `BuildingNodeEntityId` / `BuildingName`, read off the SAME four ancestors the station is read off — one more pass over rows already in hand, not a join, and nearest-first so a Room would not break it. The `SETTINGS_BOOK` definition lists `Building` and filters on the building; `Location` now labels the building's name and the station is a plain column nobody shows. The settings record screen's *Where* panel reads the building. `report.js` takes either, because links made before today carry a station. Only ONE line of component code changed (`SettingsBookScreen.tsx`: the list's node type comes from the definition), which is the point of the screen being a definition. **The seed**: all 228 remaining placeholder buildings take their own station's name and the code `BDG1` — a derivation from what the platform holds, so every screen reads as it always did except at Eel River, where one entry becomes three; and every Station and Building name is upper-cased to match the names the legacy gave. **Found on the way**: `Seed_config_Screens.sql` is GENERATED by `tools/seed_screens.py` from `docs/design/examples/screens/*.screen.json`, and its idempotence guard is a hash of the payload in the change note, so a hand-edit to the .sql deploys as a no-op and is overwritten — the same trap `generate.py` sets. The JSON is the source. **Measured**: NFR-2 failed once on a cold cache (floc by scheme 1161 ms) and read 469 ms warm; filtering the settings view by building costs the same as by station (median 1153 ms against 1138 ms over six samples of 299 rows), so the new column costs nothing | The owner asked for the list; the station had to go with it, because after #178 showing both would say the same thing twice | #179 |
| 180 | **The FLOC ends at the device position; a protection function is recorded but never tagged.** The owner, 2026-09-17, on the screen #176 built: “I understand that the mp devices contain multiple protective elements (21,51 etc. etc.) but I am not sure that it is valuable to have all of them listed, the device should really end with the device id..whatever that is... At my clients, they understand that a 21 element contains many more protective elements than just distance but it was desired to shorten it so that schematic drawings would not get busy. I believe that the device FLOC should stop at TN-4134-BDG1-PNL12-21A and that FLOC position should be assigned to the device (SEL-411L etc.)” **Checked before answering**: the estate already agrees with him — of 3105 ProtectionFunction nodes not one migrated row carried a code or a FLOC; the only five that did were smoke fixtures this session created. So what was wrong was the screen, which offered a code box on a protection function and would have produced `...-21B-5051`. **The nodes stay**, and the reason is checked rather than assumed: a scheme's members ARE ProtectionFunction nodes (`scheme.vSchemeMember`), which is what lets a relay be swapped with the scheme intact, and the settings record already lists the elements through its `Functions` column rather than through a tag — exactly the shortening his client wanted. **The rule**: `ref.LocationNodeType.CarriesFlocSegment` (NULL or 1 = yes; 0 for ProtectionFunction alone), `location.AssertNodeCode` refuses a code on such a type (50215) so no path into the platform can set one, the seed clears any that exist so the rule is true of the data and not only of new writes, and the add-a-child row hides the code box for such a type. **The other half of his sentence**: `document.vSettingsRecord` gains `Floc`, the position's own composed tag read through the placement the view already had, and the settings record's *Where* panel shows it — until now the FLOC appeared on no device-side view or screen at all | The model was right and the screen was wrong; making the rule the platform's rather than the screen's is what stops it coming back | #180 |
| 181 | **What a relay CAN do comes from its model, from the manual; what it DOES here is ticked at the position.** The owner, 2026-09-17: “lets build the checklist on the device instead, you can build the list from the devices manual. Note that this leads me to think that we should have device templates so that an admin would create an SEL-221F device template with all the possible capabilities so that when the user selects the device to add to a panel it will populate with all the appropriate capabilities”; and, on what a newly placed relay starts with, “If a new device is added, none ticked but when a standard template is used then it should follow the template”; and on the old nodes, “repoint them”. **Almost all of it already existed and had never been used**: `scheme.FunctionCapability (ModelId, FirmwareVersionId, AnsiCode, Source)` was built for exactly this (SCHEMA-DESIGN 7.3, decision 117) and no code had ever written a row, and `scheme.CommissionedFunction` was already “one row per enabled element at a position” whose node column is a plain NodeRegistry reference with nothing forcing a ProtectionFunction node — so pointing it at the DevicePosition needed no schema change (the same situation #174 recorded for `asset.AssetTerminal.StationNodeEntityId`). **Built**: `tools/capability_sel221f.py` (the pattern of `template_sel221f.py`) reads the SEL-221F IM 981207 and seeds the ten elements the manual's own Functional Specifications headings give — 21, 25, 27, 50, 50BF, 50N, 51N, 59, 67N, 79 — each row citing its heading and page, with the five the manual describes but this platform has no code for (32Q, LOP, SOTF, REJO, fault locator) listed and NOT seeded, because a code is never invented. A hand-written `scheme.vPositionFunction` reads it back with the element's C37.2 name. The position screen shows the model's list as tick boxes, nothing ticked until someone ticks it. `ProtectionFunction` is deactivated as a child type (as Bay was, #174). **The repoint**: all 3 103 commissioned functions moved to their position (checked first: 3 103 distinct (position, code) pairs, zero collisions), the redundant protection-function scheme members were withdrawn (3 007 of 3 103 — every real one — because the relay was already an Asset member of the same scheme), and a node nothing names any more is retired. **What broke and was fixed**: `location.vFloc`, `location.vFlocScheme` and `document.vSettingsRecord` all read functions through the node layer; each now reads the commissioned function at the position and the scheme through the relay placed there | The owner asked why the element layer earned its keep and the data said it did not; the value is real but sits on the model and the position, not in the tree | #181 |
| 182 | **A function with no device number is still a function: the catalogue holds the manufacturer's own words.** The owner, 2026-09-17: “there are elements in the relay that do not have ansi numbers assigned to them. That is to be expected from those devices and should not be left out, they are part of the device and must be included. When no numbers can be found for the functionality, wording will have to suffice.” #181 had listed five such functions of the SEL-221F and deliberately not seeded them. They are now capabilities like any other: **32Q** negative-sequence directional, **LOP** loss of potential, **SOTF** switch onto fault, **REJO** remote end just opened, and **FAULTLOC** fault locating — the SEL-221F's list goes from ten to fifteen. Four of those five keys are the MANUAL'S OWN abbreviation, printed in its headings and in the title of Figure 2.2; only FAULTLOC is ours, because the manual names the fault locator in words throughout and never abbreviates it, and the generated review table says so on that row. `ref.AnsiFunction` gains **`IsDeviceNumber`** (NULL = nobody has said): 1 for the thirteen C37.2 numbers, 0 for a manufacturer's abbreviation, NULL for the strings the legacy importer invented from position text. The checklist shows a number as a number and everything else as its words alone, never as a code pretending to be one. **Two traps, both found by the smoke rather than guessed at**: the flag is set by a plain UPDATE and not through `AnsiFunction_Upsert`, because that procedure is generated from the DEPLOYED catalogue and so does not take a new column's parameter until the publish after the one that adds it; and the capability seed now names a code only when the catalogue does not already hold it, because it had been overwriting the C37.2 names (21 read “Phase and ground distance” instead of “Distance relay”) and `Seed_ref_AnsiFunction_Core` had already ruled that the C37.2 name wins | The owner's point is that the relay does the work whether or not a standards body numbered it; a catalogue that only holds numbers would quietly drop a fifth of what an SEL-221F does | #182 |
| 183 | **No setting is hidden on the SEL-221F sheet; and the “normally not changed” mark is the STANDARD, not a boolean.** The owner, 2026-09-18: “For the SEL-221F relay, since it is very simple compared to the modern systems, I am going to say that all settings will be grouped as you note by the various tasks that are done but none will be hidden....for now....I will look at this again after we finish building the template”. Nothing to build: the sheet already shows all 56 settings in the manual's own twelve Specifications groups and hides none (`DeviceSettings.tsx` filters by the chosen tab only), so this records the ruling against the day the checklist tempts someone to hide what an unticked element owns. **His second thought** — “a simple boolean that could be turned on my the admin to indicate that some particular setting was part of a scheme template which would either move the setting to a special place on the template or maybe simply highlight it” — is already his own requirement of 2026-09-16 (“standard settings highlighted as 'normally not changed'”) and the schema already holds a richer home for it than a flag: `config.StandardSettingEntry` (SettingCode, **ExpectedValue**, **Tolerance**, **Basis**, Category, DisplayOrder) under a `CharacteristicSchema.StandardSettings` definition, bound through `config.DefinitionAppliesTo` to a protection application × model — and `ref.AppliesToDimension` already carries both **ProtectionApplication** and **SchemeType**, which is exactly the “part of a scheme template” he described. Verified today: the kind is seeded, **zero definitions and zero entries exist**. A flag on the setting definition could not do the job, because the same relay's standard differs between a line-distance scheme and a transformer scheme; the highlight falls out of whether a setting appears in the resolved standard, and the standard also says what the value should BE, which a boolean cannot. **Open, and his to settle**: whether “has a standard value” and “is rarely changed” are one idea or two — a reach has a standard value and is routinely tuned; a communications parameter is never touched | The owner asked to discuss rather than build; the discussion's finding is that the design already answers it, better than the shortcut | #183 |
| 184 | **`SEL221F_Template`: the device template, with PRC-023 and NPCC A-10 / Directory 4 as its compliance.** The owner, 2026-09-18: a **template** is one per device type, one level below a scheme, named “SEL221F_Template”; the standard `SEL221F_StandardProt` is dropped for this relay (#183); compliance in the template is calculated from study values on the primary elements, “not something that an admin can turn on or off”; scope “just a couple of the easier ones for now, lets say PRC-023 and NPCC A10” — PRC-023 “will require calculations to be done from the device settings”, A-10 “has no impact on settings but a huge impact on the physical design of the protection (Directory 4 compliance)... a documentation awareness case”; and the documents are at https://www.npcc.org/standards/regional-criteria. **The template** is a `CharacteristicSchema.AssetTemplate` definition (a seeded kind that had never held an instance), bound to both SEL-221F model codes through `config.DefinitionAppliesTo`, nine model-level facts from IM 981207 page-cited (`tools/template_bundle_sel221f.py`); the settings list and the fifteen capabilities stay where they are and are found through the same model. The **DEVICE_TEMPLATE** screen (`DeviceTemplateScreen.tsx`, plain React; reached from the model on a settings record) shows the facts, the elements, the five settings PRC-023's formulas read (Z3%, R1, X1, MTA, 50H) resolved to their names, the four PRC-023 rules with their triggers, and Directory 4's criteria as reading material. **NPCC**: `Seed_compliance_Standards_NPCC.sql` seeds A-10 v3 (2020-05-06) and Directory 4 (2025-12-18) with its 23 criteria R5.1—R5.20 and R6.1—R6.3, every Summary the document's own words with its page; the A-10 outcome gets the vocabulary **BPS / Not BPS** (owner: “the A10 vocabulary you note is correct”); rule `npcc_d4` is scoped `device.protects.bus.classification.NpccBulkPowerSystem = 'BPS'`, evidence outside the platform, the awareness pattern the CIP rules use. **Proved on DEV twice**: the smoke fixture, and the real SEL-411L [0160] in scheme 2103 B-PROT at Eel River HVDC — my first proof classified the near-end bus and nothing bound, correctly: `device.protects.bus` follows the terminal the scheme names, and that scheme protects line L2103 from its **Bathurst** end; BPS on the Bathurst 230 kV bus opened Directory 4 on the relay, whose Compliance tab then read “bus Bathurst 230 kV bus — NPCC BPS”; both buses restored to Not BPS afterwards. **Two generator defects found by looking at the screen**: the rule's name was a two-way guess (every non-CIP rule was “transmission relay loadability”), now per family; and a definition's Name lives outside the payload the seed guards on, so a renamed rule kept its old name on DEV until `definition_block` learned to refresh Name and Description unconditionally | The design already had every table; what was missing was the content from the documents and the screen that shows what can attach | #184 |
| 185 | **Everything reachable from the left navigation — a standing rule; a Templates menu, a Compliance menu, and the template stripped of what is not the device's.** The owner, 2026-09-18, on `SEL221F_Template`: (1) “How do I get to this screen from the main application screen. I can't find it... as this applications functionality is built out, we must always build in an intuitive way for the users to find this functionality. Since we are looking at Templates, I was expecting to see a Templates tab on the left hand side of the screen” — DEVICE_TEMPLATE had been seeded with `menu: null`, reachable only from a model link on a settings record; a screen verified at the user's layer that cannot be reached from the main screen is not done. (2) “does the compliance notes really belong with the template?... compliance is really a function of it's own, outside of the template. That is PRC-023 will be compulsory, no matter what physical device we are implementing” — right: standards and rules are per standard, not per model. **Built**, all as definitions with no new component code: `config.vAssetTemplate` (template × model, through the Effective version's AppliesTo) behind a LIST screen **Templates → Device templates** that opens the model's template; `compliance.vRequirementDetail` (standard → version → requirement) behind **Compliance → Standards**, and **Compliance → Obligation rules** over the existing `vObligationRule`; the template screen keeps Template facts, What it can do and Settings, and its seed drops the two compliance fact rows (v2, seven facts). **Two things the screen taught that the database had not**: the nav's group order is the `PAGES` list in `AppLayout.tsx`, and a group not in it is appended after Administration — Templates and Compliance are now listed before it; and the template screen had walked `vDefinitionAppliesTo` and taken the FIRST binding, which was the retired v1's, so v1's nine facts showed beside a v2 template — it now reads `vAssetTemplate`, which is Effective-only. **Seen, not fixed**: the Standards list also shows the smoke fixtures' throwaway standards (`4_...`, `r_...`, “smoke maintenance”), because no smoke run cleans them up; correct data, user-facing noise, a housekeeping decision of its own | Memory `feedback-reachable-from-the-nav`; smoke 287 passed, 0 failed | #185 |
| 186 | **Working content first; documentation collapsed until wanted.** The owner, 2026-09-18, on the template screen: “the tabs should be front and center, with the Template facts and what it can do placed in a collapsable section which can be opened if the user is interested but will typically be left closed. What I am trying to do here is to have the important, user facing stuff, at the top of the page and only show the documentation when the user needs them”. `DeviceTemplateScreen.tsx`: the settings panel first, then a native `<details>` (the expander `DeviceSettings.tsx` already uses) holding Template facts and What it can do, closed by default. A layout reorder only — no view, seed or API change, so no deploy and no smoke; verified on DEV in the browser. Recorded because it is a general principle for every screen from here on, not a one-off: what a person works with sits at the top; what explains it sits below, closed | Memory `feedback-working-content-first` | #186 |
| 187 | **A new relay at its position, its first settings from the template, and a record that says where it is and what it wears.** The owner, 2026-09-18, asked how a template gets onto a panel and went the intuitive way — Settings → the building → 2103 B-PROT → the SEL-221F record — and the screen invited two wrong conclusions: “I see that there is no FLOC so that is an indication that the device hasn't been placed yet and I am not sure how to determine if it has a template applied to it or not”. Both false on DEV: relay [3445] is Installed at its position since 2026-04-22 (the FLOC is empty because the panel and the position carry no code) and its model is bound to `SEL221F_Template` v2. He then asked that the FLOC line take the user “to the proper screen to enter this information”. **Verified before building**: no screen created a relay (the only `Asset_Add` in the app makes primary assets; the proof relays went in through the API); “New setting here” was a right-click on a settings-book ROW, which a relay with no record does not have; and `process.CopyRevisionAsDraft` returned silently for a device with no in-service revision, so SETTINGS_ADD ended in a file upload, against #168. **Built**: (1) **New relay** on a DevicePosition with nothing placed (`LocationScreen.tsx`): model, name, optional serial → `asset.Asset_Add` (ProtectiveRelay, InService) → `device.Device_Add` (part number = model code) → `asset.AlternateKey_Add` (SerialNumber) → `asset.PlaceAsset` (Installed); a refusal part-way names the asset so it is placed, not made twice. (2) **New setting** on a placed relay with no record: the person chooses the trigger and a reference; `raiseAndAdvance` (`lib/actions.ts`) raises SETTINGS_ADD scoped to the position, claims and commits step [1] with that trigger, claims step [2] and drafts its `devices` set with the relay (a draft is writable only on an Active step by its claimant — SaveDraft 50157/50164), and opens the work item; the engineer finishes [2] (scheme, philosophy) there. `CopyRevisionAsDraft` gains the empty first draft: a Designed SettingsText revision with no text, Status Draft, in the package, audited as `first-draft-from-template`, so the sheet opens Outstanding with every template setting “not set” and editable. (3) `document.vSettingsRecord` gains `PlacedFrom`, `PositionCode`, `PanelCode`, `TemplateKey`, `TemplateVersion` (Effective template via `config.vAssetTemplate` by model); the record shows **Placed** (“Installed at <position>, <panel>, since <date>”, the position a link), **FLOC** as the tag or “no tag yet — no code on <each uncoded level below the first coded one>” with each level a link to its LOCATION page (`location.fComposeFloc`: an uncoded Owner above a coded Division breaks nothing, so it is not listed), and **Template** (“SEL221F_Template v2 through the model”, a link, or “no template for this model yet”). **What the first smoke run taught**: the device drafting in `process.CommitStep` ran only inside the block of the step that PRODUCES the package (step [1]); the full procedure captures its Device set at step [2], which produces nothing, so [2]'s devices were never drafted — a pre-existing gap masked because #168 had proved it on the four-step procedure, where [1] captures both. Moved out of the produces block (§10b): a Device set captured at ANY step drafts into the package produced now or earlier in the run. Also `asset.vPlacedAsset.EntityId` is the placement, `AssetEntityId` the relay. **Seen, not built**: the scheme chosen at step [2] is captured on the step and does not make the relay a `scheme.SchemeMember`, so the record's Scheme line stays empty until membership is recorded elsewhere — the scheme-creation screen still to come. Chrome, at the owner's `TN-4416-BDG2-PNL23-21B`: New relay (SEL-221F, serial) → Installed, fifteen elements offered → New setting (Project) → the work item with [1] Committed and [2] Active, the relay already in its devices set → [2] committed with 2103 B-PROT and a philosophy → the record: Placed, FLOC `TN-4416-BDG2-PNL23-21B`, Template `SEL221F_Template v2`, the sheet Empty and editable; the [3445] record reads “no code on 2103 B-PROT (Panel) and PHASE DIST GROUND DIST & FAULT LOCATOR (21) (DevicePosition); open one to enter it”. Proof rows withdrawn (request Cancelled, revision, placement and relay soft-deleted) | Smoke 292 passed, 0 failed (four #187 checks, one of which found the CommitStep gap) | #187 |
| 188 | **Back on every screen; the record titled by its protection with its facts in a tab; the Locations list always there.** The owner, 2026-09-18, on the [3445] record: (1) “I want to have a ‘Back’ button on every screen that takes the user back to a previous screen. This occurs a lot in actual usage”; (2) the title “should really be the name of the protection ‘L2103 B-PROT’”, and the Device, Where (“very poorly named by the way, do better”) and Dates-and-state sections “take up too much room and should really just be another tab”; (3) in the settings book “the location list box should typically always be displayed and have a collapse button” — asked which reading, ruled **book only**. **Built** (web only, no view, seed or API change; no deploy, no smoke, as #186): `AppLayout.tsx` — a `‹ Back` button at the left of the bar every screen shares, the router's own history (`history.state.idx`), disabled at the first screen of the session with “nothing to go back to”; the screens' Close buttons stay (Close is “done with this”, Back is “the screen before”). `RecordScreen.tsx` — the title is the scheme's name as recorded (the device name when the record has no scheme), the relay, revision and parse state on the line beneath; the three panels become a **Record** tab, second after Settings, holding **Relay**, **Placement and scheme** (was Where) and **Dates and state**. `SettingsBookScreen.tsx` — the Locations list renders whatever the scope (it hid itself when the book was opened for one device or one change request), with a `‹`/`›` collapse in its title remembered per user (`pnc.<screen>.locations.hidden`); choosing a location while scoped leaves the scope (the book's own address with the location and state). **Stated, not asked**: the scheme is named `2103 B-PROT` in the data, so that is the title; “L2103” is the line it protects. Chrome on DEV, from the main screen: Settings → EEL RIVER 230 BDG → 2103 B-PROT → Open the record: title `2103 B-PROT`, the Settings tab first, the Record tab with the three panels; Back returned to the book with its list; the book scoped to one device showed the list, hid it, reload kept it hidden, shown again, choosing EEL RIVER 230 BDG dropped the device scope from the address | Verified in the browser; `npm run build` clean | #188 |
| 189 | **Back returns to the screen as it was left.** The owner, 2026-09-18, after #188: “ideally I would like to have the back button take us to the exact screen (with whatever device we had open before open etc.). At the present time it takes me back to a pristine version of the screen.” **Why it was pristine**: a screen's working state — the settings book's state tab, open groups, unfolded record and filters; the record's tab and comparison — lived in React state, and a route change unmounts the screen, so Back re-mounted it from the address alone. **Mechanism, one for every screen**: every history entry remembers the working state of the screen it shows. React Router gives each entry a stable `location.key`; `useEntryState(name, initial)` (`lib/hooks.ts`) is `useState` whose value is written to `sessionStorage` under `pnc.entry.<key>.<name>` and read back when a screen mounts on that key (Back, Forward, a reload of the same entry) — session-scoped, nothing to the server. `useScrollMemory()` in the layout does the same for the scroll position (restored after paint and again at 300 ms and 1 s, because the rows arrive after mount). `App.tsx` mounts one screen per entry (`<ScreenPage key={location.key}>`): a push to the same route — the book at another location — would otherwise keep the mounted screen and its state, and Back would not re-read the entry's own. A `replace` navigation makes a new key, so #188's in-place rewrite of the book's address on a location choice becomes a push: a location is a new place in the history. **Applied**: the settings book (state tab, open groups, unfolded record, text and field filters, the filter-by-field box, the locations filter) and the record (tab, comparison). Other screens get the scroll for free; their half-filled forms are not remembered — never saved, so not “the screen as it was left”. Chrome on DEV: Settings → EEL RIVER HVDC CONTROL HOUSE → filter “2103” → 2103 B-PROT opened → the SEL-221F card unfolded → Open the record → Compare → Change request → Back: the record on Compare → Back: the book at that location with the filter, the open group and the unfolded card exactly as left; F5 on that entry: the same | Verified in the browser; `npm run build` clean | #189 |
| 190 | **Double-click opens, right-click commands; the React app is the PWA.** The owner, 2026-09-18, on the settings book: movement by hot links (“Open the record”, “Change request” in the unfolded card) — “what has been found to work better in the past is to double click the description to take the user to the record screen and to use the right mouse button click which opens up a popup menu and provide options such as ‘Change request’, ‘New Device’ etc.”; and “this application will eventually need to be formatted as a PWA, it would be good if that could be done now as it does impact screen real estate usage.” **Found**: the right-click menu already existed (`data-grid.tsx` — right-click, Shift+F10, the ⋯ button; its items the definition's `commands`: Open the record, Change request, Set verified date, Request change, New setting here, Compare revisions); no double-click; the card's hot links were the definition's `card.links`. A PWA existed for the LEGACY root pages only: `manifest.webmanifest` started at `/`, one SVG icon, `sw.js` registered by `pnc.js` alone — the React app linked the manifest but registered no worker. **Built**: (1) `settings-book.screen.json` gains `rowOpen` (the list screens' convention) and loses `card.links`; `DataGrid` gains `onRowDoubleClick`; the book runs `rowOpen` on double-click and keeps the row unfolded so Back returns to it open (#189); the status line says so. The group header stays a fold (a group carries only its label). “New relay” stays at the position (#187), not a settings-book command. (2) The manifest starts at `/app/`, scope `/` (the legacy pages open inside the installed window), standalone, dark theme colour, SVG + PNG 192/512 icons drawn from the same mark (`wwwroot/icon-192.png`, `icon-512.png`); `sw.js` at `shell-11` adds `/app/` and the icons to the shell and falls back to `/app/` for an offline navigation under it — network first, never /api, /health or non-GET, as before; the React `index.html` carries theme-color and apple-touch-icon; `main.tsx` registers `/sw.js`. Standalone mode hides the browser chrome, which is why #188's Back matters. Chrome on DEV: right-click on the SEL-221F row → the six commands; double-click → the record; Back → the book with the row unfolded and a card of facts and text, no links; on `/app/` the worker is registered at scope `/`, active, controlling the page, cache `shell-11` only (`navigator.serviceWorker.getRegistrations()`, `caches.keys()`); the install prompt itself lives in the browser's address bar and was not observed from the page — UNVERIFIED by the session, for the owner to see | Smoke 293 passed, 0 failed (the manifest served as application/manifest+json, start_url /app/, PNG icons 200; sw.js at shell-11) | #190 |
| 191 | **A second change on a device: join the open request, or base a new one on it (pending modifications, part 1 of 2).** The owner, 2026-09-18, asked whether two change requests on one device should be allowed at once. **What the platform did**: nothing refused a second (`work.WorkRequest_Add` has no such rule); each new request copied the in-service revision (`process.CopyRevisionAsDraft`), so the second never saw the first's edits and whichever went in service last silently overwrote the other — a lost update on protection settings. The legacy program opened the existing request and let the person integrate; the owner distrusted one person editing another's request. Options put: leave it; one open change per device (refuse the second); or pending modifications — the second takes the first's data as its starting point and can only complete after the first, flagged if the first changes in between. **Ruled**: option 3, the flag to BLOCK approval/issue until re-based, in two increments; this is the first. **Built**: `document.RevisionLink` gains the kind `BasedOn` (subject `DocumentRevision`, identified by the basis revision's RowId, as `meta.fEntityExists` defines it — the first deploy linked by EntityId and `RevisionLink_Add` refused it; the smoke found it). `CopyRevisionAsDraft` chooses its source in order: the device's newest Outstanding record outside this package whose request is not cancelled — the **basis**, its CURRENT settings rendered by `RenderSettingsText` (its parsed rows; its file is rewritten only when its settings step commits), linked BasedOn, audited `draft-based-on-open-draft`; else the in-service revision; else the empty first draft (#187). `CommitStep`'s Baseline block refuses (50250) to put a revision in service while its basis is not in service and not withdrawn: *“<device> is based on the draft of "<request>", which is not yet in service; that request completes first.”* — the whole step commit fails, so the second cannot complete out of order. `document.vSettingsRecord` gains `BasedOnRevisionRowId`, `BasedOnWorkRequestEntityId`, `BasedOnWorkRequestTitle`, `BasedOnGridState`. `RaiseRequest.tsx`: raising against a device with an open request shows it (title, revision, status, procedure state) with **Open it — join that request** (every edit and step there is under the joiner's own name) and **Raise a second, based on it** (its note says the draft starts from that request's current settings and cannot go in service before it); nothing is raised until one is chosen. The record's Dates and state shows **Based on** with a link and “still outstanding; this revision cannot go in service before it”. **Stated for increment 2**: the frozen basis (the copy at copy time) is not yet kept as its own file — a draft's file is rewritten at its settings step — so requests based before increment 2 will have no frozen basis and must be re-based on first sight; a third based on the second chains by the same rule. Also this session: the function chips at the top of the Settings tab removed at the owner's word. Smoke, in the #168 four-step fixture: B's draft starts from A's current SLOPE 35 % (not the in-service 30 %), two outstanding records, B based on A; B's COMPLETE refused with the 50250 message while A is a draft; A completes, then B, B Active and A Archived. Chrome on DEV, [3445]: request A raised and taken to its draft; “Raise a change request” from the book showed *“SEL-221F Z1-3=.125-64 OHMS already has an open change request: #191 proof A — rev 3, Draft, procedure Running”* with the two choices; the second raised and taken to its draft; its record reads based on A, still outstanding. Both proof requests cancelled and their drafts withdrawn after | Smoke 305 passed, 0 failed (three #191 checks) | #191 |
| 192 | **The frozen basis, the “changed since” block, and the three-way re-base (pending modifications, part 2 of 2).** Ruled by the owner, 2026-09-18, after #191: when the first request changes after the second was taken from it, the second is flagged and **blocked at approval/issue until re-based**; nothing merged silently. **Built**: `process.WriteBasisSnapshot` freezes the basis — its parsed rows as `basis-<n>.json` (role Attachment) on the draft taken from it, at the copy (`CopyRevisionAsDraft`) and after every re-base; the newest is the frozen basis, older ones stay (a second file of one name is refused by `File_Write`, which the smoke found). `process.fBasisRevision` names the effective basis: the newest live BasedOn link, or the device's in-service revision when that draft is withdrawn or its request cancelled. `process.fBasisDrift` gives, per setting, **then** (frozen) · **theirs now** · **mine** and an outcome: `take` (theirs applies), `agree` (both moved to the same value), `conflict` (all three differ), `mine` (only mine moved — not drift); a draft with no frozen basis (made before this increment) reports every difference as a conflict with Then NULL, to be decided once. `process.BasisDrift` reads it over the API (`ConfigurationFile.Read`); `process.RebaseDraft` (`ConfigurationFile.Modify`) applies `take` rows through `SetParsedSetting` — one audited edit per setting — and conflicts by the engineer's decision (`{"CODE": "mine"|"theirs"}`), refusing the whole re-base (50252) when a conflict is undecided; then freezes the basis again, re-targets the link to the in-service revision when the basis is gone, and audits `draft-rebased` with the counts. `CommitStep` refuses (50251) the engineering check (Pass), the approval (Approved), the issue and the baseline while any based-on revision in the package has drift, naming the settings: *“<device> is based on "<request>", which has changed since this draft was taken (1 setting: SLOPE); re-base it first.”* The record screen shows the basis first on an outstanding based-on draft: the quiet line when unchanged; when changed, the rows (then · theirs now · mine · outcome), a theirs/mine choice on each conflict, and **Re-base — apply their changes**, disabled until every conflict is decided. **Also learned**: the API reads `api-permissions.json` from its build output — a new procedure is not callable until the API is rebuilt, not only restarted. Smoke, in the four-step fixture: A moves SLOPE to 38 % after B was taken — B's drift is one `take` row (then 35, theirs 38, mine 35) with a frozen basis; B's baseline refused with the 50251 message; re-base applies 1, B reads 38, no drift; A 40 % vs B 39 % is a `conflict`; a re-base without a decision refused naming SLOPE; ReadOnly refused; decided “mine” → kept 1, B reads 39, no drift; then the #191 ordering rule and the completions as before. Chrome on DEV, [3445]: A drafted, B based on it, A's line length moved → B's Settings tab opened with the amber panel (*“#192 proof A - 3445 has changed since this draft was taken · 1 setting”*, Line length then 55.00 · theirs now 56.00 · mine 55.00 · theirs will apply) → Re-base → *“Based on #192 proof A - 3445 — unchanged since this draft was taken.”*, B reads 56.00, two `draft-rebased` audit rows. Proof requests cancelled, drafts withdrawn | Smoke 309 passed, 0 failed (four #192 checks) | #192 |
| 193 | **The smoke leaves no trace on the Standards list.** Seen at #185 and asked about by the owner, 2026-09-19: Compliance → Standards carried 39 standard/version pairs, of which 30 were throwaway — `4_xxxxxx` and `r_xxxxxx`, version “1”, one requirement each, subject “smoke”, the oldest from 2026-09-16 (`compliance.vRequirementDetail`, `vStandard` on DEV). **Source**: the schema smoke `docs/schema/ddl/tools/smoke.py` (run by every `deploy.py` unless `--no-smoke`) creates a standard, a version and a requirement per run to prove `Standard_Upsert`, `StandardVersion_Add`, `Requirement_Add` and the rules bound to them, and withdrew nothing; fifteen deploys, thirty standards. Correct rows and honest history; noise on a screen an engineer reads. **Done**: `smoke.py` withdraws its own at the end of the run — `Requirement_SoftDelete`, `StandardVersion_SoftDelete`, `Standard_Deactivate`, attributed to the system actor, the audit rows kept — and checks the two codes are gone from `vRequirementDetail`; the 30 already on DEV withdrawn the same way once (30 requirements, 40 versions soft-deleted, 30 standards deactivated); the API smoke asserts the Standards list carries no “smoke” subject. No DDL change, no screen change (`vRequirementDetail` already reads live rows only), no deploy. **Found on the way, both dead since #181 and both fixed**: the schema smoke itself had been aborting at its wave-3 scheme section — it still created a `ProtectionFunction` node under a device position, which #181 forbids (`Seed_ref_LocationNodeTypeParent.sql:61`), so every run since 2026-09-17 died there unseen because deploys ran with `--no-smoke`; moved to the #181 model (the relay is the scheme member, the function fact is read on the position). And `scheme.RaiseProtectionOperation` collected the relays whose in-service settings it snapshots only through `ProtectionFunction` members, so since #181 (relays are `Asset` members) a protection operation snapshotted no relay settings — Asset members are now included; the revived smoke's snapshot check proves it. The smoke's cleanup withdraws all three standards it makes (W3r, W4, W4r). Verified: the schema smoke run against DEV after the change passed (270 checks, 0 failed) and left the list at the nine real standards (CIP-004, -005, -006, -007, -010, -011, NPCC-A10, NPCC-D4, PRC-023; 41 requirements); the Standards screen read in Chrome shows those nine and “41 shown of 41” | API smoke 311 passed, 0 failed | #193 |
| 194 | **The eight legacy classification fields go, and every migration decision is a rule the cutover replays.** The owner, 2026-09-19, on the record's Classification tab (Class, Use, Responsibility, Bulk power element, Protection group, Element, Line type, Number of relays, all “(migrated)”): “Most of this data is stale data … Each of these fields need to be reviewed and if redundant have the data removed … if not then we should ensure we understand what each means.” **Found on DEV**: none was a stored value — `document.vCharacteristicValue` held 0 classification rows over 11,850 records; the screen parsed them from the migrated record's summary text, where the importer dumped the legacy overflow columns; `LEGACY-FIELDS.md` said “Meaning: TODO” for all eight. Cross-tabulated against the platform's own data (11,549 records): Class is NOT the relay technology (MP on 286 electromechanical models, EM on 917 microprocessor ones, SS split 1,638/1,529); Use reads by its models as a device function class (M relays, T timers, D differentials, B breaker failure, A auxiliaries, R reclosers); Responsibility is the five regional offices and 142 of 223 stations carry more than one; Protection group A/B agrees with the scheme name about 85 % of the time; Element L/Y/U follows line / transformer / underfrequency schemes; Line type G/R only on lines, meaning unknown; Number of relays 3 on 1,018 records. **Ruled**: “all can go and the tab removed, I do not trust any of the data in those fields.” Number of relays — one settings file for three single-phase relays — is kept as design only: one settings-bearing asset with three components (`asset.AssetComponent`), the count derived, nothing migrated. Bulk power element does not seed the A-10 classifications. **Built**: the eight labels left the summary parser (`legacy.ts`); the Classification tab removed, the instrument-transformer characteristics kept on the Record tab when the model has no template (a template's Inputs shows CTR/PTR); `SETTINGS_RECORD` v2 with the ten CT/PT characteristics only; `legacy_import.py` no longer carries the eight columns and counts the rule `legacy classification columns … dropped — untrusted (#194)`; the DEV copy brought to the same state by stripping the segments from 11,561 summaries, attributed, recorded in `RECONCILIATION-2026-09-19-DEV-194.md`; `LEGACY-FIELDS.md` carries the ruling and the cross-tabs where “TODO” stood. **The owner's larger point** — “All of these 'migration' decisions need to be remembered so that we can duplicate during our actual cutover … a verified version from the client of the legacy database which I am going to want to apply all of these rules” — is now a mechanism: `docs/schema/migration/migration_rules.py` generates `MIGRATION-RULES.md` from the three places a rule can live — the importer's own `self.rule` calls (37), PostDeploy seeds that act on migrated data and carry a `-- migration-rule:` line (#178/#179 building codes and names, #181 the repoint, #194), and `DEV-ONLY-MUTATIONS.json`, the hand edits on a copy that are NOT rules (the proof rows, the smoke standards). `CUTOVER-STRATEGY.md §5` points at it; the standing rule is in CLAUDE.md and memory: a migration decision is a rule, never a hand edit. The rehearsal is not re-run here (55 min); the importer change is proven by its rule count in the next rehearsal report. Chrome on DEV: the [3445] record's tabs are Settings · Record · Compliance · Notes · Text as filed · Files and records · Compare, no classification anywhere | API smoke 311 passed, 0 failed | #194 |
| 195 | **The CIP impact rating is the building's: nowhere else offered, nowhere else accepted.** The owner, 2026-09-19, on the EEL RIVER HVDC station page: “I don't believe that Stations should have a CIP Impact Rating … the impact rating will be based on the building level and what protections are housed in those buildings.” His rulings #173 and #177 already said so; the screen and the rule lagged them. **Found on DEV**: 76 CIP ratings on location nodes, 75 of them API-smoke fixtures (a `W4_` station and building rated every run, never withdrawn); the only real one EEL RIVER HVDC CONTROL HOUSE (Building) = Low, recorded that morning by the owner's identity; no real station rated. `ref.ClassificationKind` still seeded `CipImpactRating` with `SubjectKinds ["Station"]`; `asset.RecordClassification` checked subject rules for assets only, so the panel stood on every node page and a rating on any node was accepted. **On the standard**: CIP-002 Attachment 1 writes its medium-impact criteria per station or substation (e.g. 2.5, 200–499 kV facilities at a single station with an aggregate weighted value over 3000); the BES Cyber Systems in a control house take that rating. Recording it on the building is consistent with the standard's asset-based approach and lets a station with two control houses (Eel River) rate them apart. **Built**: the kind's `SubjectKinds` is `["Building"]`; `RecordClassification` refuses (50235) a node of any other type for a kind that names node types — *“CipImpactRating is recorded on a Building, not on a Station (the rating is the building's, #177/#195)”* — withdrawals always possible; `location.fNearestClassified` unchanged (a device inherits the building it is in; a device outside any building inherits nothing and the evaluation reads Unknown, which is the truth). The panel is offered on a Building page only; a station page says where the rating lives and its buildings' ratings appear on its Inside-here list (“EEL RIVER HVDC CONTROL HOUSE · Building · CIP Low”, or “no CIP rating yet; open the building to enter it”). The API smoke's #173 block now proves a station is refused and the building's Medium is what the device reads, and withdraws its fixture building's rating at the end; the 75 fixture ratings on DEV withdrawn once (empty value, audited; DEV-ONLY-MUTATIONS). **Found by the smoke**: `compliance.vFactCatalogue` generated `device.location.classification.<kind>` only for kinds whose subject list named Station, so moving the rating to buildings made the CIP fact vanish and every CIP rule report unknown_fact; the catalogue now lists a device's inherited classification for any location level a kind is recorded on. Chrome on DEV: the EEL RIVER HVDC station page — no rating control, the building line reads “CIP Low”; the control-house page keeps the panel | API smoke 312 passed, 0 failed | #195 |
| 196 | **The A-10 classification is entered on the protected element; a device's cyber status is shown where its rating is shown.** The owner, 2026-09-19. (1) On NPCC A-10: “Eventually, the NPCC A10 classification will come from the studies or compliance group and a direct connection to their database of busses … there will need to be a connectivity model … that is too large of a body of work to get done in phase 1 … for now, the best method is for the user to manually enter the A10 classification (BPS/NONBPS) manually, at the protection level. Thoughts?” Put to him: manual, yes; at the protection level, no — A-10 classifies elements, one line is protected by four to eight settings records, and the relay is BPS only because of what it protects. The platform already derives a relay's NPCC obligation down the chain relay → scheme → protected element → terminal → bus (#184); the home for a hand entry is the **element** the scheme protects, one entry, inherited by every scheme and relay; when the study feed and a connectivity model arrive the buses become derived facts and a hand-entered element that disagrees is flagged for a person, not overwritten (the PMOD discipline). Agreed. (2) On CIP: “the final cip rating will depend on whether the device is a cyber asset or not, which doesn't appear to be happening” — the GROUND O/C position showed the building's Medium though its MCGG22 is electromechanical. **Verified**: the platform already decides this per device — `BesCyberAsset` is derived, never typed: BCA when the technology is Microprocessor or IEC61850 and the protected element is BES, else Not BCA (`Seed_config_ClassificationDerivations.sql`); the MCGG22 [1581] carries Not BCA (Derived), the SEL-221F [3445] BCA (Derived); #173's smoke proves an electromechanical relay gets no CIP obligation. The position page showed the location's rating without saying whether the device there takes it — a display gap, not a rule gap. Static (solid state, 10 models) is Not BCA under the derivation, as the owner says. **Built**: `NpccBulkPowerSystem` applies to every primary element type (Line, Transformer, Bus, Generator, Breaker, Capacitor, Reactor), so a line's page offers it; `compliance.fFixedFactValue` reads `device.protects.classification.NpccBulkPowerSystem` as the element's own declaration, else the bus's at the terminal the scheme protects from (a declaration on the element wins); `npcc_d4` reads that fact (a new rule version by payload); the record's Compliance tab and Protects line show the element's declaration first and the bus's after it, saying which rules. On a position page the Applicability panel gains **Device here**: “SEL-221F … (Microprocessor) — BES Cyber Asset: Medium impact, from the building” or “MCGG22 … (Electromechanical) — not a BES Cyber Asset; the building's rating does not apply to it”, read from the derived row; nothing typed. Smoke, in the #184 block: the line declared BPS with the bus Not BPS opens npcc_d4 on its relay; the declaration withdrawn, the bus's Not BPS stands in and it closes. Chrome on DEV: PHASE DIST position — “BES Cyber Asset: Medium impact, from the building”; GROUND O/C position — “not a BES Cyber Asset; the building's rating does not apply to it” | API smoke 313 passed, 0 failed | #196 |
| 197 | **PRC-023 applies only where an element in service is load-responsive; the reason a standard does not apply is read and kept.** The owner, 2026-09-19, on the L2103 GROUND O/C record: “neutral overcurrent relays are not load sensitive and as a result do not fall under any PRC-023 requirements … Somehow we need to make sure that all the appropriate standards get applied to the devices … this is going to be critical”, then “I believe that the PRC-023 standard provides a list of applicable elements that need to be considered. Look at it and determine if I am correct.” **Read** (PRC-023-6, the copy the seed cites, FERC letter order 2024-01-24): he is correct, and the standard carries two lists that both must hold. *4.2 Circuits*: transmission lines operated at 200 kV and above (4.2.1.1); transformers with low-voltage terminals connected at 200 kV and above (4.2.1.4); 100–200 kV lines and transformers, and sub-100 kV BES ones, only when the Planning Coordinator selects them under R6 (4.2.1.2/3/5/6). *4.1 / Attachment A*: the functional entity is one “with load-responsive phase protection systems as described in PRC-023-6 - Attachment A, applied at the terminals of the circuits defined in 4.2.1”. Attachment A 1 includes phase distance (1.1), out-of-step tripping (1.2), switch-on-to-fault (1.3), overcurrent relays (1.4), communications-aided schemes (1.5), phase overcurrent supervision of current-based pilot schemes (1.6); Attachment A 2 excludes elements enabled only when other relays fail (2.1), **“protection systems intended for the detection of ground fault conditions” (2.2)**, RAS-only elements (2.5), protection responding in 15 minutes or more (2.6), thermal emulation relays (2.7), **relay elements associated with dc lines (2.8) and dc converter transformers (2.9)**. **Found on DEV**: the rule `prc023_r1` read the element list only (`device.protects.terminal.voltage >= 200kV or … Prc023 = 'Listed'`); 155 obligations stood Open under it, 132 of them on positions coded 87T (transformer differential, not in Attachment A 1) and 102 on electromechanical relays; one on a position coded 21. `ref.AnsiFunction` held 820 codes, 13 of them C37.2 numbers and 802 legacy strings (21-B, 87T, 50/51N-A, 85TY-1 …); `scheme.CommissionedFunction` (#181: one row per element in service at a position) held 3 203 rows on 3 139 positions. **Ruled** (the owner): applicability from the elements in service in the record — “this is the value of the template … a device is only applicable if it has a load responsive element … in service” — with “automatic recognition of why PRC-023 (or any standard for that matter) does not apply to a particular device when it has load responsive element capability … there must be a reason”. **Built**: `ref.AnsiFunction.LoadResponsive` / `LoadResponsiveBasis`, set by the core seed for the C37.2 numbers with the clause each rests on (21, 78, SOTF, 50, 51, 67 included; 50N, 51N, 67N excluded by A 2.2; 87, 25, 27, 59, 79, 50BF not listed — 87 measures the difference current, not load); `ref.fAnsiLoadResponsive` reads a legacy string as the numbers it is built from — each ‘/’ part takes the longest ruled code that starts it, else its digits; the code is ground when N or G follows the digits of any part — so 50/51N is judged not load-responsive (both neutral elements), 21-B and 21Z2 load-responsive, 87T not, and 62B or 85TY-1 *not ruled*. Over the 3 203 commissioned rows: 665 load-responsive, 1 101 not, 1 437 not ruled. Two facts: `device.functions` (Set, `[load_responsive='true'|'false']`) and `device.functions.note` (each element with its ruling and clause); the rule `prc023_r1` gains the term `and device.functions[load_responsive='true'] is not empty` and records both facts. A device whose only elements are unruled reads Unknown, not false — the platform does not declare a standard inapplicable on an element it cannot classify. The evaluator's close now appends the reads to the NotApplicable row, so a closed obligation carries why it stopped applying; the record's Compliance tab runs the preview when it opens, lists each standard that does not apply with its reason in one line (“PRC-023 R1 — does not apply: no in-service load-responsive element — 50/51N: not load-responsive - ground fault detection (PRC-023-6 Attachment A 2.2)”), and gains an **Elements in service** panel (element · load-responsive yes/no/not ruled · basis). `vPositionFunction` carries the same three columns. **Not modelled yet, recorded here**: the transformer criterion (4.2.1.4, the low-voltage terminal's connection), the dc exclusions (2.8/2.9 — Eel River's converter-side relays are excluded even at 230 kV and nothing marks a dc line or converter transformer), the enablement of an element from the settings themselves (the SEL-221F's MTU/MPT/MTO masks decide which elements trip; decoding them from the manual's 3-20 table is what fills `EnabledFromConfigurationFileRevisionRowId`), and the normalisation of the 802 legacy codes (a migration rule to come; nothing rewrote them). Smoke: the fixture SEL-421 carries 21 beside 87T and stays bound; the BDD15B with 87T alone reads `false` with `device.functions {87T}` and the note; 51N reads not load-responsive with the 2.2 basis. An Effective run over DEV closed 148 over-applied obligations (NotApplicable, with the reads that closed them). Chrome on DEV: [1581] — “PRC-023 … does not apply: no in-service load-responsive element — 50/51N: … ground fault detection (PRC-023-6 Attachment A 2.2)”, Elements in service 50/51N · no; [3445] — R1 binds, 21 · yes · A 1.1 **Found on the way**: a rule given a new version (a scope edit) never saw the obligations its earlier version opened — the evaluator matched an open instance by rule *version* — so #196's and this increment's changes would have left 157 PRC-023 R1 rows Open for good; it now matches by the rule's definition, marks the earlier version's row Superseded and opens one under the version in force (4 superseded on DEV), and closes with the reads when the scope is false (148). A rule may also name `explain` facts, read when its scope is false, so the reason rides in the same trail as an opening. **Started, at the owner's request the same day**: `docs/design/APPLICABILITY.md` — one flowchart per standard of the layers the rules read and the layers still open (the PCA layer he described: a Cyber Asset on the same network as a BCA takes the BCA's requirements; the 15-minute test; ERC per requirement part; the dc exclusions; the transformer terminal; enablement from the settings). Schema smoke PASS. | API smoke 317 | #197 |
| 198 | **Compliance is never from memory.** The owner, 2026-09-19, on the applicability flowcharts (#197), two boxes of which (the CIP-003 low-impact family; “ERC selects requirement parts”) had been drawn from memory of the standards and said so: “an important rule... when dealing with compliance, never go by memory, all must be verified against the particular standard in question. This needs to be added somewhere where it will not be forgotten.” Recorded as a hard rule in `CLAUDE.md` (beside never-fabricate), as the first convention of `docs/design/APPLICABILITY.md`, and in the session memory. Applied at once: the two boxes are now *to read* boxes naming the document to open and asserting nothing of its content; the PCA layer is marked the same way — the owner's description is the requirement, the NERC Glossary and CIP-002 are the text to read before a rule is written. The standing method: a claim about a standard is read from its text in the version in force in New Brunswick (`Seed_compliance_Standards_NB.sql` names the versions and the documents), the clause is named, and what has not been read is not written down as the standard's | — | #198 |
| 199 | **One applicability flowchart, every standard attached where it binds — read from the texts; seven places where the platform's rules differ from them.** The owner, 2026-09-19: “I have some corrections that need to be made although you can likely review the standards and determine the errors as well”, then “incorporate these changes into the flowcharts and include in them where the various standards requirements sit … incorporate all of them into a single flowchart with the various standards attached to them at the appropriate locations.” **Read** (not from memory, #198): PRC-023-6; CIP-002-5.1a and NB Appendix CIP-002-5.1a-NB-0 (§G: BPS Cyber Asset with the 15-minute test, and “BES” meaning the NB Regulation's bulk power system); the Applicable Systems columns of every part of CIP-004-7, CIP-005-7, CIP-006-6, CIP-007-6, CIP-010-4, CIP-011-3; the NERC Glossary (Cyber Asset, BES Cyber Asset, PCA, ERC, ESP, EACMS, PACS, TCA — in force to 2028-06-30 and the 2028 successors); nbeub.ca versions and dates. **Errors found and corrected in the chart**: the 15-minute test is the BES Cyber Asset definition itself, not a further layer — the platform's derivation is a proxy for it; the impact rating is set per station or substation by CIP-002 Attachment 1 (2.4, 2.5 with the 700/1300 weights over 3000, 2.6–2.10), the building being only where the platform holds it (#195); Low is defined by CIP-002 3.2 and R1.3 (no discrete list) with its requirements in CIP-003-8-NB-0 (to read; -9 effective 2026-10-01); PCA is defined — a Cyber Asset connected by a routable protocol within or on the ESP of a BCS, rated as the highest BCS in that ESP — which is the owner's description, with the ESP as the qualifier; ERC is the BCS's through its ESP, not the relay's; the exemptions 4.2.3.1 (CNSC-regulated Facilities) and 4.2.3.2 (inter-ESP links) exist and are not modelled; PRC-023 R3 binds for criteria 7, 8, 9, 12 or 13; Attachment A 1.6 brings a current-based pilot scheme's phase overcurrent supervision in when it trips on loss of comms, so 87 cannot be blanket-ruled. **The requirements sit per part, not per requirement**: CIP-004 R2–R6 and CIP-005 R1.2/R2, CIP-006 R1.2–1.9/R2, CIP-007 R1.1/R4.2/R5.3/R5.6 bind Medium only with ERC; CIP-005 R1.1, CIP-006 R1.1, CIP-007 R2/R3/R4.1/R5.2/R5.4/R5.5, CIP-010 R1.1–1.4/R1.6/R3.1/R3.4/R4, CIP-011 R1/R2 bind Medium with or without ERC; CIP-010 R2.1 and R1.5/R3.2/R3.3, CIP-007 R4.4, CIP-006 R1.3, CIP-004 R5.3–5.4 are High only; PCA are reached by CIP-005/006/007/010 and CIP-011 R2, never by CIP-004 or CIP-011 R1. **Rules that differ from the text today, for the next increment**: cip004_r2/r4 (bind without ERC), cip005_r1 (ERC on all of R1), cip007_r1 (binds without ERC), cip010_r2 (opens at Medium; High only), prc023_r3 (13 only), the 87 ruling (87T right, 87L supervision per A 1.6), and the shape — one rule per part-group, so an obligation names the parts it stands for. `docs/design/APPLICABILITY.md` rewritten as one chart with the table of where each part sits; the drawn page updated. Then reshaped at the owner's direction — “I would prefer that the flowchart starts with assets and works its way down … if the client has an asset, they can follow it through to determine all of the compliance obligations pertaining to that asset” — to start at the primary asset: its BES status (Glossary BES definition, 100 kV, I1–I5, E1–E4; NB's own bulk power system definition to read), the bus at its terminals (A-10 → Directory 4 on every protection system protecting it), its voltage and type (PRC-023 4.2 → each relay's Attachment A elements → R1, R3, R4, R5), and its station (CIP-002 Attachment 1 → each Cyber System associated with its Facilities → exemptions, Cyber Asset, 15-minute test, PCA, ERC → the parts) | — | #199 |
| 200 | **The CTs and PTs on a tab of their own: Analog inputs.** The owner, 2026-09-19, on the SEL-221F [3445] record's Settings tab, where the Inputs panel (CTR 240, PTR 2000, SPTR 2000 with the manual's words on what feeds each) sat above the settings book and the book's “Current and potential inputs” category held the same three settings: “I would prefer to have the CTs and PTs located on a separate tab called something like ‘Analog Inputs’.” **Built** (web only): a record tab **Analog inputs**, second after Settings, holding the template's ratio settings as the same grid with the same per-field edit the book uses (the grid and its `process.SetParsedSetting` edit lifted out of `SettingsByFunction` into one `SettingsGrid`, used by both), then the ten instrument-transformer characteristics (CT main 1–4, PT main, CT aux 1–4, PT aux; migrated values or draft edits), which were the Record tab's and only shown when the model had no template — now every record's. The Inputs panel is gone from the Settings tab and the ratio rows are no longer offered in the book (a category emptied by that is not offered), so CTR/PTR/SPTR live in one place; `isRatio` stays the single definition of an analog-input setting. A model without a template gets the characteristics with a line saying so. Chrome on DEV: [3445] Analog inputs — CTR 240, PTR 2000, SPTR 2000 and the Instrument transformers panel; Settings — no Inputs panel, no Current and potential inputs category; [1581] MCGG22 (no template) — the Instrument transformers panel on Analog inputs, no longer on Record | — | #200 |
| 201 | **Instrument transformers are equipment in their own right: typed, placed, nameplated, named as a scheme's CT and VT sources, and checked against the relay's ratio settings.** The owner, 2026-09-19, on the Analog inputs tab (#200): “it is confusing to have auxiliary transformers and analog inputs … one set are the physical devices (PTs, CTs, aux transformers etc.) and one is the device settings … Instrument transformers should be first class devices in their own right with testing (saturation curves, ratio and polarity etc.)”, and: “auxiliary transformers were discussed back at the start of this project and were directed to be a hybrid device … Read back and find them.” **Found**: the vision §4.3 (decided 2026-09-03) — instrument transformers and the other auxiliary equipment are first-class equipment records in Phase 1, “a relay setting is expressed against the instrument transformers that feed it”; §10.1 — Hybrid is “one foot at primary voltage, the other on the secondary circuit”: CTs, PTs/VTs, insulators, lightning arresters, line traps, coupling capacitors, carrier tuners, merging units, NCITs (predecessor decisions 2, 3, 88). SCHEMA-DESIGN §4.1 carried the class; DEV held the Hybrid class row and no Hybrid type — the predecessor's eleven INTERFACE types were never brought across. The legacy database has no instrument-transformer table: CT_MAIN1 is a ratio string on 6 928 of 11 888 settings records (1200-5, 800-5, 600-1 … with NA and ???? among them). **Built**: `Seed_ref_AssetType_Hybrid` — the eleven types lifted and cited (CT, VT, COUPLING_CAPACITOR_VT, CCPD, COUPLING_CAPACITOR, WAVE_TRAP, METERING_UNIT, RECTIFIER_TRANSFORMER, VOLTAGE_REGULATOR as Hybrid; CT_AUX and METER as Secondary by the vision's rule, marked as differing from the predecessor; VT_AUX new, the legacy PT_AUX field names one). `CT_Template` and `VT_Template` (CharacteristicSchema.AssetTemplate: ratio in use, ratio taps, cores, rated secondary, accuracy class, burden, knee point, polarity marking, nameplate) bound as the types' default template through `ref.AssetType.DefaultTemplateDefinitionEntityId`, unused until now. `asset.vInstrumentTransformer` (identity, serial, placement and station, ratio in use, the schemes it feeds) and `scheme.vSchemeSource` (a scheme's CtSource / VtSource members with the ratio read as a number: 1200:5, 1200-5, 1200/5 → 240). Screens: **Instrument transformers** list in the nav, the **Instrument transformer** page (equipment, nameplate editable through one asset-characteristics panel, the schemes it feeds with an add-as-source action, a Tests panel that says #202), **New instrument transformer here** on a station, building, yard, bay, equipment position or panel page (Asset_Add, AlternateKey_Add, PlaceAsset — a non-device asset stands at any node — and the ratio as its characteristic), and on the record's Analog inputs tab a **Fed by** line per ratio setting: the scheme's CT sources against CTR, its VT sources against PTR and SPTR, *matches* / *differs from the setting* / *the scheme names no source* — the legacy ratios below it titled as the record's declared ratios. No migration rule: no transformer is invented from a string. **Found on the way**: with a location.Node alias in scope SQL Server reads `[location].[fStationOf]` as the Node's GEOGRAPHY column's method (Msg 209) — the station is found inside the placement subquery; a seed's `SELECT @e =` that finds no row leaves the variable as it was (the VT template was skipped on the first deploy; `SET @e = NULL` first); the post-deploy seeds are registered in `PostDeploy.sql`, not globbed; `ref.vLocationNodeTypeParent` gives a Bay no parent at all (a Yard sits in a Station, an EquipmentPosition in a Yard) — the smoke's CT stands at an equipment position, and a bay cannot be made anywhere until a rule says where. Not built, said on the page: the tests (#202: a Program.TestPlan per kind — ratio, polarity, excitation curve — recorded against the transformer with its readings; the definition's step shape is not established yet). Chrome on DEV: a CT “L2103 line CT” 1200:5 made at EEL RIVER HVDC, named 2103 B-PROT's CT source from its page; the [3445] record's Analog inputs reads “CTR = 240 — L2103 line CT 1200:5 = 240 · matches”; the Instrument transformers list shows it with its station and the scheme it feeds | API smoke 321 | #201 |
| 202 | **An instrument transformer is a child of the Yard.** The owner, 2026-09-19, on #201's talk of bays: “instrument transformers on a transmission network are not installed in a bay, as they are on a distribution network. On the transmission network, they are located in a yard so they are located as a child of the Yard. Now, later on when we start looking into other locations we may place bays within buildings and then auxiliaries could also be placed as a child of bays but for now, they are in the yards only.” **Built**: `asset.PlaceAsset` refuses (50218, in the owner's words) a CT, VT, CVT, CCPD or metering unit anywhere but a Yard, and a panel-mounted auxiliary CT or VT anywhere but a Panel — widening to bays is one line there when that day comes; the location page offers *New instrument transformer here* only on a yard (the main types) or a panel (the auxiliaries), so it never offers what the database refuses. The smoke's CT is made in the fixture's yard and a CT at a panel is refused. DEV: a *230 kV yard* (Y230) made under EEL RIVER HVDC and the L2103 line CT moved into it (hand entries, not migration edits) | API smoke 322 | #202 |
| 203 | **A bay is a child of a building.** The owner, 2026-09-19: “bays will be children of a building” — the bay that may come inside a building, where auxiliaries could later stand (#202). **Built**: the pair (Bay, Building) in `Seed_ref_LocationNodeTypeParent` and active; the yard-side bay pairs #174 set aside stay deactivated (that ruling stands: the client's yard has no bays). `location.AddNode` reads the table, so the location page now offers a Bay under a building and refuses one under a yard in the database's words. What may sit under a building's bay is a later ruling. Smoke: a bay under the fixture building 200, under its yard 409 | API smoke 323 | #203 |
| 204 | **An instrument transformer's tests — ratio, polarity and the excitation curve — are a procedure run against it; each step's readings are the sheet.** The owner (#201): “Instrument transformers should be first class devices in their own right with testing (saturation curves, ratio and polarity etc.)”; 2026-09-19: “go ahead with #202's tests increment.” **How V2 records a test, verified**: `config.TestPlanStep` / `TestPlanReading` were removed in W0 (STEPS.md) — a test plan is a `Program.Procedure` document, started by a workflow's `onEnter.startProcedure` under a `Program.WorkType`, against a request scoped to an Asset; a step's `capture` fields are held as its draft and, on commit, the step produces the `record.Record` it names (`process.CommitStep`), with the request, the performer, the outcome and the procedure version; the captures stay readable on the step (decision #115); the `record.TestSheet / TestReading` subclass rows are the Doble-import shape and no step writes them. **Built**: `CT_TEST` — [1] Ratio (tap, applied and measured current, ratio found, error, method; Pass/Fail; sheet), [2] Polarity (Correct/Reversed, method; sheet), [3] Excitation curve (knee point voltage and current, the points as read one V, I pair per line, method, the test set's export as evidence; Pass/Fail/Marginal; sheet), [4] Engineer review (remarks; Accepted/Rejected; an Attestation); `VT_TEST` — ratio (winding, voltages, ratio, error), polarity, review. Workflows `CT_TEST_REQUEST` / `VT_TEST_REQUEST` (the simple request shape: In progress starts the procedure; Close needs it Completed) and work types `CT_TEST` / `VT_TEST` binding them; all seeded through `AddProcedureVersion` / `AddWorkflowVersion` and approved only while no Effective version exists (a deploy never overturns an authored version). No formula expression in the documents, so none needs the compiler; no reading limit is enforced — a ratio-error acceptance depends on the accuracy class, a rule to come as data; the technician's outcome and the engineer's review are the verdict, which is the requirements' split (the tool keeps its raw artefact; the platform owns the accepted result and the readings). The transformer page's **Tests** panel: *Test this transformer* raises the request for the type (CT_TEST for a current type, VT_TEST for a voltage type) and opens the work item, where the generic step screen takes the readings; the panel lists the transformer's test requests with their state and its sheets and attestations with when, outcome, summary and the committed captures read from the step (`process.vStepInstance.Draft` by `CommittedRecordEntityId`). **Found on the way, and ruled**: the engine started every procedure over the workflow's subject — the request — whatever the document declared, so the first sheet stood against the request and the transformer's page found nothing. Now `process.StartProcedure` runs a root procedure over the subject its document names: declared `Asset`, started by a workflow over a request scoped to an asset, it runs over that asset with the request kept on the instance; a scope of another kind is refused (50148); `WorkRequest` (or `Any`) runs over the request as before — every existing procedure declares `WorkRequest`, so no run changes. `work.vChangeRequestStatus` and `document.vSettingsRecord` find a request's root run by `WorkRequestEntityId` (a new filtered index; the plan seeks it), not by subject; all 12 123 root runs on DEV are still found. The request raised on DEV before the rule (5b052c71…, its ratio sheet against the request) is left as it is — a DEV hand entry, not data to migrate. Smoke: a CT_TEST request on the fixture CT starts the procedure; the ratio step refuses a commit without the required tap (50191, “required captures are missing: tap”) and commits with the readings; polarity, excitation and the engineer's review commit; three TestSheet records and one Attestation stand against the CT; the request closes on the completed procedure; ReadOnly cannot raise one. Chrome on DEV: the L2103 line CT — Test this transformer → the work item → [1] Ratio claimed and committed as Tech Smoke (1200:5, 600 A, 2.5 A, 240, 0 %, Primary injection, Pass) → the CT page reads Tests · 1 sheet: the request In progress, the sheet 11:00 PM [1] Ratio test — Pass, and the six readings | API smoke 330 | #204 |
| 205 | **The Analog inputs tab holds the transformers that feed the protection; the relay's ratio settings are settings, back in the book.** The owner, 2026-09-20, on the [3445] record's Analog inputs tab: “there are actually relay settings that should be moved to the settings tab. The analog inputs tab should only contain information on the CTs and PTs which are feeding the protections and not device specific settings.” #200 had put the template's ratio settings (CTR, PTR, SPTR on the SEL-221F) on that tab as an editable grid and taken them out of the book; #201 added the Fed-by check beneath them. **Now**: the settings book shows every category of the template, the ratio category (“Current and potential inputs” on the SEL-221F) among them, with the same per-field edit when the revision is outstanding; the Analog inputs tab is **Feeding this protection** — the scheme's CT and VT sources (`scheme.vSchemeSource`), each a link to the transformer's page with its type, role, ratio in use and the number it makes, in service or not, and which relay ratio setting it feeds with the verdict (matches / differs, with the setting's value / not set / ratio not readable); a ratio setting with no source of its kind named is listed as such (“PTR — 2103 B-PROT names no VT source; name it from the transformer's page”); no scheme, no template and no source each said in words. The verdict stays on this tab, phrased from the transformer's side, because it is about what the transformer feeds; the setting is read and edited only in the book. Below, unchanged, the CT and PT ratios the legacy record declared as text (the SETTINGS_RECORD characteristics) — a description of the transformers, not a device setting. No schema, API or smoke change. Chrome on DEV: [3445] Active — Settings tab, category Current and potential inputs: CTR 240, PTR 2000, SPTR 2000; Analog inputs: “Feeding this protection · 1 transformer — L2103 line CT · CT · CT source · 1200:5 = 240 · feeds CTR · matches”, PTR and SPTR each “names no VT source”, the declared ratios below and no settings grid; [0747] Outstanding — the category offers the CTR, PTR and SPTR value boxes | API smoke 330 (#204's run; no API change) | #205 |
| 206 | **The instrument transformers a protection needs exist: made from the legacy record and the devices' functions by a replayable rule; edited, placed, connected, retired and deleted by the user.** The owner, 2026-09-20: the bottom of the Analog inputs tab is “what was imported from the legacy database, which can be retired completely”; from the current and potential inputs “there are three sets of instrument transformers … a three phase set of CTs feeding the protection, a three phase set of PTs feeding the protection and a single phase PT feeding the sync input … the application should create those three devices and assign them to this protection. There may be no formal FLOC set for them yet and this will require user input”; the application “must [allow] the editing/adding/deleting etc. of instrument transformers so that the user can also manually correct any outliers”; and the global rule: “when a particular device indicates that it has CT and PT inputs, it is reasonable to assume that they exist because without them, the protection is useless … a KD relay is a distance protection which requires voltage and current inputs.” Asked whose the transformers are: the scheme's, one set per distinct declared ratio — “no matter which way we go on this, it is going to require the user to correct a large number of errors” (breaker-and-a-half paralleled CTs, separate secondary windings). This reverses #201's “no transformer is invented from a string”: the string is the ratio of a set the device's own inputs prove exists. **Found first**: the legacy CT/PT strings were never stored characteristics — the importer puts them in the migrated record's summary and the tab showed them as placeholders; no migrated station has a Yard, so the sets stand unplaced until a person places them; the parse of CTR/PTR/SPTR runs after import, so the rule reads the strings and the functions, and the parsed settings stay the cross-check. **The rule** (`Seed_asset_InstrumentTransformersFromLegacy`, replayed on every deploy, idempotent by an `asset.AlternateKey` of kind MigrationSource, never re-creating a set a person retired, never duplicating a source the scheme has): per scheme with an in-service device, one CT set per distinct `CT_MAIN1..4` ratio and one PT set per `PT_MAIN` (name `<scheme> CTs 1200:5`, Phases 3, RatioInUse, role CtSource/VtSource); where no string named one and a device's functions need the input, a set with the ratio unknown; a single-phase `<scheme> sync PT` (role **SyncVtSource**, new) where a device has a 25; per device its `CT_AUX1..4` / `PT_AUX` as CT_AUX / VT_AUX at its panel. On DEV: 1803 declared CT sets and 816 PT sets, 3 + 34 sets from the functions' floor, 44 sync PTs, 502 auxiliaries (502 at their panel), for 1178 schemes; 3214 instrument transformers in all. **Which inputs a function needs** is data: `ref.AnsiFunction.AnalogInputs` (I, V, IV, VSYNC, NONE) with its basis, read through `ref.fAnsiLoadResponsive` (NeedsCurrent / NeedsVoltage / NeedsSyncVoltage on `scheme.vPositionFunction`) over the same base codes as the PRC-023 ruling; 39 codes ruled — the table proposed by the session from the device numbers' definitions and approved by the owner with the plan; it is NOT read from a standard's text and the basis says so; a code the catalogue lacked is added with its number as its name (no C37.2 name invented). Devices needing current 1569, voltage 724, a sync voltage 156. **Retired**: the SETTINGS_RECORD characteristic schema (soft-deleted), the tab's legacy section and the `legacy.ts` keys; the summary text keeps the strings as the imported record. **Found on the way, and fixed**: `UX_Placement_InstalledAtNode` let a node hold ONE installed asset — the predecessor's one-device-per-position rule — so a second transformer in a yard, or a second auxiliary on a panel, was refused (met first as a doomed publish transaction, then on the L2103 CT's yard from the page). Now a placement row says whether it **shares its node** (`asset.Placement.SharesNode`, nullable because the table is system-versioned; the unique index applies only to rows that do not), `asset.PlaceAsset` sets it from the node type (a DevicePosition or EquipmentPosition holds one; a Yard, Panel, Building or Station holds many), and `Seed_asset_PlacementOccupancy` corrected the rows written before (515 rows share their node now). Also: a scheme name is not unique — 2103 B-PROT exists at EEL RIVER HVDC and at BATHURST TERMINAL — and the publish runs its post-deploy in one transaction, so a rule must pre-check and never throw. **Built for the user**: the transformer page edits the equipment (`Asset_Revise`), places it — the yards of its station (known through the scheme it feeds when unplaced) with **New yard at <station>** (`location.AddNode`) — works each membership (in service, the **connection note**, remove: `SchemeMember_Revise` / `_SoftDelete`, one component on both pages), retires it and deletes it once no scheme names it; the Analog inputs tab shows the transformers with phases, placement and note, and **Add a transformer feeding this protection** (type, role incl. Sync VT source, phases, ratio, note); Phases joins both nameplate templates (added to the Effective version so recorded values keep their rows); the location form takes Phases. Chrome on DEV: [3445] Analog inputs — “2103 B-PROT PTs 2000:1 · VT source · 3-phase · not placed · feeds PTR · matches” beside the L2103 line CT, “SPTR — names no Sync VT source”, no legacy section; the PT's page: station by the scheme, Where it stands offering the 230 kV yard and New yard, Feeds with the actions, Retire, Delete withheld while it feeds a scheme; placed in the 230 kV yard beside the CT once SharesNode landed | API smoke 334 | #206 |
| 207 | **The predecessor's CCPD type is the S&C potential device — the neutral potential device on a capacitor bank — and is named so.** The owner, 2026-09-20, on the Analog inputs tab's add-a-transformer list: “what is the difference between a coupling capacitor potential device and a coupling capacitor voltage transformer? Are you referring to the S&C neutral potential device used for capacitor banks when referring to potential device? and normal CCVTs with the other?” The session had not drawn that line: both codes were lifted from the predecessor's INTERFACE types in #201 and CCPD's description was written from its code name; no definition of it exists in the predecessor's docs or schema (searched). Ruled: “define CCPD as the S&C neutral potential device although the naming could cause issues so in this case we should indicate S&C potential device.” **Done**: `ref.AssetType` CCPD keeps its code; its name is **S&C potential device** and its description says what it is (a single-phase voltage from the bank neutral, feeding a protection as a VT source; the VT nameplate template stays bound); the add-a-transformer form defaults it to single-phase. COUPLING_CAPACITOR_VT stays the CCVT. No asset of either type exists yet, so this is a wording change, replayed by the seed. Chrome on DEV: the add form's type list reads S&C potential device beside Coupling capacitor voltage transformer | API smoke 334 (#206's run; no API change) | #207 |
| 208 | **A protection's analog input is a thing — the CTs paralleled into it, and the relay-side endpoint the cabling will land on.** The owner, 2026-09-20, with the Eel River one-line: line 2103 is fed breaker-and-a-half from E2103 and E2103-TC4; “each of the breakers will have their own set of CTs feeding into the SEL-221F device. For these older protections which only have a single set of current inputs, the two sets of CTs will be parallelled before being brought in to the device”; the application must handle two (or three) CT sets on one input with the same test capability each already has, must show that they are paralleled — “they will always be connected in parallel so a more appropriate method may be just to allow the user to add parallel CTs to the ct input” — and must not block the later cabling work (“from the physical instrument transformer locations in the yard, through cable trenches, into the building and to the panels etc. with all the associated cable details (cable numbers, terminal blocks etc.)”). **The design, in a paragraph**: `scheme.AnalogInput` is a scheme's relay-side endpoint — “Current 1”, “Current 2”, “Voltage 1”, “Sync voltage 1” (BiTemporal, one code per scheme); every source membership (`scheme.SchemeMember.AnalogInputEntityId`) feeds exactly one; two or more transformers on one input are paralleled — derived from the count, never stored; the input is where the coming cabling ends (the transformer's secondary at the yard end, the input at the relay end, cables, trenches and terminal blocks between — later entities that reference both ends), so nothing here is undone for it; tests stay per transformer (#204). **Built**: `scheme.AddSchemeMember` lands every source on an input — the one given (which must be this scheme's, 50272) or found-or-made by kind (the lowest-numbered “Current n”, “Voltage n”, “Sync voltage n”; made when the scheme has none) so every caller that never heard of inputs still works; `scheme.vSchemeSource` carries the input and its ParallelCount; `scheme.vSchemeInput` (the generator took `vAnalogInput` for the table's own current view) gives each input its sources, distinct ratios and IsParallel. The record's Analog inputs tab is grouped by input, judged as one against the relay setting of its kind — *matches* when every paralleled ratio equals the setting, *paralleled ratios differ* (arithmetic, not a standard's claim) when the CTs on an input disagree, *differs* when they agree with each other and not with the setting — with **Add a paralleled CT** on each current input and an input choice (existing or new) on the add form; the transformer page names each membership's input and its partners in parallel. **The catch-up rule** (`Seed_scheme_AnalogInputs`, replayable, idempotent): every source without an input — the #206 rule's, the hand-made and the smoke's — gets one per scheme and role by distinct ratio in ratio order (the same ratio shares the input; a different ratio is a different input, so an auxiliary PT is not read as paralleled with the main set); the first run had put every VT source on one “Voltage”, so a rule-made source on a mixed-ratio input whose ratio is not the input's lowest is detached and regrouped, and the unnumbered codes take their number. On DEV: current inputs 1960 (33 paralleled), voltage 983 (108 paralleled — same-ratio VT sources of one scheme, the user's to confirm), sync voltage 46; 427 schemes with more than one current input (the differentials' CT_MAIN1..n); no source without an input (0). Two publishes: the tables, then the generated procedures (#182). Chrome on DEV, Eel River 2103 B-PROT [3445]: the L2103 line CT on Current 1 → Add a paralleled CT → “E2103-TC4 CTs 1200:5” → “Current 1 · 2 CTs · in parallel · feeds CTR · matches”; the new CT's page: “Current 1 · in parallel with L2103 line CT”, its own Tests panel | API smoke 337 | #208 |
| 209 | **A stray voltage class in the transformer's list was schema-smoke debris; the smoke now sweeps its own leftovers, and reference data gets a screen next.** The owner, 2026-09-20, editing a CT's voltage class: “the first selection [r_a45729 (69 kV)] is obviously an error, where did it come from and can the user delete it from the list.” **Found**: the schema smoke (`tools/smoke.py`) makes a throw-away 69 kV class `r_<6 hex>` with two relays on it and deactivates it at the end of the run; the 2026-09-19 09:14 run failed before its cleanup, so that one class stayed active — the 36 others of its shape on DEV were inactive; seven `smoke_*` asset types, one model and one manufacturer of other aborted runs were active with it. The voltage-class list reads only active rows, so nothing else showed. No screen offers a reference-data retirement and the API does not expose the reference procedures to a signed-in user, so today only a database hand can do it. **Done (agreed)**: the leftovers deactivated on DEV as the system actor (soft; the audit stays; recorded in DEV-ONLY-MUTATIONS as not-a-rule — test fixtures, not migrated data); the schema smoke sweeps, at the start of every run, whatever an earlier run left active by the smoke's own code shapes, so an aborted run can no longer leave a fixture in a list. **Next (agreed)**: a reference-data screen where an Administrator adds a voltage class, corrects its name or nominal kV and retires one — the “the user can delete it themselves” asked for, as its own increment. | — | #209 |
| 210 | **Reference data has a screen: voltage classes added, corrected and retired by the people who may.** From #209 (the owner: “can the user delete it from the list since it is incorrect and should not show up as an option?”). **Corrected on the way**: the session had said the API did not expose the reference procedures — it does: `api-permissions.json` maps the `ref` schema to the Definition class, so `ref.VoltageClass_Upsert` needs Definition.Modify (PCEngineer, Administrator) and `ref.VoltageClass_Deactivate` needs Definition.Archive (Administrator); only the screen was missing. **Built**: the **Reference data** screen (Administration; `REFERENCE_DATA`, a list definition over `ref.vVoltageClass` that `pages/Screen.tsx` hands to `ReferenceDataScreen`): the active voltage classes in display order, each corrected in place (nominal kV, transmission, order — the code is the key and cannot be renamed: add the right one and retire the wrong one), retired on a second click (the page says how many assets carry the code and that they keep it), and added (a retired code added again is reinstated, said in the form); read-only without the permission, saying which role it needs. The transformer's edit form and the primary asset's terminal select keep a retired class as an option marked “(retired)”, so an unrelated save never clears it. Chrome on DEV: Reference data → 500kV added → offered in the E2103 CT's voltage-class list and saved on it → retired from Reference data (“1 asset carries it and keeps it”) → gone from the list; the test value taken off the CT afterwards. | API smoke 339 | #210 |
| 211 | **The Analog inputs tab reads first and edits on purpose; a second CT set is placed by what the relay can take.** The owner, 2026-09-20, after correcting the Eel River 2103 B-PROT transformers and ending with an empty “Current 2”: (1) “Data input is still too confusing to ensure that the users are inputting things in the proper manner”; (2) “Is there a way for the user to correct these errors themselves?”; (3) the badge-style buttons “are very confusing as I don't know whether they are simply information tags, buttons, status … These actions entities need to be moved somewhere where it is obvious what their functionality is.” And on the planned “paralleled or a separate input?” question: it “will depend entirely on whether the device … supports one or more than one current input … an SEL-221F device only has one possible CT input … an SEL-411L would have the capability to have more than 1 … have the application recognize the capabilities of the device being applied and tailor these questions based on those capabilities.” **The capability is data the platform holds**: a settings template carries one ratio setting per analog input — SEL-221F: CTR, PTR, SPTR (one current, one voltage, one sync input); SEL-311C: CTR, CTRP, PTR, PTRS (two current, a sync); SEL-551: CTR, CTRN (two current) — read from the seeded templates, recognised by code (CTR*, PTR*, SPTR*), unit (ratio) or the ratio category; a model with no template has no known capability and nothing is invented for it. **Built**: the tab says what the relay takes (“takes 1 current input (CTR), 1 voltage (PTR) and 1 sync voltage (SPTR) — read from its settings template”), names each scheme input's relay setting (“Current 1 · CTR”), and flags an input beyond the relay's count (“beyond this relay's inputs”). **View mode** is information only — inputs, transformers, ratios, status pills, notes; **Edit inputs** turns on a column headed Actions with plain labelled buttons (Mark not in service, Connection note, Remove from scheme; per input: Add a paralleled CT, Remove this input when empty) and the add forms; Done returns to view; the transformer page's Feeds panel has the same two modes (Edit feeds). **Where a new set lands is decided by the relay**: one input of the kind and it already fed → no question, the form says “SEL-221F has one current input (CTR): this set is paralleled into Current 1 with E2103, E2103-TC4”; more inputs than the scheme uses → one plain choice by radio button, paralleled into an existing input (default) or a separate input named by the relay setting it would feed; no template → the same choice without names. **Empty inputs cannot linger**: removing the last transformer from an input removes the input with it; an empty one shows Remove this input. Chrome on DEV, the Eel River record: view mode as above with Current 2 flagged; Edit inputs → Actions column → Remove this input → Current 2 gone; the add form states the SEL-221F rule; Done; the E2103 CT's page shows Feeds in both modes. Follow-up the same day: the relay is named by its short model name (“SEL-221F distance relay”, the model name without its legacy-label suffix) and each input's table has fixed column widths so the columns line up from one input to the next — the owner: “this looks much much better”. | API smoke 340 | #211 |
| 212 | **An instrument transformer's secondary windings are first-class: each with its own ratio and nameplate, each usable by a different protection.** The owner, 2026-09-20: “most if not all of them come with a single primary connection and multiple (2, 3, 4 or 5 typically) secondary windings each with its own ratio, name etc., which can be used in various protections. There needs to be a way for the user to add secondary windings to an instrument transformer and assign them to the various protections. At the moment, I believe that the app is handling this by notes only, but this is a physical thing that needs to have a first class place in the database.” And the standing instruction, recorded in CLAUDE.md and memory the same day: look for where the model is thinner than the plant and raise it — the phases build towards one platform holding the primary system, the secondary system “right from the secondary CT winding used, to the cabling, to the panels, devices”, the DC and telecom systems, the 61850 network, maintenance and compliance; “build our early phases such that the later phases are an addition as opposed to a complete redesign.” **The paragraph**: a winding is a row of `asset.InstrumentWinding`, a child of the transformer (the AssetTerminal shape): its number and code (S1, 1Y, X), purpose, ratio taps and the ratio in use, accuracy class, burden, knee point, rated secondary, connection. A source membership names the winding it uses (`scheme.SchemeMember.WindingEntityId`; `AddSchemeMember` validates it as the transformer's, 50273, and takes the lowest-numbered when none is given), so “2103 B-PROT Current 1 is fed by E2103 winding S1” is a fact and the relay's CTR is judged against that winding's ratio (`scheme.vSchemeSource` reads the winding's ratio first, the old nameplate characteristic as the fallback). The transformer's nameplate keeps only what is the whole transformer's — Phases, polarity marking, other — the seven per-winding keys soft-deleted from the CT/VT templates (recorded values keep their rows). The winding is the yard-side endpoint the cabling will reference; its taps, text now, become the terminals then. **The catch-up rule** (`Seed_asset_InstrumentWindings`, replayable): every transformer without a winding gets S1 carrying its recorded ratio; every source membership without a winding gets its transformer's lowest. On DEV: 3233 windings (2983 with a ratio), no transformer without one (0), no membership without one (0). **Built for the user**: the transformer page's **Secondary windings** panel (view; Edit windings with an Actions column — edit in place, remove when nothing uses it; Add a winding), the Feeds panel naming each membership's winding with a **Use winding** select in edit mode, the same select on the record's Analog inputs tab, and the add forms making S1 with the ratio typed. `asset.vTransformerWinding` gives each winding what uses it (named so: the generator owns `vInstrumentWinding` as the table's current view; and its key is `WindingEntityId`, since the API scopes a view's `EntityId` as the family of its base table — a winding's id would have been checked as an asset's and every row hidden). **Gaps raised, not built** (the standing instruction): a three-phase set is three physical units with their own serials and tests (a later increment: phase units as child assets of the set); auxiliary CTs and PTs are elements in the secondary path, not sources (they move into the path when the cabling phase models it); tests are per winding (the next authored CT_TEST version captures the winding by reference); ratio taps are terminals (rows when the cabling comes); no burden calculation is attempted. Chrome on DEV, E2103's page: Secondary windings S1 1200:5 from the catch-up → Edit windings → Add S2 600:5, 1200:5 taps, C400, 5 A → the record's Analog inputs tab, Use winding S2 → “paralleled ratios differ — E2103 600:5, E2103-TC4 1200:5” → back to S1 → matches | API smoke 342 | #212 |
| 213 | **A winding's taps are discrete rows; the ratio in use is the tap the wires are landed on.** The owner, 2026-09-20, on #212's fourth gap (ratio taps kept as text): “should the available taps be a type of setting? Every CT has discrete tap capabilities which should be listed even though they may not be known at this time.” Agreed: a tap is a terminal pair with a fixed ratio on the nameplate (X1-X2 = 600:5, X1-X3 = 1200:5); the ratio in use is which pair is landed — chosen, discrete, changed on purpose — so it is a setting of the protection in the CTR sense, and it must stay honest where the nameplate has not been read yet. **The paragraph**: a tap is a row of `asset.WindingTap`, a child of the winding: tap number, terminals (optional — some nameplates list ratios only), ratio, notes; one current row per winding and tap number; bi-temporal. The winding's `TapInUseEntityId` names the tap landed, and `asset.SetWindingTap` is the one way to land it: the tap must be a current tap of that winding (50274), and the winding is revised — audited, bi-temporal — with the tap and its `RatioInUse` copied from the tap's ratio in the same act, so the relay's CTR check (`scheme.vSchemeSource`) and every list keep reading `RatioInUse`; a null tap clears the landing and leaves the typed ratio as it is. A winding with no tap rows keeps its text ratio and says so (“taps not recorded”); a winding with taps listed and none landed says “tap not landed”, and its ratio field is no longer typed by hand. The tap rows are the winding-side terminals the cabling phase will land on. No migration rule: taps are not in the legacy record and nothing is invented — on DEV today 6 tap rows, 1 landed, over 3239 windings (3236 with no taps recorded). **Built for the user**: the transformer page's Secondary windings panel shows each winding's ratio as the tap landed (“1200:5 · X1-X3”) or the typed ratio; a **Taps** toggle per winding lists the taps (terminals, ratio, in use); in Edit windings: Add a tap, **Land on this tap**, **Use the typed ratio** (clears), Remove a tap not in use. `asset.vTransformerTap` (named so because the generator owns `vWindingTap` as the table's current view — #212's lesson, re-learned when the first publish's hand-written view was overwritten; keyed `TapEntityId` with `AssetEntityId` for the scope) lists them; `asset.vTransformerWinding` carries `TapInUseEntityId`, `TapInUseTerminals`, `TapCount`. `SetWindingTap` is hand-written and listed in `api-permissions.json` as Asset.Modify. **Stated, not built**: landing a tap is audited today; making it a step of a work request like a relay setting — designed, checked, applied in the field, read back — is a later increment, when the tap joins the settings record's inputs. Chrome on DEV, E2103's page: Edit windings → S2's Taps → X1-X2 600:5 and X1-X3 1200:5 recorded → Land on this tap X1-X3 → the S2 row reads “1200:5 · X1-X3”; the 2103 B-PROT membership stays on S1 | API smoke 344 | #213 |
| 214 | **Compliance evaluates itself: a change a rule reads is re-evaluated within seconds, the standing verdict is on record with its reason, and the device's tab has no button.** The owner, 2026-09-20, on L2103 B-PROT's Compliance tab: “why is an evaluate now button necessary? Should the evaluation be an automatic function of what is presently known about the system?” Ruling: event-driven, along with the removal. What stood: an hourly pass (#171), a preview on tab open (#197) that fed the “does not apply — why” list from memory only, and two buttons (Evaluate now = the preview again; Commit = the hourly pass early). Nothing persisted said when a device was last evaluated, and a rule that never applied left no row at all. **The paragraph**: a write that changes something a rule reads leaves a request (`compliance.EvaluationRequest`: what changed, by which procedure, by whom — `Engine/ComplianceTriggers.cs` names the procedures, called after every successful write through the generic endpoint and after a step commit or transition over a device); the compliance worker (`ComplianceService`) takes the pending requests every five seconds, expands them to the devices affected (`compliance.ExpandEvaluationRequests`: the device, the devices protecting an asset or from its bus, the candidates under a node, a scheme's members and protectors, everything for a rule approved — the candidates and their protects walk materialised once per turn: the first cut, an inline function applied per request, timed out at 30 s under the smoke's load thirteen times and left requests pending for five minutes) and runs one Effective pass over them (trigger FactChanged; RuleApproved for an approval); the hourly pass stays as the catch-all, with a catch-up pass at start when any candidate has no standing evaluation. Every Effective pass leaves, per subject, when it was last evaluated (`compliance.SubjectEvaluation`) and, per rule and derivation, the standing verdict with its reason and reads (`compliance.SubjectVerdict`, written only on change, so SinceAt is when the verdict began; both system-versioned, so what the platform believed on a date is the history). The device's tab reads that (`compliance.vDeviceVerdict`, `vDeviceEvaluation`) — “Evaluated 17:41 · after a change”, each rule applies / does not apply — reason — since / undetermined / could not be evaluated, the PRC-023 working from the recorded reads — and runs nothing. The reason's words moved from the tab to `ComplianceEvaluator.Reasons` so they are recorded, not recomputed. A person runs a pass by hand only from Compliance › **Evaluation** (the requests, the passes with duration and notes, the rules a pass could not evaluate, Run the pass now with Obligation.Modify). **Measured on DEV**: a request is served 4–9 s after the write in the smoke (the 5 s poll plus a 1–3 s pass over the devices it touches); the catch-up pass over the estate (275 candidate devices) 141 s. Today 291 requests (291 served), 72 FactChanged passes averaging 2138 ms, 275 devices with a standing evaluation, 5225 verdict rows. **The debris**: the red “smoke_w4_…_R: no scope” on the owner's clip was a schema-smoke rule the W4 block approved and retired only at the very end of the run — a run that died before its end (#209's finding again) left it effective for every pass until the next run; the block now retires its rules as soon as it is done with them, the start-of-run sweep (#209) also retires the smoke's own standards a dead run left, and a rule a pass cannot evaluate is named on the pass, never on a device. **Gaps raised, not built**: a write made in SQL directly, a seed, a ruling in the fact catalogue itself (`ref.AnsiFunction`) leaves no request — the hourly pass covers them; a device that loses its last classification and protects link stops being a candidate and its open obligations are not closed by any pass (true before too; candidates ∪ subjects with open instances is the later fix). API smoke: the write→evaluate pairs of #173/#184/#196/#197 became write → wait for the platform → read the record (no smoke pass runs Effective by hand except the one that finds nothing left to do); #214 checks the requests, the FactChanged runs, the process hook and the verdicts. Chrome on DEV: L2103 B-PROT's Compliance tab reads the standing verdicts with no button; the SEL-221F's position (PHASE DIST … (21)): 21 taken out of service → the tab reads “PRC-023 R1 does not apply — no in-service load-responsive element” and the obligation closes; 21 put back → “applies”, a new obligation opens (the position left as found); the Evaluation screen lists both requests, served by their FactChanged passes | API smoke 346 | #214 |
| 215 | **The logic mask editor: the SEL-221F Relay Word is data from the manual, and a mask setting is edited bit by bit.** The owner, 2026-09-20, on the Logic settings tab of a 221F record: “One of the things that would make setting these obsolete relays easier is to create a logic editor for the SELOGIC settings … an editor to assist the user in selecting the appropriate bits. This editor should be selected through the use of an edit button on the setting.” What stood: the ten masks as `Format = 'mask3'` template rows (#168), the value three hex bytes stored verbatim, a free-text cell; the 24 Relay Word bits nowhere in the repository. **Read from the manual** (IM 981207, `Z:\Archive-WorkingData-Cloud\Work\DATA\Manuals\SEL\221F-2-3-4_IM_19981207.pdf`, this session): 2-5 Table 2.1 and 2-20 (the Relay Word, three rows of eight), 2-21 Table 2.4 (each bit's meaning), 3-14/3-15 (SHOWSET shows the masks as hex; “A4 → 1010 0100” laid over 67N…REJO, so the left bit of a row is the most significant bit of its byte), 3-20 (LOGIC n, the ten masks; “one selects and zero deselects a member of the mask”), 5-32…5-38 (each mask's purpose, typical bits, example, and “Never mask the TRIP bit into the MTU/MPT/MTB/MTO logic mask”; “The TRIP bit is replaced by the BFT bit in the SEL-221F-3/121F-3 and SEL-221F-4 models”; the bits intended for relay testing). **The paragraph**: a relay's Relay Word is data, not code — a `Program.RelayWord` definition (`RELAY_WORD_SEL_221F`, generated by `tools/relay_word_sel221f.py` beside the template's generator, reviewable in `docs/design/examples/templates/sel-221f.relay-word.md`, every string the manual's with its page) naming the settings template it belongs to, the three rows of eight bits with meanings, the byte order, the -3/-4 variant, the testing bits, and for each of the ten masks its purpose, caution, typical bits, example and the never-bits. On the Settings tab a mask row shows its hex with **Bits** (the decoded rows under the setting, ticked bits named and explained) and, when the revision is outstanding, **Edit bits**: tick boxes with each bit's meaning, the row's hex as it changes, a hex field kept in step both ways, the mask's guidance from the manual, a red line with the manual's words when a never-bit is ticked, row 3 bit 2 named “TRIP (BFT on the -3/-4)”; Save writes the filed shape (“F0 A4 00”) through the same `process.SetParsedSetting` as any other value, so the file the platform writes keeps its form and the round trip stays byte-identical (#168). Another SEL relay with masks is another definition, no new code (`lib/relayWord.ts`, `DeviceSettings.tsx` MaskBits). **Verified**: Chrome on DEV — the 2103 B-PROT record's MTU “F0 A4 00” decodes as Z1P Z1G Z2PT Z2GT · 67N 51NT 50H, the manual's own MTU example; on an outstanding 221F draft, Edit bits → the seven ticked → the hex reads F0 A4 00 → Save → “MTU saved as F0 A4 00”, the file the platform writes carries `LOGIC SETTINGS: MTU=F0 A4 00`; TRIP ticked → the warning “Never mask the TRIP bit into the MTU logic mask…” (5-33) and F0 A4 40. **Gaps raised, not built**: the device's variant (-2 or -3/-4) is recorded nowhere (the model code does not say it; `sel-221f.capabilities.md`: a person must know which relay is in front of them) — a characteristic or the firmware row, later; with the bits as data, decoding MTU/MPT/MTO to say which elements actually trip (#197's EnabledFrom…) becomes possible — later; the SEL-121F shares this Relay Word but has no template yet | API smoke 348 | #215 |
| 216 | **A device's hardware configuration is recorded on the device — the SEL-221F's jumper positions from its manual — and the instruction manual is kept with the template, open in the page.** The owner, 2026-09-21: “the SEL-221F must have communication port settings… baud rate, databits etc. but I don't see them in the template”, then “I believe that the port settings were accommodated through set P x”. Read from the manual (IM 981207): the command summary (3-28/3-29) lists SET and LOGIC n only, no SET P; “The baud rates of the ports are set by jumpers located near the front of the main board… Available rates are 300, 600, 1200, 2400, 4800, and 9600 baud”; the serial data format “eight data bits, two stop bits, no parity bit. This format cannot be altered” (3-2); JMP105 selects the baud per port, JMP103 disables password protection, JMP104 enables OPEN and CLOSE (6-2, 3-8, 3-22). So the template is right to carry no port settings, and the jumpers had no place in the platform — the owner: “I agree with your assessment, make the necessary additions to handle this.” And: “I would like to have a copy of the manual kept with the template and accessible for review in a tab or possibly separate window… the field staff work on a single monitor.” **The paragraph**: a device's hardware configuration is a group of characteristics on its asset template — `SEL221F_Template` v3 gains **Hardware**: PORT 1 and PORT 2F/2R baud (a closed list `SEL221F_BAUD`, the manual's six rates), JMP103 and JMP104 as Yes/No, every entry the manual's words with its page (`tools/template_bundle_sel221f.py`, which now seeds enumerations). A value is recorded per relay through one guarded, audited procedure, `asset.SetAssetCharacteristic` (the key resolved against the device's template — the model's, else the type's default; typed by DataType; checked against the list, 50291; the prior row closed in valid time; NULL clears; `audit.LogAction` with key, from, to and the work request) — the generated `CharacteristicValue_Add/_Revise` checked nothing and logged nothing, and every characteristics save goes this way now, the instrument transformers' nameplates included. The record's Settings tab shows **Hardware** under the settings book, editable only while the record is Outstanding (the change request is recorded on the audit row); it is the relay's, not the revision's — every record of the device shows the same values — and nothing of it goes into the settings file. The instruction manual is a document of class `InstructionManual` whose revision holds the PDF and links *About* the template definition (`RevisionLink.SubjectKind` gains Definition and DefinitionVersion), loaded per environment from Z: by `tools/load_manual.py` (idempotent by SHA-256; the PDF never enters git — `docs/runbook/LOAD-MANUALS.md`); `GET files/{id}` serves a PDF or a text inline with ranges (`?download=1` for the download); the record gains a **Manual** tab and the template screen a Manual panel — the PDF in the page in the browser's own viewer (a frame over a blob the page fetched with its identity; the CSP gains `frame-src 'self' blob:`, `frame-ancestors 'none'` stays), with **Open in its own tab** for a second screen. **A security decision**: `Document.Read` on a document was Global-scope only (`security.fHasPermission`, the rule of #134 for subjects with no place in the tree); a REFERENCE document — one whose revision is linked About a Definition — is now read by any role carrying `Document.Read` at any scope, as definitions are: it is the manufacturer's book, not a site's record, and the field staff at one station need it. Every other document stays as it was. **Verified**: API smoke — PORT 1 = 2400 recorded and read back, 4321 refused as not in the list, JMP104 Y → true, an unknown key refused, ReadOnly refused, the audit row names the key and the work request, cleared again; a document links About the template definition; the loaded SEL-221F manual served inline as application/pdf (7 658 721 bytes) and read by ReadOnly and by the Node-scoped engineer. Chrome on DEV: the outstanding 221F draft's Settings tab → Hardware → PORT 1 2400, JMP104 Yes, saved and read back; the Manual tab shows the 260-page PDF in the page with its own viewer, and Open in its own tab. **Gaps raised, not built**: baud is per port and there is no port child table yet (two keys are the interim shape; ports as rows later, the #212 pattern); the manual's page cites are printed labels and the viewer takes physical pages (a page map per manual would let a cite open its page); the procedure records the work request but does not refuse a write without one (a jumper can be found changed on site) | API smoke 351 | #216 |
| 217 | **The legacy rationale documents come in as a replayable rule — one Document per file, linked About the configuration-file revision it belongs to, frozen there.** The owner, 2026-09-21, on 2103 B-PROT: “was there a rational document found for this record? There should be one.” None was in the platform: decision #159 had deferred documents to a later wave and the import never read the legacy document folder. Found on Z:\Archive-WorkingData-Cloud\Work\Backup\Documents: 143 station folders (Edmundston to Woodstock — the first half of the alphabet is absent, Bathurst Terminal's 199 in-service records with it), 9,858 files, every one dated 2026-04-22, the legacy backup's day; 8,831 named by the rotation (A 3,508 · M 2,087 · P 3,236). The owner: “importing … is necessary to get the system up and running reliably but will not be updated past the revision that they are presently on”; the rationale as an idea is “very important in that they explain why something is the way it is”, but the Word document is “a source of double entry” — the structured rationale that generates the settings values, the file and a Word document from one entry is the next increment, and “the word rational documents eventually disappearing”. **Measured before the rule was written** (`dbRelayManagement_Legacy_Cutover`): an A file matches the base's single A row; a P file's CR is the P row's own for 1,979 and a dropped D record's for 1,169; an M file's CR belongs to the P row the change became for 1,244 (the copy predates the rename) and to the M row for 188 — so a file is matched by NUMBER and CHANGE REQUEST across letters, through the provenance key `Revision:<letter><number>|<CR>` the migration already carries, and the file's letter is only the document's state when copied. **Corrected the same day**: the first form of the rule matched A files by number alone and reported 2,199 files in a folder that was not their number's station — that figure was an artefact of a poor folder-to-location name match on the session's side. The owner: “the folder should have lined up with the terminal station in all cases. There are instances where two documents can have the same Axxxx number since numbers are reused after retirement.” Re-measured with the folder names normalised to the legacy LOCATION (135 of 143 folders map): 6,610 of 6,887 placeable files sit in the folder of the row they were placed on; 169 A files sit where no row of their number is; 10 A files sit where the number's P or D row is (a retired device, its number reused — `a0006.doc` at Iroquois Terminal is the retired device's, the A row is Eel River 138); 98 change-request files disagree with their revision's station by the folder only. So the station is decisive and a number alone names no device. **The paragraph**: `rationale_stage` in `legacy_import.py` (`--docs <folder>`; without it the stage loads nothing, so the run replays unchanged on the full share): each rotation-named Word file becomes a Document of class **Rationale** (seeded) with one Issued revision holding the file as filed (`document.File_Write`, Attachment, the bytes bound as varbinary(max) on the run's cursor) and a `RevisionLink` About the configuration-file revision (SubjectKind DocumentRevision) — never the device, so a later revision's record cannot mistake it for its own; provenance `RationaleFile:<name>` with the bytes' hash (a re-run writes nothing for an unchanged file; changed bytes are flagged, the earlier file stands); An A file is placed by NUMBER AND STATION — the folder maps to a legacy LOCATION by name; the A row of the number at that station, else the retired device's last superseded (P) row there (the chain orders by change request, §5), else not loaded and flagged `RationaleFolderMismatch`; a change-request file goes to the row with its number and change request (unique), and when only the folder disagrees it is loaded there and flagged (the owner, 2026-09-21: “load and flag”); a folder that names no legacy location loads nothing (flag `RationaleFolderUnknown`); the same file filed twice for one revision loads once. The provenance key is the folder and the name, so the same name in two station folders is two devices' documents. Every file the rule cannot place is counted under its reason. The record's Files and records lists the rationale first, “Rationale (legacy, as filed)”, the Documentation button opens that list, and a record whose legacy documentation track reads NA says so. **On DEV** (the corrected rule, replayed after the first placement was withdrawn — `DEV-ONLY-MUTATIONS.json`): loaded 3,427 by number and change request, 3,161 A documents to the in-service revision of their number at the folder's station, 12 A documents to the retired device's last superseded revision at the folder's station, and 100 change-request documents whose folder disagrees with their revision's station (loaded there, flagged); 44 files filed twice for one revision loaded once — 6,656 documents on 6,515 revisions (a revision can hold more than one: the A and the M copies of a completed change both name its row). Not loaded — 1,190 whose change request belongs to a dropped D record (R-06), 555 matching no revision, 224 A documents whose folder holds no row of their number (flagged), 34 whose folder holds only a dropped record of the number, 105 in the six folders that name no legacy location (Grand Lake Plant, Grand Lake Unit 8, Great Falls Plant, Nepisiquit Falls Unit 1, 2 and 3), 23 with no legacy row for the number, 84 Word files not named by the rotation; skipped 556 Word lock or recovery files and 387 relay setting files beside the documents (`RECONCILIATION-2026-09-21-DEV-217.md`; flags `RationaleFolderMismatch` 324, `RationaleFolderUnknown` 6; the register regenerated). **Verified**: API smoke — a Rationale document linked About a configuration-file revision through the generic endpoints is read back and retired; a migrated rationale is served as `application/msword` attachment. Chrome on DEV: BE1-59 A3E [0002] → Files and records lists “Rationale (legacy, as filed) A0002.doc” first and the name opens it as a Word download (a logged read, #144); the Iroquois Terminal copy of `a0006.doc` sits on the archived P0006 revision (change request 680) with the importer's note beside it — “the number's in-service row is at Eel River Term 138; this is the retired device's last record at Iroquois Terminal” — and the Eel River copy on the in-service revision; 2103 B-PROT → “The legacy documentation track for this change reads NA — no rationale document was recorded for it.” B-PROT's change 9617847 has no document: the legacy Relay Document Management row reads NA — no rationale was recorded for it. **Open ask**: the complete documents share from the client (the copy on Z: is half the alphabet and five months old); the rule replays on it and the reconciliation says what was and was not loaded. **Gap raised, not built**: the migration models a legacy number as ONE device (`devices_stage`), so a retired device whose number was reused appears as the archived early revisions of the current device, placed at the current device's station — the Iroquois P0006 record reads Eel River Terminal. The rule places the rationale where the legacy rows say, but the device itself is still one asset; a later rule should split a number's row history into devices by station run, the retired ones as retired assets at their own station. | API smoke 353 | #217 |
| 218 | **A Word document opens in Word itself, from the record, through a short-lived link the API issues for that one file and that one user.** The owner, 2026-09-21, on the saved download: “That isn't very convenient for the user, is there no way to open the file up directly in word”. There is: Windows registers the Office URI scheme, and `ms-word:ofv|u|<url>` makes Word fetch the document from that URL and open it read-only — proved on the build laptop against a throwaway file server (Word sent OPTIONS, HEAD, GET and opened the file). The obstacle is identity: Word fetches with its own HTTP client, which carries neither the page's DEV header nor, reliably, the browser's session; on DEV a plain link answers 401 “No identity.” **The paragraph**: the page asks the API for a link (`POST files/{id}/link`, authorised as the read itself is — `Document.Read` on the file's document, decided now); the link is `/api/v1/files/{id}/link/{token}/{file name}`, the token naming the file, the user and an expiry (default 90 s), signed with a key the server holds (`Files:LinkKey` in ignored config; a random key per process when absent, so links then outlive neither the process nor their lifetime); on the GET, `RequestUserMiddleware` accepts a valid token as that user's identity for that file only (`FileLinkTokens`), and the read is authorised and logged to them as any other — another file, an altered or expired token → 401 `link_invalid`. The page launches `ms-word:ofv|u|<link>`; the browser asks once whether to open Word; “save” beside the name keeps the download. A PDF or a text still opens in its own tab. **Measured on the way** (the build laptop, Word 2014-labelled Microsoft 365): Word requests nothing for a URL whose last segment has no document extension; it drops a query string on its second round of requests, so the token cannot travel there; it probes the link's folder with OPTIONS before HEAD and GET, and a 401 on the probe ends the open (the middleware now answers OPTIONS under the file routes with 204 and `Allow`, no identity, nothing else); and after one failed open it makes no further request to that host and port for a while (the retest had to move to a fresh port). **Verified**: API smoke — a link opens the migrated rationale with no identity of its own (GET, HEAD, OPTIONS 204), the token opens its own file only, ReadOnly is issued a link too; Word on the build laptop opened `A0394.doc` (relay [0394], Eel River Terminal) straight from the DEV API through the issued link, in Protected View, the read logged to the link's user. UNVERIFIED in this session: the browser's own “Open Microsoft Word?” prompt on the page's click — Chrome shows it outside the page, where the session's tools cannot see or press it; the owner's first click answers it. **A security decision**: a signed, expiring, single-file link is the identity for one GET — the first place the platform accepts an identity from anything but the directory or the DEV header. It is issued only to someone who may read the document at that moment, names them, and is logged to them; it is not a share link (90 s, one file, no listing). In production the API sits behind Windows authentication: UNVERIFIED whether IIS lets the linked GET through anonymously to the token, or challenges Word for Negotiate first (Word would then authenticate as the signed-in user, and the token is redundant but harmless) — to be settled on the first Windows-authenticated host. | API smoke 356 | #218 |
| 219 | **The structured rationale: one section per protective element, the settings sheet grouped the same way, the supervising settings handled explicitly — the engineer enters the inputs once and the platform makes the settings values, the settings file and the Word document. And the runbook for bringing a model in.** The owner, 2026-09-21: “lets continue with the next steps. Be sure that we are taking notes of all of these steps so that we can repeat them again for other relays without this back and forth” — `docs/runbook/BRING-A-MODEL-IN.md` (part 1, `4b26509`): the seventeen steps of the SEL-221F with their tools, the “Settled — do not ask again” table of nineteen rulings, the traps, the per-model checklist; CLAUDE.md: a model increment updates it in the same commit. Then, on the first plan for the rationale: “I am not sure how it will be successful...as a single document...There are too many variables. The only way I see this as being a possibility is to have a rational 'section' for each of the protective elements being used … there is a distance tab but all the elements are globbed together when in fact, they should be grouped together in Zone 1, Zone 2 etc. with all appropriate settings. Same goes for the overcurrent elements which should be broken up into phase, ground … there could be a rational statement for each of these which, in the end, could be combined by the application to create the rational document”; “we will need to use the 15 elements to make sure that we can handle all the possible protections...an important point which also must be accounted for are the settings which are used to supervise elements … supervising overcurrents for distance protections”; on the grain (the fifteen capabilities with the Relay Word bits beneath them): “that is the right direction, lets go”. **Read from the manual** (IM 981207, 2-18 “Distance Relay Logic”, 2-24), the supervision as the relay has it: `Z1P = (21AB1·50AP·50BP + …)·FDS·NOT(LOP·LOPE)`, `Z1G = (21AG1·50AG + …)·50N·FDS·NOT(LOP·LOPE)`, zones 2 and 3 alike, `FDS = 3P21 + 32Q`, `67N = 67NP·[32Q + (LOP·LOPE) + NOT(67NTC)]`, `51NP` likewise; “the negative-sequence directional elements always supervises the distance elements.” Read from two legacy rationales on DEV (A4811.docx Grand Falls L1175 2002/2023/2024; A0394.doc Eel River 2000/2026): one skeleton — revision history; relay, CT/PT; line data to the remote terminal; numbered decisions per element; masks with their purpose; a settings summary. **The paragraph**: the ELEMENT MAP is part of the relay's Relay Word definition (`tools/relay_word_sel221f.py` v2): sixteen elements (Z1, Z2, Z3 under 21; 50P and 50H under 50; 50NG; 51N; 67N; 32Q; LOP; 79; 25 with 27/59; REJO; SOTF; FAULTLOC; 50BF) each with its capability, its outputs, the settings it OWNS and what SUPERVISES it, every relation quoting the manual's equation and page; six plain groups (ID, LINE, INPUTS, TIMERS, COMMS, MASKS) hold the settings that are no element's — the 56 settings each in exactly one place. The settings sheet sub-groups every tab by element with its outputs and its supervision line (`ElementGroups` in `DeviceSettings.tsx`; nothing hidden, #183). The RATIONALE TEMPLATE is a `Program.Rationale` definition (a new kind: decision 78 keeps payloads to Program kinds, so the template is a program whose characteristic definitions are the INPUTS and whose payload the SECTIONS; the §8.8 kinds `RationaleSchemeType`/`RationaleDevice` stay for the philosophy split) — `RATIONALE_SEL221F_LINE` (`tools/rationale_sel221f.py`): twenty-one sections in document order (shared LINE, INPUTS, FAULTSTUDY; one per element; TIMERS, COMMS, MASKS), ninety-five inputs with defaults labelled by their source document, fifty-four formulas (grammar 1, compiled by FormulaCompile; `line.*`, `device.*`, `input.*`, `value.*`, `setting.*` — `MTA = round(atan2(line.X1, line.R1), 2)`, `CTR = round(input.CtPrimary / input.CtSecondary, 2)`, `50H = coalesce(input.Pickup50H, round(0.5 × input.CloseInFaultA, 0))`, the charging current from the line's MVA/mile, length and voltage), a statement per section with `{placeholders}` and the element's `{supervision}` line, `Used_`/`Trips_`/`Recloses_` marks per element from which MTU, MRI and MTO are derived in the Relay Word's bit order. The line's impedance moves onto the line asset: `LINE_Template` (`tools/template_line.py`: R1, X1, R0, X0 Ω primary, LengthMiles, ChargingMvaPerMile) as the type's default template, recorded through `asset.SetAssetCharacteristic` on the primary-asset screen's Impedance panel. Applying (`RationaleEngine`; `POST /api/v1/rationale/{revision}/apply`; a Draft revision only) resolves the line — the scheme's, the stored pick, or the engineer's (`Line` input, a Reference) — and its facts, evaluates the sections in map order, writes every owned setting through `process.SetParsedSetting`, stores the inputs as `document.CharacteristicValue` rows on a Draft Rationale revision linked About the settings revision (`document.CreateRationale`, `document.SetRationaleValue`), files `rationale.json` and `rationale.docx` — the platform's own WordprocessingML writer (`Documents/DocxWriter.cs`, no package) assembling title block, revision history, the shared sections, the fault-study table, one numbered section per element with its values, the masks and the settings summary — and logs the act; `document.ApproveRevision` issues the rationale with its settings revision, after which nothing edits it. The record's Rationale tab shows it all and opens the document in Word (#218). **Verified**: API smoke — the #170 line carries impedance; the #187 draft's template has 21 sections over 16 elements and the picked line resolves every fact; apply writes Z1% 85, Z2% 130, CTR 160, PTR 1200, R1/X1 as the line's, MTA as atan2 of the line, MTU from the elements' marks; the zone 1 statement names the remote station and its supervisors; the rationale revision holds both files and the Word package reads back; a second apply updates and refiles; ReadOnly is refused. On DEV: L2103 (Bathurst — Eel River HVDC, 230 kV) given A4811's line values as a DEV test entry; the [0798] outstanding draft at Grand Falls applied with L2103 picked — 53 settings written, MTU F4 A0 00 (Z1, Z2, Z3, 51N, 67N), MRI F0 80 00, MTO 00 04 00; Word opened the generated document (108 paragraphs, 3 tables); Chrome: the Settings tab's Distance elements sub-grouped Zone 1 / Zone 2 / Zone 3 each with its supervision line; the Rationale tab with the line, the facts, the sections and Open in Word. **Gaps raised, not built**: the defaults (85 %, 130 %, delays, pickups) are the two legacy documents' practice, labelled so — a written NB Power line-protection philosophy becomes the cite and the scheme-type half of §8.8; CT/VT ratios are inputs until the scheme's analog inputs carry a ratio per winding tap (#211/#213); the fault study is a table input, not yet a cited Study document; MPT, MTB, MA1–MA4, MRC stay inputs; the 50BF section is a placeholder (variants -2/-3/-4 unmodelled); the position's commissioned functions prefill nothing where none are recorded (DEV). | API smoke 363 | #219 |
| 220 | **A relay's record shows the relay's own history, raises the next change from where the person is standing, and lists only documents on Files and records.** The owner, 2026-09-22, walking 1175 A-PROT: of a button that opens the one change request that made the revision — “wouldn't the user simply go to some history section of the database to find out historical information...for every change request on a device....all in one place?”; of Compare — “a comparison between the same device through two change requests, is also not very useful. Users can review the rational of each when a change request reason is not enough. The one area when a comparison of settings would be valuable would be to compare an 'online' version which would be downloaded from the device with what is in the database as the in-service setting. Basically a confirmation of what is in service is what you think is in service. This again would be in a much later stage … that would require communications with field devices which hasn't even been planned yet”; of the last two rows of Files and records — “I am not seeing any great value in them”; then “agreed on all changes above”. **The paragraph**: the record gains a **History** tab — every revision of that relay, newest first, with the change request that made it (a click opens it), who asked for it, when it was requested, calculated and verified, its time in service, and the rationale document it holds, opened in Word (#218); a change the migration brought in is marked *legacy* and shows the legacy record's own dates instead of the import's. **Compare is removed** (tab, header button and the `#compare` address), and with it the per-record “Open its change request”: history is where a past change is read. The header gains **Raise a change request** on an in-service record, calling the same action the settings book's right-click menu calls, so the person who opened the record to change it can — the gap that made this walk-through go wrong. **Files and records** lists documents and evidence only: the revision's own settings text (which the Settings tab's file link and the Text as filed tab already show) and the migrated `ConfigurationFileRevision` detail row (which the Record tab shows) are gone, leaving the rationale and the change's evidence. **Verified** in Chrome on DEV, 1175 A-PROT (Grand Falls): History lists rev 2 Active *11/21/2023 to now* and rev 1 Archived *7/16/2002 to 11/21/2023*, both against their legacy change orders (CR 9607122, CR 6379), requested by Andrew Schorn, calculated and verified dates from the legacy records, rev 2 holding A4811.docx; the header offers Raise a change request, History, Documentation, Close and no Compare or Open its change request; Files and records shows one row, the legacy rationale. **Raised, not built**: the change-order page still prints a migrated request's import timestamps as Requested and Started and the capture date as Completed, and Devices 0 where there is no settings package — the History tab marks such a change legacy, but that page is not yet honest about it; the comparison the owner does want — the relay's online settings against the in-service record — is a later phase and is written into `docs/OPEN-QUESTIONS.md` (the schema already takes a captured file: `File_Write @IsCapturedFromDevice`, `ConfigurationFile.CaptureKind`). | Chrome on DEV | #220 |
| 221 | **The application speaks as a protection and control person speaks.** The owner, 2026-09-22: “the text wording in much of this document seem very convoluted and complex, I would like your wording to be more in line with what a protection and control human would understand and write. This goes for the wording in the entire application.” **The rule** is `docs/design/UI-WORDING.md`, named in CLAUDE.md: every word a user reads is what a P&C engineer would say to a technician beside them — short sentences, the trade's words (relay, scheme, station, panel, position, in service, outstanding, change request, CT, PT, zone, pickup, mask), an empty state that says what is missing and what to do, a refusal that says what the person cannot do. Never on a screen: a decision number, “the owner, 2026-09-…” or a date as a reason, a view, procedure, permission or definition-kind name, a tool path, “the platform …” as the subject of a sentence, “audited as your change”, or a “this phase / later phase” aside. A manual's or a standard's own words stay verbatim with their citation (the settings descriptions, the Relay Word bit meanings, the mask cautions, the PRC-023 and NPCC text, the rationale statements); every decision number that left a screen went into the code comment on its line, so nothing lost its trace. **Measured before and after**: an inventory over 30 screens and 19 screen definitions found ~173 of ~640 strings at fault — 30 citing a decision number (every screen definition's description did), 10 quoting the owner or a date, ~84 naming a view, procedure, permission or tool, ~95 explaining the platform's design instead of the work. `tools/check_wording.py` (new) reads the sources and the definitions and lists what breaks the rule: 96 findings at the start, **0 now**. All 19 descriptions were rewritten as one plain sentence each; four screens' column labels made plain (Package → Settings package, Parsed → Settings read, About/Who/Pending → Relay or asset / Raised by / Waiting). ~200 strings were reworded across the screens. Examples: “Then is the basis as it stood when this draft was taken; theirs is the basis now; mine is this draft.” → the three columns named **Started from / Changed to / This draft** with a sentence saying what re-basing does; “Not yet evaluated — the platform evaluates a device within seconds of a change that a rule reads…” → “Not checked yet. A relay is checked within seconds of a change, and every hour. There is nothing to press.”; “Placing a device needs Asset.Modify.” → “You may not place a device here.”; the verified-date buttons “1. Claim / 2. Witness (as the second person) / 3. Check in that date” → “1. Take it on / 2. Witness it — the second person / 3. Record that date”. **One seeded text also reached a screen**: the Analog inputs tab showed “made by the migration rule (#206)” from the notes the instrument-transformer seed writes. The seed now writes those notes in plain words and, as a counted rule, brings the rows made before the correction into line, so a re-run and the cutover end in the same place. **Verified**: the wording check at zero; `tsc` and the web build clean; the screen definitions reseeded and deployed; Chrome on DEV over the settings book, a record's ten tabs, the requests, locations, primary assets, instrument transformers, schemes, device templates, standards, obligation rules, evaluation and reference data — no decision number, owner quote or permission code on any of them. API smoke 363 PASS, schema smoke PASS. **A finding raised on the way**: a Node-scoped reader asking `asset/vPlacement` for one asset (`AssetEntityId=…`) times out on the server — the read-scope plan of that view, the #172 class, now that the estate carries 7 880 placements; the unfiltered, paged read is unaffected and the smoke's scope check pages instead. The view's plan is a separate increment. | API smoke 363 · schema smoke PASS | #221 |
| 223 | **A change request offers the few actions that fit what the person is standing on, each saying what it is for; the fixtures a smoke run leaves are withdrawn; the Rationale tab appears only where there is a rationale to read or to write.** The owner, 2026-09-22, looking at the Action type list on a relay: “there are many Action types for change requests which have little explanation, how is the user supposed to know which one to select”; and of the Rationale tab on a legacy record: “when viewing a legacy import (that is when there is no rational created yet), the rationale tab should really not be shown since the wording is showing formula's etc and has little real meaning”. **What was wrong**: the list held 215 work types, **207 of them this smoke's leavings** (keys stamped `W4_<run>_SETTINGS_CHANGE`), each shown as `KEY — Name` with no word on what it is for; and the Rationale tab offered an empty form of formulas on a record that has no rationale and cannot take one. **The paragraph**: a smoke run withdraws every definition it made, and anything an earlier run left, at cleanup (`config.Definition_SoftDelete`, now callable under `Definition.Archive`) — 313 withdrawn on the first run, the list down to nine; the picker also ignores a run-stamped key, so a leftover can never reach a person. A command that raises a request names the actions it offers, so a relay's record and the settings book offer the five settings actions and not the transformer tests; the option shows the action's name, not its key, and the chosen one's own description sits under the box. The two actions that had none were given one, and `Standard` was renamed **Other work** — the name told the reader which workflow it runs, which is ours to know. The Rationale tab follows the WORK, not the state: it is offered on a change raised in the platform, and not on one the migration carried over — the owner, seeing it still on an outstanding legacy change: “if it an outstanding work request from the legacy system, why is the Rationale tab still being displayed”. A legacy change was designed elsewhere and its Word rationale is on Files and records; the tab is where the rationale is written and read here. The marker is the work request's own `MigrationRunId`, not a guess from its title. **A correction made the same day**: #219's rationale demonstration had been applied to the wrong record — revision 3 of the SEL-221F [0798], 1111 A-PROT at Grand Falls, whose change request is a Delete Order, using the line L2103 (Bathurst to Eel River HVDC), which is not its line. The owner asked for it back: the 26 settings whose values it changed were restored to what the migration filed, each from its own audit row, and the rationale it made was withdrawn with its files, all through the platform's procedures; the record's own legacy rationale M0798_9617635.doc stands. Recorded in `DEV-ONLY-MUTATIONS.json` as not-a-rule. **Verified** in Chrome on DEV: the picker on 0523 A-PROT (Bathurst) offers Settings change, New setting, Settings retired, Settings verified and Relay settings change (four steps), with “A change to the settings of one or more devices” under it; the Rationale tab is absent on 0058 A-PROT rev 5 at Grand Falls (an outstanding change from the old system) and on a legacy in-service record, and present on a change raised here. API smoke 364 PASS (the archived count now measures the estate without the run's fixtures, as the active count has since #215). | API smoke 364 | #223 |
| 222 | **A scoped read computes its readable set in a statement of its own, before the view is touched — and that statement carries no hint.** `security.fReadableSubjects` is an inline function, so inside one statement the optimizer is free to push the read's own filter into its body, and when the filter is on the very column the scope joins on, it does. Found on DEV 2026-09-22 (the finding raised at the end of #221): `asset/vPlacement?AssetEntityId=…` and `asset/vAsset?EntityId=…` for a user with one NodeSubtree grant both answered **HTTP 500 “Execution Timeout Expired” at 30.3 s and 30.1 s**, while the same reads unfiltered returned a page in 0.35 s. **The actual plan names the operator.** The asset id propagated into the function to `asset.Placement` inside `scope_assets`, whose estimate fell to one row; with that branch believed to cost nothing the grant's own join (`sn.EntityId = r.ScopeNodeEntityId`) was deferred behind a hash join, so the subtree `CROSS APPLY` ran for **every node in the estate** — the Filter carrying `below.Path LIKE sn.Path + …` executed 10 409 times over 108 347 281 rows drawn from an eager index spool of `location.Node`: **87.28 s of the query's 87.49 s**. This is #172's failure one level up: #172 made each branch seekable, but a branch is only seekable in the plan the function is actually given, and the caller's filter changes that plan. **What does not work, measured, not assumed**: `OPTION (FORCE ORDER)` 83.2 s, `OPTION (OPTIMIZE FOR UNKNOWN)` 87.0 s, `OPTION (HASH JOIN)` refused by the optimizer, and a covering `location.Node ([Path]) INCLUDE ([EntityId])` filtered index — the obvious index fix — **83.6 s, no help at all**, because the damage is the join order inside the function, not a missing seek. **The fix is in `SqlSession.QueryViewAsync`**: the readable set is inserted into a table variable keyed on its one column in a statement of its own, and the view is then joined to that table (the `Any` family's `IN` reads the same table). **The second half of the fix is that the INSERT carries no hint.** Almost all of `fReadableSubjects` is compilation, not execution — **387 ms compiling against 7 ms running** for the administrator's 12 579 work requests (actual plan) — so the first attempt, which put `OPTION (RECOMPILE)` on the INSERT as the read already carried, paid that compile twice and took an administrator's request-status screen from 0.39 s to 0.68 s, **failing the smoke's NFR-2 one-second assertion**. With the INSERT's plan cached it is 0.26 s, better than before the change. #147's warning (a plan cached for one grant's readable set serving another) is answered by the shape rather than by a hint: the read is over the table variable and still recompiles, and the INSERT's plan was run across grants both ways — Global plan then subtree read 0.10 s, subtree plan then Global read 0.06 s. **Measured on DEV, ~7 900 placements, warm, over HTTP**: `vPlacement?AssetEntityId=` 500 at 30.3 s → **200 in 0.08 s**, `vAsset?EntityId=` 500 at 30.1 s → **200 in 0.06 s**; and nothing an administrator reads is slower — request status 0.41–0.56 s → 0.26–0.29 s, the settings records of one request 0.78–0.79 s → 0.67–0.70 s, the whole Active grid and the node tree unchanged. In SQL the API's own statement for the failing read went **95.0 s → 0.30 s** (0.05–0.10 s once the INSERT's plan is cached), and the `Any` family, which unions all five sets, gained as much: `asset.vClassification` 3.57 → 2.14 s, `asset.vOwnershipLink` **274.7 s → 3.9 s**. **The set is unchanged, counted both ways on the same grant**: assets 542, asset rows 507, nodes 1 316, records 2 060, classifications 289 — identical old and new; an asset outside the grant still answers 403. **The rule this sets**, extending #172: a read scope is not only *written* as seekable branches, it is *evaluated where the read's own predicates cannot reach it* — and a `RECOMPILE` on a statement that inlines a large security function is not free, it is the dominant cost. It is not one view's defect: 157 of the catalogue's 441 views are scoped, 50 of them on the Asset family, and each would have failed the same way the first time someone asked it for one relay. | `src/PnC.Api/Data/SqlSession.cs`; the estimated and actual execution plans and every timing above, taken on `PnCPlatform_V2_DEV` 2026-09-22; IDENTITY.md §5 unchanged in meaning; agrees with #172 (seekable branches) and #169 (the plan is taken, not assumed) | W8 |
| 122 | **The in-browser gate is observed by the session in Chrome and kept as a screenshot** (`docs/workflow/evidence/`); the smoke asserts what HTTP can — the files served under the CSP with no inline script or style, the editor files in the shell list, the dry run's answers and their paths, the read-back as text, the refusal, the unchanged version count | No browser automation is part of the smoke (W1's browser claim was an observation too); a screenshot a later reader can see is the evidence the workflow's "observed, not asserted" rule asks for | W5 |

---

## Superseded

| # | Superseded by | What changed |
|---|---|---|
| 50 (in part) | 61 | The fork on device technology stands; the electromechanical branch captures a **text settings file** parsed by a per-model template, not fixed typed fields |
| 112 (in part) | 121 | The one-step placeholder v1 of `DRAWING_REVISION` is retired by v2, authored in the tool; the seed loads v1 only on a database with no Effective `DRAWING_REVISION` at all |
| 174 (in part) | 177 | The CIP impact rating is the **building's**, not the yard's; the yard ruling and the terminal-names-a-yard increment it implied are withdrawn. The rest of #174 (a person builds the tree; equipment sits directly in a yard, no bays) stands |

None yet.
