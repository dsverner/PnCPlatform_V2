-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [process].[ProcedureStep_Update]
    @RowId UNIQUEIDENTIFIER,
    @DefinitionVersionRowId UNIQUEIDENTIFIER,
    @StepId NVARCHAR(64),
    @BlockPath NVARCHAR(400),
    @Ordinal INT,
    @Title NVARCHAR(200),
    @RoleAlias NVARCHAR(40),
    @RoleCode NVARCHAR(40),
    @RecordKindCode NVARCHAR(40),
    @SignoffAction NVARCHAR(100) = NULL,
    @RequiresWitness BIT = 0,
    @HasPrecondition BIT = 0,
    @HasDue BIT = 0,
    @AllowsDeviation BIT = 0,
    @AdvancesWorkflowKey NVARCHAR(100) = NULL,
    @AdvancesTransition NVARCHAR(100) = NULL,
    @ProducesName NVARCHAR(40) = NULL,
    @ProducesKind NVARCHAR(40) = NULL,
    @ActorId UNIQUEIDENTIFIER = NULL
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;
    DECLARE @now DATETIMEOFFSET(7) = SYSDATETIMEOFFSET();
    IF @ActorId IS NULL EXEC [personnel].[ResolveActor] @ActorId = @ActorId OUTPUT;
    UPDATE [process].[ProcedureStep] SET [DefinitionVersionRowId] = @DefinitionVersionRowId, [StepId] = @StepId, [BlockPath] = @BlockPath, [Ordinal] = @Ordinal, [Title] = @Title, [RoleAlias] = @RoleAlias, [RoleCode] = @RoleCode, [RecordKindCode] = @RecordKindCode, [SignoffAction] = @SignoffAction, [RequiresWitness] = @RequiresWitness, [HasPrecondition] = @HasPrecondition, [HasDue] = @HasDue, [AllowsDeviation] = @AllowsDeviation, [AdvancesWorkflowKey] = @AdvancesWorkflowKey, [AdvancesTransition] = @AdvancesTransition, [ProducesName] = @ProducesName, [ProducesKind] = @ProducesKind, [ModifiedBy] = @ActorId, [ModifiedAt] = @now
    WHERE [RowId] = @RowId AND [IsDeleted] = 0;
    IF @@ROWCOUNT = 0 THROW 50104, N'process.ProcedureStep: no live row with that key.', 1;
END;
