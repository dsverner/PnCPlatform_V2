-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [process].[InstanceVersionSet_Update]
    @RowId UNIQUEIDENTIFIER,
    @ProcedureInstanceEntityId UNIQUEIDENTIFIER,
    @CalleeKey NVARCHAR(100),
    @DefinitionVersionRowId UNIQUEIDENTIFIER,
    @ActorId UNIQUEIDENTIFIER = NULL
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;
    DECLARE @now DATETIMEOFFSET(7) = SYSDATETIMEOFFSET();
    IF @ActorId IS NULL EXEC [personnel].[ResolveActor] @ActorId = @ActorId OUTPUT;
    UPDATE [process].[InstanceVersionSet] SET [ProcedureInstanceEntityId] = @ProcedureInstanceEntityId, [CalleeKey] = @CalleeKey, [DefinitionVersionRowId] = @DefinitionVersionRowId, [ModifiedBy] = @ActorId, [ModifiedAt] = @now
    WHERE [RowId] = @RowId AND [IsDeleted] = 0;
    IF @@ROWCOUNT = 0 THROW 50104, N'process.InstanceVersionSet: no live row with that key.', 1;
END;
