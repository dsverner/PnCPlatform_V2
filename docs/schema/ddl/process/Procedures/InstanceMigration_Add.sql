-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [process].[InstanceMigration_Add]
    @ProcedureInstanceEntityId UNIQUEIDENTIFIER,
    @FromDefinitionVersionRowId UNIQUEIDENTIFIER,
    @ToDefinitionVersionRowId UNIQUEIDENTIFIER,
    @Decision NVARCHAR(20),
    @Reason NVARCHAR(400),
    @DecidedByActorId UNIQUEIDENTIFIER,
    @DecidedAt DATETIMEOFFSET(7),
    @StepMapping NVARCHAR(MAX) = NULL,
    @ActorId UNIQUEIDENTIFIER = NULL,
    @MigrationRunId UNIQUEIDENTIFIER = NULL,
    @EntityId UNIQUEIDENTIFIER = NULL OUTPUT,
    @RowId UNIQUEIDENTIFIER = NULL OUTPUT
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;
    DECLARE @now DATETIMEOFFSET(7) = SYSDATETIMEOFFSET();
    IF @ActorId IS NULL EXEC [personnel].[ResolveActor] @ActorId = @ActorId OUTPUT;
    BEGIN TRANSACTION;
    SET @EntityId = ISNULL(@EntityId, NEWID());
    INSERT [process].[InstanceMigrationRegistry] ([EntityId]) VALUES (@EntityId);
    DECLARE @out TABLE ([RowId] UNIQUEIDENTIFIER);
    INSERT [process].[InstanceMigration] ([EntityId], [CreatedBy], [CreatedAt], [ModifiedBy], [ModifiedAt], [MigrationRunId], [ProcedureInstanceEntityId], [FromDefinitionVersionRowId], [ToDefinitionVersionRowId], [Decision], [Reason], [DecidedByActorId], [DecidedAt], [StepMapping])
    OUTPUT inserted.[RowId] INTO @out
    VALUES (@EntityId, @ActorId, @now, @ActorId, @now, @MigrationRunId, @ProcedureInstanceEntityId, @FromDefinitionVersionRowId, @ToDefinitionVersionRowId, @Decision, @Reason, @DecidedByActorId, @DecidedAt, @StepMapping);
    SELECT @RowId = [RowId] FROM @out;
    COMMIT TRANSACTION;
END;
