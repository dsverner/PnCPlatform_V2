-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [process].[BlockInstance_Add]
    @ProcedureInstanceEntityId UNIQUEIDENTIFIER,
    @ParentBlockInstanceEntityId UNIQUEIDENTIFIER = NULL,
    @BlockPath NVARCHAR(400),
    @BlockKind NVARCHAR(20),
    @IterationKey NVARCHAR(100) = NULL,
    @Pass INT = 1,
    @MemberSubjectKind NVARCHAR(40) = NULL,
    @MemberSubjectEntityId UNIQUEIDENTIFIER = NULL,
    @State NVARCHAR(40),
    @Outcome NVARCHAR(40) = NULL,
    @StartedAt DATETIMEOFFSET(7) = NULL,
    @CompletedAt DATETIMEOFFSET(7) = NULL,
    @ActorId UNIQUEIDENTIFIER = NULL,
    @MigrationRunId UNIQUEIDENTIFIER = NULL,
    @EntityId UNIQUEIDENTIFIER = NULL OUTPUT,
    @RowId UNIQUEIDENTIFIER = NULL OUTPUT
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;
    DECLARE @now DATETIMEOFFSET(7) = SYSDATETIMEOFFSET();
    IF @ActorId IS NULL EXEC [personnel].[ResolveActor] @ActorId = @ActorId OUTPUT;
    IF @MemberSubjectEntityId IS NOT NULL AND [meta].[fEntityExists](@MemberSubjectKind, @MemberSubjectEntityId) = 0
        THROW 50101, N'process.BlockInstance: no MemberSubject entity of kind @MemberSubjectKind with that id.', 1;
    BEGIN TRANSACTION;
    SET @EntityId = ISNULL(@EntityId, NEWID());
    INSERT [process].[BlockInstanceRegistry] ([EntityId]) VALUES (@EntityId);
    DECLARE @out TABLE ([RowId] UNIQUEIDENTIFIER);
    INSERT [process].[BlockInstance] ([EntityId], [CreatedBy], [CreatedAt], [ModifiedBy], [ModifiedAt], [MigrationRunId], [ProcedureInstanceEntityId], [ParentBlockInstanceEntityId], [BlockPath], [BlockKind], [IterationKey], [Pass], [MemberSubjectKind], [MemberSubjectEntityId], [State], [Outcome], [StartedAt], [CompletedAt])
    OUTPUT inserted.[RowId] INTO @out
    VALUES (@EntityId, @ActorId, @now, @ActorId, @now, @MigrationRunId, @ProcedureInstanceEntityId, @ParentBlockInstanceEntityId, @BlockPath, @BlockKind, @IterationKey, @Pass, @MemberSubjectKind, @MemberSubjectEntityId, @State, @Outcome, @StartedAt, @CompletedAt);
    SELECT @RowId = [RowId] FROM @out;
    COMMIT TRANSACTION;
END;
