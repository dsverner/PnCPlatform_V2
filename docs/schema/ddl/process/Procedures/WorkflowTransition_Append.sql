-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [process].[WorkflowTransition_Append]
    @WorkflowInstanceEntityId UNIQUEIDENTIFIER,
    @OccurredAt DATETIMEOFFSET(7),
    @FromState NVARCHAR(40),
    @ToState NVARCHAR(40),
    @TransitionName NVARCHAR(100),
    @ActorId UNIQUEIDENTIFIER,
    @Reason NVARCHAR(400) = NULL,
    @FiredByStepInstanceEntityId UNIQUEIDENTIFIER = NULL,
    @GuardEvaluation NVARCHAR(MAX) = NULL,
    @ActionLogId BIGINT = NULL,
    @MigrationRunId UNIQUEIDENTIFIER = NULL,
    @TransitionId BIGINT = NULL OUTPUT
AS
BEGIN
    SET NOCOUNT ON;
    IF @ActorId IS NULL EXEC [personnel].[ResolveActor] @ActorId = @ActorId OUTPUT;
    INSERT [process].[WorkflowTransition] ([WorkflowInstanceEntityId], [OccurredAt], [FromState], [ToState], [TransitionName], [ActorId], [Reason], [FiredByStepInstanceEntityId], [GuardEvaluation], [ActionLogId], [MigrationRunId])
    VALUES (@WorkflowInstanceEntityId, @OccurredAt, @FromState, @ToState, @TransitionName, @ActorId, @Reason, @FiredByStepInstanceEntityId, @GuardEvaluation, @ActionLogId, @MigrationRunId);
    SET @TransitionId = SCOPE_IDENTITY();
END;
