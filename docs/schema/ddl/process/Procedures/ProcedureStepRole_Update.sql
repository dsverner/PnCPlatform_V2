-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [process].[ProcedureStepRole_Update]
    @RowId UNIQUEIDENTIFIER,
    @ProcedureStepRowId UNIQUEIDENTIFIER,
    @RoleCode NVARCHAR(40),
    @RequiresAst NVARCHAR(MAX) = NULL,
    @ActorId UNIQUEIDENTIFIER = NULL
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;
    DECLARE @now DATETIMEOFFSET(7) = SYSDATETIMEOFFSET();
    IF @ActorId IS NULL EXEC [personnel].[ResolveActor] @ActorId = @ActorId OUTPUT;
    UPDATE [process].[ProcedureStepRole] SET [ProcedureStepRowId] = @ProcedureStepRowId, [RoleCode] = @RoleCode, [RequiresAst] = @RequiresAst, [ModifiedBy] = @ActorId, [ModifiedAt] = @now
    WHERE [RowId] = @RowId AND [IsDeleted] = 0;
    IF @@ROWCOUNT = 0 THROW 50104, N'process.ProcedureStepRole: no live row with that key.', 1;
END;
