-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [process].[InstanceMigration_Update]
    @RowId UNIQUEIDENTIFIER,
    @ProcedureInstanceEntityId UNIQUEIDENTIFIER,
    @FromDefinitionVersionRowId UNIQUEIDENTIFIER,
    @ToDefinitionVersionRowId UNIQUEIDENTIFIER,
    @Decision NVARCHAR(20),
    @Reason NVARCHAR(400),
    @DecidedByActorId UNIQUEIDENTIFIER,
    @DecidedAt DATETIMEOFFSET(7),
    @StepMapping NVARCHAR(MAX) = NULL,
    @ActorId UNIQUEIDENTIFIER = NULL
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;
    DECLARE @now DATETIMEOFFSET(7) = SYSDATETIMEOFFSET();
    IF @ActorId IS NULL EXEC [personnel].[ResolveActor] @ActorId = @ActorId OUTPUT;
    UPDATE [process].[InstanceMigration] SET [ProcedureInstanceEntityId] = @ProcedureInstanceEntityId, [FromDefinitionVersionRowId] = @FromDefinitionVersionRowId, [ToDefinitionVersionRowId] = @ToDefinitionVersionRowId, [Decision] = @Decision, [Reason] = @Reason, [DecidedByActorId] = @DecidedByActorId, [DecidedAt] = @DecidedAt, [StepMapping] = @StepMapping, [ModifiedBy] = @ActorId, [ModifiedAt] = @now
    WHERE [RowId] = @RowId AND [IsDeleted] = 0;
    IF @@ROWCOUNT = 0 THROW 50104, N'process.InstanceMigration: no live row with that key.', 1;
END;
