-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [compliance].[EvidencePackageManifest_Append]
    @PackageEntityId UNIQUEIDENTIFIER,
    @ItemKind NVARCHAR(40),
    @ItemRowId UNIQUEIDENTIFIER,
    @FileSha256 BINARY(32) = NULL,
    @ManifestId BIGINT = NULL OUTPUT
AS
BEGIN
    SET NOCOUNT ON;
    INSERT [compliance].[EvidencePackageManifest] ([PackageEntityId], [ItemKind], [ItemRowId], [FileSha256])
    VALUES (@PackageEntityId, @ItemKind, @ItemRowId, @FileSha256);
    SET @ManifestId = SCOPE_IDENTITY();
END;
