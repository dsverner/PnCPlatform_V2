-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [compliance].[EvaluationRequest_Append]
    @SubjectKind NVARCHAR(20),
    @SubjectEntityId UNIQUEIDENTIFIER = NULL,
    @Reason NVARCHAR(200),
    @RequestedAt DATETIMEOFFSET(7),
    @ActorId UNIQUEIDENTIFIER,
    @ProcessedAt DATETIMEOFFSET(7) = NULL,
    @RunId UNIQUEIDENTIFIER = NULL,
    @DevicesFound INT = NULL,
    @RequestId UNIQUEIDENTIFIER = NULL OUTPUT
AS
BEGIN
    SET NOCOUNT ON;
    IF @ActorId IS NULL EXEC [personnel].[ResolveActor] @ActorId = @ActorId OUTPUT;
    IF @SubjectEntityId IS NOT NULL AND [meta].[fEntityExists](@SubjectKind, @SubjectEntityId) = 0
        THROW 50101, N'compliance.EvaluationRequest: no Subject entity of kind @SubjectKind with that id.', 1;
    SET @RequestId = ISNULL(@RequestId, NEWID());
    INSERT [compliance].[EvaluationRequest] ([RequestId], [SubjectKind], [SubjectEntityId], [Reason], [RequestedAt], [ActorId], [ProcessedAt], [RunId], [DevicesFound])
    VALUES (@RequestId, @SubjectKind, @SubjectEntityId, @Reason, @RequestedAt, @ActorId, @ProcessedAt, @RunId, @DevicesFound);
END;
