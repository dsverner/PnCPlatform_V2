-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [compliance].[EvidencePackage_Add]
    @AuditRequestEntityId UNIQUEIDENTIFIER = NULL,
    @SubjectKind NVARCHAR(40),
    @SubjectEntityId UNIQUEIDENTIFIER = NULL,
    @StandardVersionRowId UNIQUEIDENTIFIER,
    @PeriodStartAt DATETIMEOFFSET(7),
    @PeriodEndAt DATETIMEOFFSET(7),
    @ValidAsOf DATETIMEOFFSET(7),
    @BelievedAsOf DATETIMEOFFSET(7),
    @PreparedByActorId UNIQUEIDENTIFIER,
    @PreparedAt DATETIMEOFFSET(7),
    @ReviewedByActorId UNIQUEIDENTIFIER = NULL,
    @ReviewedAt DATETIMEOFFSET(7) = NULL,
    @ApprovedByActorId UNIQUEIDENTIFIER = NULL,
    @ApprovedAt DATETIMEOFFSET(7) = NULL,
    @SubmittedAt DATETIMEOFFSET(7) = NULL,
    @SubmittedToEntityEntityId UNIQUEIDENTIFIER = NULL,
    @PackageDocumentEntityId UNIQUEIDENTIFIER = NULL,
    @PackageHash BINARY(32) = NULL,
    @ActorId UNIQUEIDENTIFIER = NULL,
    @MigrationRunId UNIQUEIDENTIFIER = NULL,
    @EntityId UNIQUEIDENTIFIER = NULL OUTPUT,
    @RowId UNIQUEIDENTIFIER = NULL OUTPUT
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;
    DECLARE @now DATETIMEOFFSET(7) = SYSDATETIMEOFFSET();
    IF @ActorId IS NULL EXEC [personnel].[ResolveActor] @ActorId = @ActorId OUTPUT;
    IF @SubjectEntityId IS NOT NULL AND [meta].[fEntityExists](@SubjectKind, @SubjectEntityId) = 0
        THROW 50101, N'compliance.EvidencePackage: no Subject entity of kind @SubjectKind with that id.', 1;
    BEGIN TRANSACTION;
    SET @EntityId = ISNULL(@EntityId, NEWID());
    INSERT [compliance].[EvidencePackageRegistry] ([EntityId]) VALUES (@EntityId);
    DECLARE @out TABLE ([RowId] UNIQUEIDENTIFIER);
    INSERT [compliance].[EvidencePackage] ([EntityId], [CreatedBy], [CreatedAt], [ModifiedBy], [ModifiedAt], [MigrationRunId], [AuditRequestEntityId], [SubjectKind], [SubjectEntityId], [StandardVersionRowId], [PeriodStartAt], [PeriodEndAt], [ValidAsOf], [BelievedAsOf], [PreparedByActorId], [PreparedAt], [ReviewedByActorId], [ReviewedAt], [ApprovedByActorId], [ApprovedAt], [SubmittedAt], [SubmittedToEntityEntityId], [PackageDocumentEntityId], [PackageHash])
    OUTPUT inserted.[RowId] INTO @out
    VALUES (@EntityId, @ActorId, @now, @ActorId, @now, @MigrationRunId, @AuditRequestEntityId, @SubjectKind, @SubjectEntityId, @StandardVersionRowId, @PeriodStartAt, @PeriodEndAt, @ValidAsOf, @BelievedAsOf, @PreparedByActorId, @PreparedAt, @ReviewedByActorId, @ReviewedAt, @ApprovedByActorId, @ApprovedAt, @SubmittedAt, @SubmittedToEntityEntityId, @PackageDocumentEntityId, @PackageHash);
    SELECT @RowId = [RowId] FROM @out;
    COMMIT TRANSACTION;
END;
