-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [compliance].[SubjectVerdict_Upsert]
    @SubjectKind NVARCHAR(40),
    @SubjectEntityId UNIQUEIDENTIFIER,
    @VerdictKind NVARCHAR(20),
    @DefinitionEntityId UNIQUEIDENTIFIER,
    @VersionRowId UNIQUEIDENTIFIER,
    @Result NVARCHAR(60) = NULL,
    @Reason NVARCHAR(400) = NULL,
    @ReadsJson NVARCHAR(MAX) = NULL,
    @Error NVARCHAR(400) = NULL,
    @SinceRunId UNIQUEIDENTIFIER,
    @SinceAt DATETIMEOFFSET(7),
    @ActorId UNIQUEIDENTIFIER = NULL,
    @MigrationRunId UNIQUEIDENTIFIER = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @now DATETIMEOFFSET(7) = SYSDATETIMEOFFSET();
    IF @ActorId IS NULL EXEC [personnel].[ResolveActor] @ActorId = @ActorId OUTPUT;
    IF EXISTS (SELECT 1 FROM [compliance].[SubjectVerdict] t WHERE t.[SubjectKind] = @SubjectKind AND t.[SubjectEntityId] = @SubjectEntityId AND t.[VerdictKind] = @VerdictKind AND t.[DefinitionEntityId] = @DefinitionEntityId)
        UPDATE t SET [VersionRowId] = @VersionRowId, [Result] = @Result, [Reason] = @Reason, [ReadsJson] = @ReadsJson, [Error] = @Error, [SinceRunId] = @SinceRunId, [SinceAt] = @SinceAt, [IsActive] = 1, [ModifiedBy] = @ActorId, [ModifiedAt] = @now FROM [compliance].[SubjectVerdict] t WHERE t.[SubjectKind] = @SubjectKind AND t.[SubjectEntityId] = @SubjectEntityId AND t.[VerdictKind] = @VerdictKind AND t.[DefinitionEntityId] = @DefinitionEntityId;
    ELSE
        INSERT [compliance].[SubjectVerdict] ([SubjectKind], [SubjectEntityId], [VerdictKind], [DefinitionEntityId], [VersionRowId], [Result], [Reason], [ReadsJson], [Error], [SinceRunId], [SinceAt], [CreatedBy], [CreatedAt], [ModifiedBy], [ModifiedAt], [MigrationRunId]) VALUES (@SubjectKind, @SubjectEntityId, @VerdictKind, @DefinitionEntityId, @VersionRowId, @Result, @Reason, @ReadsJson, @Error, @SinceRunId, @SinceAt, @ActorId, @now, @ActorId, @now, @MigrationRunId);
END;
