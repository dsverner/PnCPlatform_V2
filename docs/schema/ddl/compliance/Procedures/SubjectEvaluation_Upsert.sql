-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [compliance].[SubjectEvaluation_Upsert]
    @SubjectKind NVARCHAR(40),
    @SubjectEntityId UNIQUEIDENTIFIER,
    @LastRunId UNIQUEIDENTIFIER,
    @LastEvaluatedAt DATETIMEOFFSET(7),
    @LastTrigger NVARCHAR(20),
    @RulesEvaluated INT,
    @ActorId UNIQUEIDENTIFIER = NULL,
    @MigrationRunId UNIQUEIDENTIFIER = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @now DATETIMEOFFSET(7) = SYSDATETIMEOFFSET();
    IF @ActorId IS NULL EXEC [personnel].[ResolveActor] @ActorId = @ActorId OUTPUT;
    IF EXISTS (SELECT 1 FROM [compliance].[SubjectEvaluation] t WHERE t.[SubjectKind] = @SubjectKind AND t.[SubjectEntityId] = @SubjectEntityId)
        UPDATE t SET [LastRunId] = @LastRunId, [LastEvaluatedAt] = @LastEvaluatedAt, [LastTrigger] = @LastTrigger, [RulesEvaluated] = @RulesEvaluated, [IsActive] = 1, [ModifiedBy] = @ActorId, [ModifiedAt] = @now FROM [compliance].[SubjectEvaluation] t WHERE t.[SubjectKind] = @SubjectKind AND t.[SubjectEntityId] = @SubjectEntityId;
    ELSE
        INSERT [compliance].[SubjectEvaluation] ([SubjectKind], [SubjectEntityId], [LastRunId], [LastEvaluatedAt], [LastTrigger], [RulesEvaluated], [CreatedBy], [CreatedAt], [ModifiedBy], [ModifiedAt], [MigrationRunId]) VALUES (@SubjectKind, @SubjectEntityId, @LastRunId, @LastEvaluatedAt, @LastTrigger, @RulesEvaluated, @ActorId, @now, @ActorId, @now, @MigrationRunId);
END;
