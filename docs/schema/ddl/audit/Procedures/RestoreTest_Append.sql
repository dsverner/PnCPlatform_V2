-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [audit].[RestoreTest_Append]
    @BackupRunId BIGINT,
    @RestoredToServer NVARCHAR(200),
    @StartedAt DATETIMEOFFSET(7),
    @CompletedAt DATETIMEOFFSET(7) = NULL,
    @IntegrityCheckPassed BIT = NULL,
    @RowCountsMatched BIT = NULL,
    @AchievedRtoMinutes INT = NULL,
    @ActorId UNIQUEIDENTIFIER,
    @Outcome NVARCHAR(20),
    @Notes NVARCHAR(MAX) = NULL,
    @RestoreTestId BIGINT = NULL OUTPUT
AS
BEGIN
    SET NOCOUNT ON;
    IF @ActorId IS NULL EXEC [personnel].[ResolveActor] @ActorId = @ActorId OUTPUT;
    INSERT [audit].[RestoreTest] ([BackupRunId], [RestoredToServer], [StartedAt], [CompletedAt], [IntegrityCheckPassed], [RowCountsMatched], [AchievedRtoMinutes], [ActorId], [Outcome], [Notes])
    VALUES (@BackupRunId, @RestoredToServer, @StartedAt, @CompletedAt, @IntegrityCheckPassed, @RowCountsMatched, @AchievedRtoMinutes, @ActorId, @Outcome, @Notes);
    SET @RestoreTestId = SCOPE_IDENTITY();
END;
