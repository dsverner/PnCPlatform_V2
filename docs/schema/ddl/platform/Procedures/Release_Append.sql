-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [platform].[Release_Append]
    @Version NVARCHAR(40),
    @ReleasedAt DATETIMEOFFSET(7),
    @DacpacHash BINARY(32),
    @PackageHash BINARY(32) = NULL,
    @SbomDocumentEntityId UNIQUEIDENTIFIER = NULL,
    @ReleaseDocumentEntityId UNIQUEIDENTIFIER = NULL,
    @RecordedByActorId UNIQUEIDENTIFIER,
    @Notes NVARCHAR(MAX) = NULL,
    @ReleaseId BIGINT = NULL OUTPUT
AS
BEGIN
    SET NOCOUNT ON;
    INSERT [platform].[Release] ([Version], [ReleasedAt], [DacpacHash], [PackageHash], [SbomDocumentEntityId], [ReleaseDocumentEntityId], [RecordedByActorId], [Notes])
    VALUES (@Version, @ReleasedAt, @DacpacHash, @PackageHash, @SbomDocumentEntityId, @ReleaseDocumentEntityId, @RecordedByActorId, @Notes);
    SET @ReleaseId = SCOPE_IDENTITY();
END;
