-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [event].[Event_Append]
    @OccurredAt DATETIMEOFFSET(7),
    @TimeSourceQuality TINYINT,
    @SourceDeviceEntityId UNIQUEIDENTIFIER,
    @SourceKind NVARCHAR(40),
    @Format NVARCHAR(40),
    @SampleRateHz DECIMAL(18,4) = NULL,
    @DurationMs INT = NULL,
    @NominalFrequencyHz DECIMAL(18,4) = NULL,
    @TriggerDescription NVARCHAR(400) = NULL,
    @ProtectionOperationEntityId UNIQUEIDENTIFIER = NULL,
    @RawFileEntityId UNIQUEIDENTIFIER = NULL,
    @IngestedAt DATETIMEOFFSET(7),
    @IngestRunId UNIQUEIDENTIFIER = NULL,
    @EventId UNIQUEIDENTIFIER = NULL OUTPUT
AS
BEGIN
    SET NOCOUNT ON;
    SET @EventId = ISNULL(@EventId, NEWID());
    INSERT [event].[Event] ([EventId], [OccurredAt], [TimeSourceQuality], [SourceDeviceEntityId], [SourceKind], [Format], [SampleRateHz], [DurationMs], [NominalFrequencyHz], [TriggerDescription], [ProtectionOperationEntityId], [RawFileEntityId], [IngestedAt], [IngestRunId])
    VALUES (@EventId, @OccurredAt, @TimeSourceQuality, @SourceDeviceEntityId, @SourceKind, @Format, @SampleRateHz, @DurationMs, @NominalFrequencyHz, @TriggerDescription, @ProtectionOperationEntityId, @RawFileEntityId, @IngestedAt, @IngestRunId);
END;
