-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [event].[SampleBlock_Append]
    @ChannelId BIGINT,
    @BlockSequence INT,
    @SampleCount INT,
    @Encoding NVARCHAR(40),
    @Samples VARBINARY(MAX)
AS
BEGIN
    SET NOCOUNT ON;
    INSERT [event].[SampleBlock] ([ChannelId], [BlockSequence], [SampleCount], [Encoding], [Samples])
    VALUES (@ChannelId, @BlockSequence, @SampleCount, @Encoding, @Samples);
END;
