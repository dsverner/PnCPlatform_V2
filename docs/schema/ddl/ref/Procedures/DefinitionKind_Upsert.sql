-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [ref].[DefinitionKind_Upsert]
    @DefinitionKind NVARCHAR(40),
    @MetaKind NVARCHAR(20),
    @Name NVARCHAR(200),
    @Description NVARCHAR(MAX) = NULL,
    @HasPayloadText BIT,
    @ActorId UNIQUEIDENTIFIER = NULL,
    @MigrationRunId UNIQUEIDENTIFIER = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @now DATETIMEOFFSET(7) = SYSDATETIMEOFFSET();
    IF @ActorId IS NULL EXEC [personnel].[ResolveActor] @ActorId = @ActorId OUTPUT;
    IF EXISTS (SELECT 1 FROM [ref].[DefinitionKind] t WHERE t.[DefinitionKind] = @DefinitionKind)
        UPDATE t SET [MetaKind] = @MetaKind, [Name] = @Name, [Description] = @Description, [HasPayloadText] = @HasPayloadText, [IsActive] = 1, [ModifiedBy] = @ActorId, [ModifiedAt] = @now FROM [ref].[DefinitionKind] t WHERE t.[DefinitionKind] = @DefinitionKind;
    ELSE
        INSERT [ref].[DefinitionKind] ([DefinitionKind], [MetaKind], [Name], [Description], [HasPayloadText], [CreatedBy], [CreatedAt], [ModifiedBy], [ModifiedAt], [MigrationRunId]) VALUES (@DefinitionKind, @MetaKind, @Name, @Description, @HasPayloadText, @ActorId, @now, @ActorId, @now, @MigrationRunId);
END;
