-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [ref].[DrawingStatus_Upsert]
    @DrawingStatusCode NVARCHAR(40),
    @Name NVARCHAR(200),
    @Description NVARCHAR(MAX) = NULL,
    @DisplayOrder INT = 0,
    @ActorId UNIQUEIDENTIFIER = NULL,
    @MigrationRunId UNIQUEIDENTIFIER = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @now DATETIMEOFFSET(7) = SYSDATETIMEOFFSET();
    IF @ActorId IS NULL EXEC [personnel].[ResolveActor] @ActorId = @ActorId OUTPUT;
    IF EXISTS (SELECT 1 FROM [ref].[DrawingStatus] t WHERE t.[DrawingStatusCode] = @DrawingStatusCode)
        UPDATE t SET [Name] = @Name, [Description] = @Description, [DisplayOrder] = @DisplayOrder, [IsActive] = 1, [ModifiedBy] = @ActorId, [ModifiedAt] = @now FROM [ref].[DrawingStatus] t WHERE t.[DrawingStatusCode] = @DrawingStatusCode;
    ELSE
        INSERT [ref].[DrawingStatus] ([DrawingStatusCode], [Name], [Description], [DisplayOrder], [CreatedBy], [CreatedAt], [ModifiedBy], [ModifiedAt], [MigrationRunId]) VALUES (@DrawingStatusCode, @Name, @Description, @DisplayOrder, @ActorId, @now, @ActorId, @now, @MigrationRunId);
END;
