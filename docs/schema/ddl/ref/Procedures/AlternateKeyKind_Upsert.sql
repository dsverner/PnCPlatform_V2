-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [ref].[AlternateKeyKind_Upsert]
    @KeyKindCode NVARCHAR(50),
    @Name NVARCHAR(200),
    @SchemaName SYSNAME,
    @SubjectKindCode NVARCHAR(40),
    @ScopeRequired BIT = 0,
    @ScopeSubjectKindCode NVARCHAR(40) = NULL,
    @Description NVARCHAR(MAX) = NULL,
    @ActorId UNIQUEIDENTIFIER = NULL,
    @MigrationRunId UNIQUEIDENTIFIER = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @now DATETIMEOFFSET(7) = SYSDATETIMEOFFSET();
    IF @ActorId IS NULL EXEC [personnel].[ResolveActor] @ActorId = @ActorId OUTPUT;
    IF EXISTS (SELECT 1 FROM [ref].[AlternateKeyKind] t WHERE t.[KeyKindCode] = @KeyKindCode)
        UPDATE t SET [Name] = @Name, [SchemaName] = @SchemaName, [SubjectKindCode] = @SubjectKindCode, [ScopeRequired] = @ScopeRequired, [ScopeSubjectKindCode] = @ScopeSubjectKindCode, [Description] = @Description, [IsActive] = 1, [ModifiedBy] = @ActorId, [ModifiedAt] = @now FROM [ref].[AlternateKeyKind] t WHERE t.[KeyKindCode] = @KeyKindCode;
    ELSE
        INSERT [ref].[AlternateKeyKind] ([KeyKindCode], [Name], [SchemaName], [SubjectKindCode], [ScopeRequired], [ScopeSubjectKindCode], [Description], [CreatedBy], [CreatedAt], [ModifiedBy], [ModifiedAt], [MigrationRunId]) VALUES (@KeyKindCode, @Name, @SchemaName, @SubjectKindCode, @ScopeRequired, @ScopeSubjectKindCode, @Description, @ActorId, @now, @ActorId, @now, @MigrationRunId);
END;
