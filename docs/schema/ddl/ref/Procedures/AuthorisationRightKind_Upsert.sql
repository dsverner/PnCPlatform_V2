-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [ref].[AuthorisationRightKind_Upsert]
    @RightKindCode NVARCHAR(40),
    @Name NVARCHAR(200),
    @ActorId UNIQUEIDENTIFIER = NULL,
    @MigrationRunId UNIQUEIDENTIFIER = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @now DATETIMEOFFSET(7) = SYSDATETIMEOFFSET();
    IF @ActorId IS NULL EXEC [personnel].[ResolveActor] @ActorId = @ActorId OUTPUT;
    IF EXISTS (SELECT 1 FROM [ref].[AuthorisationRightKind] t WHERE t.[RightKindCode] = @RightKindCode)
        UPDATE t SET [Name] = @Name, [IsActive] = 1, [ModifiedBy] = @ActorId, [ModifiedAt] = @now FROM [ref].[AuthorisationRightKind] t WHERE t.[RightKindCode] = @RightKindCode;
    ELSE
        INSERT [ref].[AuthorisationRightKind] ([RightKindCode], [Name], [CreatedBy], [CreatedAt], [ModifiedBy], [ModifiedAt], [MigrationRunId]) VALUES (@RightKindCode, @Name, @ActorId, @now, @ActorId, @now, @MigrationRunId);
END;
