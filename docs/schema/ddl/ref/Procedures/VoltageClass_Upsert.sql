-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [ref].[VoltageClass_Upsert]
    @VoltageClassCode NVARCHAR(20),
    @NominalKv DECIMAL(10,3),
    @IsTransmission BIT = 0,
    @DisplayOrder INT = 0,
    @ActorId UNIQUEIDENTIFIER = NULL,
    @MigrationRunId UNIQUEIDENTIFIER = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @now DATETIMEOFFSET(7) = SYSDATETIMEOFFSET();
    IF @ActorId IS NULL EXEC [personnel].[ResolveActor] @ActorId = @ActorId OUTPUT;
    IF EXISTS (SELECT 1 FROM [ref].[VoltageClass] t WHERE t.[VoltageClassCode] = @VoltageClassCode)
        UPDATE t SET [NominalKv] = @NominalKv, [IsTransmission] = @IsTransmission, [DisplayOrder] = @DisplayOrder, [IsActive] = 1, [ModifiedBy] = @ActorId, [ModifiedAt] = @now FROM [ref].[VoltageClass] t WHERE t.[VoltageClassCode] = @VoltageClassCode;
    ELSE
        INSERT [ref].[VoltageClass] ([VoltageClassCode], [NominalKv], [IsTransmission], [DisplayOrder], [CreatedBy], [CreatedAt], [ModifiedBy], [ModifiedAt], [MigrationRunId]) VALUES (@VoltageClassCode, @NominalKv, @IsTransmission, @DisplayOrder, @ActorId, @now, @ActorId, @now, @MigrationRunId);
END;
