-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [document].[Rationale_Update]
    @RevisionRowId UNIQUEIDENTIFIER,
    @SchemeTypeTemplateDefinitionVersionRowId UNIQUEIDENTIFIER = NULL,
    @DeviceTemplateDefinitionVersionRowId UNIQUEIDENTIFIER = NULL,
    @ActorId UNIQUEIDENTIFIER = NULL
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;
    DECLARE @now DATETIMEOFFSET(7) = SYSDATETIMEOFFSET();
    IF @ActorId IS NULL EXEC [personnel].[ResolveActor] @ActorId = @ActorId OUTPUT;
    UPDATE [document].[Rationale] SET [SchemeTypeTemplateDefinitionVersionRowId] = @SchemeTypeTemplateDefinitionVersionRowId, [DeviceTemplateDefinitionVersionRowId] = @DeviceTemplateDefinitionVersionRowId, [ModifiedBy] = @ActorId, [ModifiedAt] = @now
    WHERE [RevisionRowId] = @RevisionRowId AND [IsDeleted] = 0;
    IF @@ROWCOUNT = 0 THROW 50104, N'document.Rationale: no live row with that key.', 1;
END;
