-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [document].[Drawing_Update]
    @RevisionRowId UNIQUEIDENTIFIER,
    @SheetNumber INT = NULL,
    @SheetCount INT = NULL,
    @TitleLine1 NVARCHAR(200) = NULL,
    @TitleLine2 NVARCHAR(200) = NULL,
    @AssetTitle NVARCHAR(200) = NULL,
    @JobNumber NVARCHAR(100) = NULL,
    @WbsNumber NVARCHAR(100) = NULL,
    @DrawnByActorId UNIQUEIDENTIFIER = NULL,
    @CheckedByActorId UNIQUEIDENTIFIER = NULL,
    @DesignedByActorId UNIQUEIDENTIFIER = NULL,
    @DrawingStatusCode NVARCHAR(40) = NULL,
    @Orientation NVARCHAR(20) = NULL,
    @PaperSize NVARCHAR(20) = NULL,
    @Scale NVARCHAR(40) = NULL,
    @DrawingKind NVARCHAR(20),
    @ActorId UNIQUEIDENTIFIER = NULL
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;
    DECLARE @now DATETIMEOFFSET(7) = SYSDATETIMEOFFSET();
    IF @ActorId IS NULL EXEC [personnel].[ResolveActor] @ActorId = @ActorId OUTPUT;
    UPDATE [document].[Drawing] SET [SheetNumber] = @SheetNumber, [SheetCount] = @SheetCount, [TitleLine1] = @TitleLine1, [TitleLine2] = @TitleLine2, [AssetTitle] = @AssetTitle, [JobNumber] = @JobNumber, [WbsNumber] = @WbsNumber, [DrawnByActorId] = @DrawnByActorId, [CheckedByActorId] = @CheckedByActorId, [DesignedByActorId] = @DesignedByActorId, [DrawingStatusCode] = @DrawingStatusCode, [Orientation] = @Orientation, [PaperSize] = @PaperSize, [Scale] = @Scale, [DrawingKind] = @DrawingKind, [ModifiedBy] = @ActorId, [ModifiedAt] = @now
    WHERE [RevisionRowId] = @RevisionRowId AND [IsDeleted] = 0;
    IF @@ROWCOUNT = 0 THROW 50104, N'document.Drawing: no live row with that key.', 1;
END;
