-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [document].[Study_Add]
    @RevisionRowId UNIQUEIDENTIFIER,
    @StudyKind NVARCHAR(20),
    @Software NVARCHAR(100) = NULL,
    @SoftwareVersion NVARCHAR(40) = NULL,
    @SystemModelAt DATETIMEOFFSET(7) = NULL,
    @NetworkModelCaseEntityId UNIQUEIDENTIFIER = NULL,
    @FaultLevelAssumptions NVARCHAR(MAX) = NULL,
    @TopologyReference NVARCHAR(400) = NULL,
    @ValidWhile NVARCHAR(400) = NULL,
    @IsStale BIT = 0,
    @ActorId UNIQUEIDENTIFIER = NULL,
    @MigrationRunId UNIQUEIDENTIFIER = NULL
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;
    DECLARE @now DATETIMEOFFSET(7) = SYSDATETIMEOFFSET();
    IF @ActorId IS NULL EXEC [personnel].[ResolveActor] @ActorId = @ActorId OUTPUT;
    INSERT [document].[Study] ([RevisionRowId], [CreatedBy], [CreatedAt], [ModifiedBy], [ModifiedAt], [MigrationRunId], [StudyKind], [Software], [SoftwareVersion], [SystemModelAt], [NetworkModelCaseEntityId], [FaultLevelAssumptions], [TopologyReference], [ValidWhile], [IsStale])
    VALUES (@RevisionRowId, @ActorId, @now, @ActorId, @now, @MigrationRunId, @StudyKind, @Software, @SoftwareVersion, @SystemModelAt, @NetworkModelCaseEntityId, @FaultLevelAssumptions, @TopologyReference, @ValidWhile, @IsStale);
END;
