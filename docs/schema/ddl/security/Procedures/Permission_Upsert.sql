-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [security].[Permission_Upsert]
    @PermissionCode NVARCHAR(80),
    @SubjectClass NVARCHAR(40),
    @Verb NVARCHAR(20),
    @Name NVARCHAR(200),
    @ActorId UNIQUEIDENTIFIER = NULL,
    @MigrationRunId UNIQUEIDENTIFIER = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @now DATETIMEOFFSET(7) = SYSDATETIMEOFFSET();
    IF @ActorId IS NULL EXEC [personnel].[ResolveActor] @ActorId = @ActorId OUTPUT;
    IF EXISTS (SELECT 1 FROM [security].[Permission] t WHERE t.[PermissionCode] = @PermissionCode)
        UPDATE t SET [SubjectClass] = @SubjectClass, [Verb] = @Verb, [Name] = @Name, [IsActive] = 1, [ModifiedBy] = @ActorId, [ModifiedAt] = @now FROM [security].[Permission] t WHERE t.[PermissionCode] = @PermissionCode;
    ELSE
        INSERT [security].[Permission] ([PermissionCode], [SubjectClass], [Verb], [Name], [CreatedBy], [CreatedAt], [ModifiedBy], [ModifiedAt], [MigrationRunId]) VALUES (@PermissionCode, @SubjectClass, @Verb, @Name, @ActorId, @now, @ActorId, @now, @MigrationRunId);
END;
