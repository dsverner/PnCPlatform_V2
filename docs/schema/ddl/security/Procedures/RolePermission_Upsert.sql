-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [security].[RolePermission_Upsert]
    @RoleCode NVARCHAR(40),
    @PermissionCode NVARCHAR(80),
    @ActorId UNIQUEIDENTIFIER = NULL,
    @MigrationRunId UNIQUEIDENTIFIER = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @now DATETIMEOFFSET(7) = SYSDATETIMEOFFSET();
    IF @ActorId IS NULL EXEC [personnel].[ResolveActor] @ActorId = @ActorId OUTPUT;
    IF EXISTS (SELECT 1 FROM [security].[RolePermission] t WHERE t.[RoleCode] = @RoleCode AND t.[PermissionCode] = @PermissionCode)
        UPDATE t SET [IsActive] = 1, [ModifiedBy] = @ActorId, [ModifiedAt] = @now FROM [security].[RolePermission] t WHERE t.[RoleCode] = @RoleCode AND t.[PermissionCode] = @PermissionCode;
    ELSE
        INSERT [security].[RolePermission] ([RoleCode], [PermissionCode], [CreatedBy], [CreatedAt], [ModifiedBy], [ModifiedAt], [MigrationRunId]) VALUES (@RoleCode, @PermissionCode, @ActorId, @now, @ActorId, @now, @MigrationRunId);
END;
