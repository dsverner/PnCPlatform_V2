-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [security].[RolePermission_Deactivate]
    @RoleCode NVARCHAR(40),
    @PermissionCode NVARCHAR(80),
    @ActorId UNIQUEIDENTIFIER = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @now DATETIMEOFFSET(7) = SYSDATETIMEOFFSET();
    IF @ActorId IS NULL EXEC [personnel].[ResolveActor] @ActorId = @ActorId OUTPUT;
    UPDATE [security].[RolePermission] SET [IsActive] = 0, [ModifiedBy] = @ActorId, [ModifiedAt] = @now WHERE [RoleCode] = @RoleCode AND [PermissionCode] = @PermissionCode AND [IsActive] = 1;
    IF @@ROWCOUNT = 0 THROW 50106, N'security.RolePermission: no active row with that key.', 1;
END;
