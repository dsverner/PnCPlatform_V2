-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [record].[TestSheet_Add]
    @TestPlanDefinitionVersionRowId UNIQUEIDENTIFIER,
    @InstrumentRowId UNIQUEIDENTIFIER = NULL,
    @AmbientConditions NVARCHAR(400) = NULL,
    @IsPartial BIT = 0,
    @ValidFrom DATETIMEOFFSET(7) = NULL,
    @ValidFromQuality TINYINT = 0,
    @ActorId UNIQUEIDENTIFIER = NULL,
    @MigrationRunId UNIQUEIDENTIFIER = NULL,
    @EntityId UNIQUEIDENTIFIER,
    @RowId UNIQUEIDENTIFIER = NULL OUTPUT
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;
    DECLARE @now DATETIMEOFFSET(7) = SYSDATETIMEOFFSET();
    SET @ValidFrom = ISNULL(@ValidFrom, @now);
    IF @ActorId IS NULL EXEC [personnel].[ResolveActor] @ActorId = @ActorId OUTPUT;
    BEGIN TRANSACTION;
    IF NOT EXISTS (SELECT 1 FROM [record].[RecordRegistry] WHERE [EntityId] = @EntityId)
        THROW 50102, N'record.TestSheet: @EntityId is not a registered Record.', 1;
    DECLARE @out TABLE ([RowId] UNIQUEIDENTIFIER);
    INSERT [record].[TestSheet] ([EntityId], [ValidFrom], [ValidFromQuality], [CreatedBy], [CreatedAt], [ModifiedBy], [ModifiedAt], [MigrationRunId], [TestPlanDefinitionVersionRowId], [InstrumentRowId], [AmbientConditions], [IsPartial])
    OUTPUT inserted.[RowId] INTO @out
    VALUES (@EntityId, @ValidFrom, @ValidFromQuality, @ActorId, @now, @ActorId, @now, @MigrationRunId, @TestPlanDefinitionVersionRowId, @InstrumentRowId, @AmbientConditions, @IsPartial);
    SELECT @RowId = [RowId] FROM @out;
    COMMIT TRANSACTION;
END;
