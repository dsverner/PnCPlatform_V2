-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [migration].[PositionCatalogue_Update]
    @RowId UNIQUEIDENTIFIER,
    @SourceSystem NVARCHAR(128),
    @LegacyLocation NVARCHAR(60),
    @LegacyEquipment NVARCHAR(100),
    @NodeTypeCode NVARCHAR(40) = NULL,
    @SubtypeCode NVARCHAR(40) = NULL,
    @FlocSegment NVARCHAR(60) = NULL,
    @DisplayName NVARCHAR(200) = NULL,
    @ParentFlocPath NVARCHAR(400) = NULL,
    @Disposition NVARCHAR(20) = N'Proposed',
    @Basis NVARCHAR(40) = NULL,
    @Notes NVARCHAR(MAX) = NULL,
    @ActorId UNIQUEIDENTIFIER = NULL
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;
    DECLARE @now DATETIMEOFFSET(7) = SYSDATETIMEOFFSET();
    IF @ActorId IS NULL EXEC [personnel].[ResolveActor] @ActorId = @ActorId OUTPUT;
    UPDATE [migration].[PositionCatalogue] SET [SourceSystem] = @SourceSystem, [LegacyLocation] = @LegacyLocation, [LegacyEquipment] = @LegacyEquipment, [NodeTypeCode] = @NodeTypeCode, [SubtypeCode] = @SubtypeCode, [FlocSegment] = @FlocSegment, [DisplayName] = @DisplayName, [ParentFlocPath] = @ParentFlocPath, [Disposition] = @Disposition, [Basis] = @Basis, [Notes] = @Notes, [ModifiedBy] = @ActorId, [ModifiedAt] = @now
    WHERE [RowId] = @RowId AND [IsDeleted] = 0;
    IF @@ROWCOUNT = 0 THROW 50104, N'migration.PositionCatalogue: no live row with that key.', 1;
END;
