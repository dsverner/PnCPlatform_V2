-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [migration].[StationCatalogue_Update]
    @RowId UNIQUEIDENTIFIER,
    @SourceSystem NVARCHAR(128),
    @LegacyLocation NVARCHAR(60),
    @AssetNumber CHAR(4) = NULL,
    @DivisionCode NVARCHAR(8) = NULL,
    @NodeEntityId UNIQUEIDENTIFIER = NULL,
    @Disposition NVARCHAR(20) = N'Proposed',
    @Basis NVARCHAR(40) = NULL,
    @Confidence DECIMAL(5,2) = NULL,
    @Notes NVARCHAR(MAX) = NULL,
    @ActorId UNIQUEIDENTIFIER = NULL
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;
    DECLARE @now DATETIMEOFFSET(7) = SYSDATETIMEOFFSET();
    IF @ActorId IS NULL EXEC [personnel].[ResolveActor] @ActorId = @ActorId OUTPUT;
    UPDATE [migration].[StationCatalogue] SET [SourceSystem] = @SourceSystem, [LegacyLocation] = @LegacyLocation, [AssetNumber] = @AssetNumber, [DivisionCode] = @DivisionCode, [NodeEntityId] = @NodeEntityId, [Disposition] = @Disposition, [Basis] = @Basis, [Confidence] = @Confidence, [Notes] = @Notes, [ModifiedBy] = @ActorId, [ModifiedAt] = @now
    WHERE [RowId] = @RowId AND [IsDeleted] = 0;
    IF @@ROWCOUNT = 0 THROW 50104, N'migration.StationCatalogue: no live row with that key.', 1;
END;
