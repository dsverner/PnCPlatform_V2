-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [migration].[PositionCatalogue_Add]
    @SourceSystem NVARCHAR(128),
    @LegacyLocation NVARCHAR(60),
    @LegacyEquipment NVARCHAR(100),
    @NodeTypeCode NVARCHAR(40) = NULL,
    @SubtypeCode NVARCHAR(40) = NULL,
    @FlocSegment NVARCHAR(60) = NULL,
    @DisplayName NVARCHAR(200) = NULL,
    @ParentFlocPath NVARCHAR(400) = NULL,
    @Disposition NVARCHAR(20) = N'Proposed',
    @Basis NVARCHAR(40) = NULL,
    @Notes NVARCHAR(MAX) = NULL,
    @ActorId UNIQUEIDENTIFIER = NULL,
    @MigrationRunId UNIQUEIDENTIFIER = NULL,
    @EntityId UNIQUEIDENTIFIER = NULL OUTPUT,
    @RowId UNIQUEIDENTIFIER = NULL OUTPUT
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;
    DECLARE @now DATETIMEOFFSET(7) = SYSDATETIMEOFFSET();
    IF @ActorId IS NULL EXEC [personnel].[ResolveActor] @ActorId = @ActorId OUTPUT;
    BEGIN TRANSACTION;
    SET @EntityId = ISNULL(@EntityId, NEWID());
    INSERT [migration].[PositionCatalogueRegistry] ([EntityId]) VALUES (@EntityId);
    DECLARE @out TABLE ([RowId] UNIQUEIDENTIFIER);
    INSERT [migration].[PositionCatalogue] ([EntityId], [CreatedBy], [CreatedAt], [ModifiedBy], [ModifiedAt], [MigrationRunId], [SourceSystem], [LegacyLocation], [LegacyEquipment], [NodeTypeCode], [SubtypeCode], [FlocSegment], [DisplayName], [ParentFlocPath], [Disposition], [Basis], [Notes])
    OUTPUT inserted.[RowId] INTO @out
    VALUES (@EntityId, @ActorId, @now, @ActorId, @now, @MigrationRunId, @SourceSystem, @LegacyLocation, @LegacyEquipment, @NodeTypeCode, @SubtypeCode, @FlocSegment, @DisplayName, @ParentFlocPath, @Disposition, @Basis, @Notes);
    SELECT @RowId = [RowId] FROM @out;
    COMMIT TRANSACTION;
END;
