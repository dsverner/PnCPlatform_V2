-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [personnel].[Actor_Append]
    @PersonEntityId UNIQUEIDENTIFIER = NULL,
    @ActingUserEntityId UNIQUEIDENTIFIER = NULL,
    @ActorKind NVARCHAR(20),
    @DelegationEntityId UNIQUEIDENTIFIER = NULL,
    @SystemName NVARCHAR(100) = NULL,
    @CreatedAt DATETIMEOFFSET(7),
    @ActorId UNIQUEIDENTIFIER = NULL OUTPUT
AS
BEGIN
    SET NOCOUNT ON;
    SET @ActorId = ISNULL(@ActorId, NEWID());
    INSERT [personnel].[Actor] ([ActorId], [PersonEntityId], [ActingUserEntityId], [ActorKind], [DelegationEntityId], [SystemName], [CreatedAt])
    VALUES (@ActorId, @PersonEntityId, @ActingUserEntityId, @ActorKind, @DelegationEntityId, @SystemName, @CreatedAt);
END;
