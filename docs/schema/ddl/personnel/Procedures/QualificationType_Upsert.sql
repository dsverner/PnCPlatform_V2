-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [personnel].[QualificationType_Upsert]
    @QualificationTypeCode NVARCHAR(40),
    @Name NVARCHAR(200),
    @WorkCategory NVARCHAR(40) = NULL,
    @ApplicableAssetTypeCode NVARCHAR(40) = NULL,
    @RequiredOjtHours DECIMAL(8,2) = NULL,
    @RequiredExperienceYears DECIMAL(4,1) = NULL,
    @RecertificationDays INT = NULL,
    @RequiresSupervisorSignOff BIT = 0,
    @ActorId UNIQUEIDENTIFIER = NULL,
    @MigrationRunId UNIQUEIDENTIFIER = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @now DATETIMEOFFSET(7) = SYSDATETIMEOFFSET();
    IF @ActorId IS NULL EXEC [personnel].[ResolveActor] @ActorId = @ActorId OUTPUT;
    IF EXISTS (SELECT 1 FROM [personnel].[QualificationType] t WHERE t.[QualificationTypeCode] = @QualificationTypeCode)
        UPDATE t SET [Name] = @Name, [WorkCategory] = @WorkCategory, [ApplicableAssetTypeCode] = @ApplicableAssetTypeCode, [RequiredOjtHours] = @RequiredOjtHours, [RequiredExperienceYears] = @RequiredExperienceYears, [RecertificationDays] = @RecertificationDays, [RequiresSupervisorSignOff] = @RequiresSupervisorSignOff, [IsActive] = 1, [ModifiedBy] = @ActorId, [ModifiedAt] = @now FROM [personnel].[QualificationType] t WHERE t.[QualificationTypeCode] = @QualificationTypeCode;
    ELSE
        INSERT [personnel].[QualificationType] ([QualificationTypeCode], [Name], [WorkCategory], [ApplicableAssetTypeCode], [RequiredOjtHours], [RequiredExperienceYears], [RecertificationDays], [RequiresSupervisorSignOff], [CreatedBy], [CreatedAt], [ModifiedBy], [ModifiedAt], [MigrationRunId]) VALUES (@QualificationTypeCode, @Name, @WorkCategory, @ApplicableAssetTypeCode, @RequiredOjtHours, @RequiredExperienceYears, @RecertificationDays, @RequiresSupervisorSignOff, @ActorId, @now, @ActorId, @now, @MigrationRunId);
END;
