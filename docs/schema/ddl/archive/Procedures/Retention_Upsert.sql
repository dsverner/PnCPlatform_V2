-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [archive].[Retention_Upsert]
    @RetentionCode NVARCHAR(40),
    @RecordKindCode NVARCHAR(40) = NULL,
    @EventKind NVARCHAR(40) = NULL,
    @RuleDefinitionEntityId UNIQUEIDENTIFIER,
    @Description NVARCHAR(MAX) = NULL,
    @ActorId UNIQUEIDENTIFIER = NULL,
    @MigrationRunId UNIQUEIDENTIFIER = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @now DATETIMEOFFSET(7) = SYSDATETIMEOFFSET();
    IF @ActorId IS NULL EXEC [personnel].[ResolveActor] @ActorId = @ActorId OUTPUT;
    IF EXISTS (SELECT 1 FROM [archive].[Retention] t WHERE t.[RetentionCode] = @RetentionCode)
        UPDATE t SET [RecordKindCode] = @RecordKindCode, [EventKind] = @EventKind, [RuleDefinitionEntityId] = @RuleDefinitionEntityId, [Description] = @Description, [IsActive] = 1, [ModifiedBy] = @ActorId, [ModifiedAt] = @now FROM [archive].[Retention] t WHERE t.[RetentionCode] = @RetentionCode;
    ELSE
        INSERT [archive].[Retention] ([RetentionCode], [RecordKindCode], [EventKind], [RuleDefinitionEntityId], [Description], [CreatedBy], [CreatedAt], [ModifiedBy], [ModifiedAt], [MigrationRunId]) VALUES (@RetentionCode, @RecordKindCode, @EventKind, @RuleDefinitionEntityId, @Description, @ActorId, @now, @ActorId, @now, @MigrationRunId);
END;
