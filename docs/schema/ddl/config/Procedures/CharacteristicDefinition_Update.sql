-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [config].[CharacteristicDefinition_Update]
    @RowId UNIQUEIDENTIFIER,
    @DefinitionVersionRowId UNIQUEIDENTIFIER,
    @CharacteristicKey NVARCHAR(100),
    @Name NVARCHAR(200),
    @Description NVARCHAR(MAX) = NULL,
    @DataType NVARCHAR(20),
    @UnitCode NVARCHAR(20) = NULL,
    @Base NVARCHAR(20) = NULL,
    @IsRequired BIT = 0,
    @AllowedValuesDefinitionRowId UNIQUEIDENTIFIER = NULL,
    @ReferenceTargetKind NVARCHAR(40) = NULL,
    @ValidationExpression NVARCHAR(MAX) = NULL,
    @DisplayOrder INT = 0,
    @DisplayGroup NVARCHAR(100) = NULL,
    @IsCatalogueFact BIT = 0,
    @ActorId UNIQUEIDENTIFIER = NULL
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;
    DECLARE @now DATETIMEOFFSET(7) = SYSDATETIMEOFFSET();
    IF @ActorId IS NULL EXEC [personnel].[ResolveActor] @ActorId = @ActorId OUTPUT;
    UPDATE [config].[CharacteristicDefinition] SET [DefinitionVersionRowId] = @DefinitionVersionRowId, [CharacteristicKey] = @CharacteristicKey, [Name] = @Name, [Description] = @Description, [DataType] = @DataType, [UnitCode] = @UnitCode, [Base] = @Base, [IsRequired] = @IsRequired, [AllowedValuesDefinitionRowId] = @AllowedValuesDefinitionRowId, [ReferenceTargetKind] = @ReferenceTargetKind, [ValidationExpression] = @ValidationExpression, [DisplayOrder] = @DisplayOrder, [DisplayGroup] = @DisplayGroup, [IsCatalogueFact] = @IsCatalogueFact, [ModifiedBy] = @ActorId, [ModifiedAt] = @now
    WHERE [RowId] = @RowId AND [IsDeleted] = 0;
    IF @@ROWCOUNT = 0 THROW 50104, N'config.CharacteristicDefinition: no live row with that key.', 1;
END;
