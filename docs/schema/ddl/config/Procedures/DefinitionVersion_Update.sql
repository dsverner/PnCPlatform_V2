-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [config].[DefinitionVersion_Update]
    @RowId UNIQUEIDENTIFIER,
    @DefinitionEntityId UNIQUEIDENTIFIER,
    @VersionNumber INT,
    @Status NVARCHAR(20),
    @EffectiveFrom DATETIMEOFFSET(7) = NULL,
    @EffectiveTo DATETIMEOFFSET(7) = NULL,
    @ApprovedBy UNIQUEIDENTIFIER = NULL,
    @ApprovedAt DATETIMEOFFSET(7) = NULL,
    @ChangeNote NVARCHAR(MAX) = NULL,
    @TestEvidenceRecordId UNIQUEIDENTIFIER = NULL,
    @PayloadText NVARCHAR(MAX) = NULL,
    @PayloadHash BINARY(32) = NULL,
    @ActorId UNIQUEIDENTIFIER = NULL
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;
    DECLARE @now DATETIMEOFFSET(7) = SYSDATETIMEOFFSET();
    IF @ActorId IS NULL EXEC [personnel].[ResolveActor] @ActorId = @ActorId OUTPUT;
    UPDATE [config].[DefinitionVersion] SET [DefinitionEntityId] = @DefinitionEntityId, [VersionNumber] = @VersionNumber, [Status] = @Status, [EffectiveFrom] = @EffectiveFrom, [EffectiveTo] = @EffectiveTo, [ApprovedBy] = @ApprovedBy, [ApprovedAt] = @ApprovedAt, [ChangeNote] = @ChangeNote, [TestEvidenceRecordId] = @TestEvidenceRecordId, [PayloadText] = @PayloadText, [PayloadHash] = @PayloadHash, [ModifiedBy] = @ActorId, [ModifiedAt] = @now
    WHERE [RowId] = @RowId AND [IsDeleted] = 0;
    IF @@ROWCOUNT = 0 THROW 50104, N'config.DefinitionVersion: no live row with that key.', 1;
END;
