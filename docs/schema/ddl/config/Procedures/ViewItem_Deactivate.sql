-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [config].[ViewItem_Deactivate]
    @ScreenKey NVARCHAR(60),
    @ItemKey NVARCHAR(60),
    @ActorId UNIQUEIDENTIFIER = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @now DATETIMEOFFSET(7) = SYSDATETIMEOFFSET();
    IF @ActorId IS NULL EXEC [personnel].[ResolveActor] @ActorId = @ActorId OUTPUT;
    UPDATE [config].[ViewItem] SET [IsActive] = 0, [ModifiedBy] = @ActorId, [ModifiedAt] = @now WHERE [ScreenKey] = @ScreenKey AND [ItemKey] = @ItemKey AND [IsActive] = 1;
    IF @@ROWCOUNT = 0 THROW 50106, N'config.ViewItem: no active row with that key.', 1;
END;
