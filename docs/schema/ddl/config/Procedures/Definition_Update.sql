-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [config].[Definition_Update]
    @RowId UNIQUEIDENTIFIER,
    @DefinitionKind NVARCHAR(40),
    @DefinitionKey NVARCHAR(100),
    @Name NVARCHAR(200),
    @Description NVARCHAR(MAX) = NULL,
    @OwningRoleCode NVARCHAR(40) = NULL,
    @ActorId UNIQUEIDENTIFIER = NULL
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;
    DECLARE @now DATETIMEOFFSET(7) = SYSDATETIMEOFFSET();
    IF @ActorId IS NULL EXEC [personnel].[ResolveActor] @ActorId = @ActorId OUTPUT;
    UPDATE [config].[Definition] SET [DefinitionKind] = @DefinitionKind, [DefinitionKey] = @DefinitionKey, [Name] = @Name, [Description] = @Description, [OwningRoleCode] = @OwningRoleCode, [ModifiedBy] = @ActorId, [ModifiedAt] = @now
    WHERE [RowId] = @RowId AND [IsDeleted] = 0;
    IF @@ROWCOUNT = 0 THROW 50104, N'config.Definition: no live row with that key.', 1;
END;
