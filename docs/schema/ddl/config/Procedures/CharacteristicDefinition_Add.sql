-- GENERATED by tools/generate.py from the deployed catalog — do not edit; regenerate with: python tools/generate.py
CREATE PROCEDURE [config].[CharacteristicDefinition_Add]
    @DefinitionVersionRowId UNIQUEIDENTIFIER,
    @CharacteristicKey NVARCHAR(100),
    @Name NVARCHAR(200),
    @Description NVARCHAR(MAX) = NULL,
    @DataType NVARCHAR(20),
    @UnitCode NVARCHAR(20) = NULL,
    @Base NVARCHAR(20) = NULL,
    @IsRequired BIT = 0,
    @AllowedValuesDefinitionRowId UNIQUEIDENTIFIER = NULL,
    @ReferenceTargetKind NVARCHAR(40) = NULL,
    @ValidationExpression NVARCHAR(MAX) = NULL,
    @DisplayOrder INT = 0,
    @DisplayGroup NVARCHAR(100) = NULL,
    @IsCatalogueFact BIT = 0,
    @ActorId UNIQUEIDENTIFIER = NULL,
    @MigrationRunId UNIQUEIDENTIFIER = NULL,
    @EntityId UNIQUEIDENTIFIER = NULL OUTPUT,
    @RowId UNIQUEIDENTIFIER = NULL OUTPUT
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;
    DECLARE @now DATETIMEOFFSET(7) = SYSDATETIMEOFFSET();
    IF @ActorId IS NULL EXEC [personnel].[ResolveActor] @ActorId = @ActorId OUTPUT;
    BEGIN TRANSACTION;
    SET @EntityId = ISNULL(@EntityId, NEWID());
    INSERT [config].[CharacteristicDefinitionRegistry] ([EntityId]) VALUES (@EntityId);
    DECLARE @out TABLE ([RowId] UNIQUEIDENTIFIER);
    INSERT [config].[CharacteristicDefinition] ([EntityId], [CreatedBy], [CreatedAt], [ModifiedBy], [ModifiedAt], [MigrationRunId], [DefinitionVersionRowId], [CharacteristicKey], [Name], [Description], [DataType], [UnitCode], [Base], [IsRequired], [AllowedValuesDefinitionRowId], [ReferenceTargetKind], [ValidationExpression], [DisplayOrder], [DisplayGroup], [IsCatalogueFact])
    OUTPUT inserted.[RowId] INTO @out
    VALUES (@EntityId, @ActorId, @now, @ActorId, @now, @MigrationRunId, @DefinitionVersionRowId, @CharacteristicKey, @Name, @Description, @DataType, @UnitCode, @Base, @IsRequired, @AllowedValuesDefinitionRowId, @ReferenceTargetKind, @ValidationExpression, @DisplayOrder, @DisplayGroup, @IsCatalogueFact);
    SELECT @RowId = [RowId] FROM @out;
    COMMIT TRANSACTION;
END;
