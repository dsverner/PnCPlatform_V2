# SEL-221F line distance protection rationale - `RATIONALE_SEL221F_LINE`

Generated by `tools/rationale_sel221f.py` (#219). One section per protective element of the element map
(`sel-221f.relay-word.md`), plus the shared line data, inputs and fault study. The engineer answers the inputs on the
record's Rationale tab; the platform computes the settings, writes them onto the outstanding revision, and assembles the
Word document from the statements. Defaults are starting values from the two legacy rationales named on each row -
**A4811.docx (Grand Falls Plant, L1175 A-Protection)** and **A0394.doc (Eel River Terminal, 0025 A-Prot)** - not a philosophy statement; when a written NB Power philosophy is found it becomes the cite.

## Plant facts the engine resolves first

| Fact | Meaning |
|---|---|
| `line.Name` | the line's name |
| `line.RemoteStation` | the station at the line's other terminal |
| `line.NearStation` | the station at the device's terminal |
| `line.kV` | the line's nominal voltage in kV (from its voltage class) |
| `line.R1` | positive-sequence resistance, Ω primary (the line asset) |
| `line.X1` | positive-sequence reactance, Ω primary |
| `line.R0` | zero-sequence resistance, Ω primary |
| `line.X0` | zero-sequence reactance, Ω primary |
| `line.LengthMiles` | line length, miles |
| `line.ChargingMvaPerMile` | line charging, MVA per mile (optional) |
| `device.Name` | the relay's name |
| `device.Station` | the relay's station |

## Sections, in document order

### Line data (`LINE`, shared)

Statement: Relay line settings to `{line.RemoteStation}` (`{line.Name}`, `{line.kV}` kV): Z1 = `{line.R1}` + j`{line.X1}` Ω primary, line angle `{setting.MTA}`°; Z0 = `{line.R0}` + j`{line.X0}` Ω primary; LL = `{line.LengthMiles}` miles. `{note}`

| Setting written | Formula |
|---|---|
| `R1` | `line.R1` |
| `X1` | `line.X1` |
| `R0` | `line.R0` |
| `X0` | `line.X0` |
| `MTA` | `round(atan2(line.X1, line.R1), 2)` |

| Input | Type | Unit | Default | Source / meaning |
|---|---|---|---|---|
| `Line` (The line) | Reference |  |  | The transmission line this relay protects; prefilled from the scheme that names the relay and the line it protects, else picked here. |
| `Note_LINE` (Note) | Text |  |  | Your own words on the line data, appended to the section. |

### Current and potential inputs (`INPUTS`, shared)

Statement: CT ratio `{input.CtPrimary}`:`{input.CtSecondary}` (CTR = `{setting.CTR}`); PT ratio `{input.PtPrimary}`:1 (PTR = `{setting.PTR}`); synchronizing PT ratio `{input.SptPrimary}`:1 (SPTR = `{setting.SPTR}`). `{note}`

| Setting written | Formula |
|---|---|
| `CTR` | `round(input.CtPrimary / input.CtSecondary, 2)` |
| `PTR` | `input.PtPrimary` |
| `SPTR` | `input.SptPrimary` |

| Input | Type | Unit | Default | Source / meaning |
|---|---|---|---|---|
| `CtPrimary` (CT ratio, primary) | Decimal | A | 800 | The current transformer ratio's primary amperes (A4811: 'CT Ratio=800-5'; A0394: 600-5). |
| `CtSecondary` (CT ratio, secondary) | Decimal | A | 5 | The current transformer ratio's secondary amperes. |
| `PtPrimary` (PT ratio, primary) | Decimal |  | 1200 | The potential transformer ratio (A4811: 'PT Ratio=1200-1'; A0394: 600-1). Ratio to 1. |
| `SptPrimary` (Synchronizing PT ratio) | Decimal |  | 700 | The synchronizing potential transformer ratio to 1 (A4811: 'Set SPTR=700 - The sync pt is set on its 115 volt winding'; A0394: 350). |
| `Note_INPUTS` (Note) | Text |  |  | Your own words on the instrument transformers. |

### Fault study (`FAULTSTUDY`, shared)

Statement: Fault study `{input.FaultStudyDate}`: the currents the overcurrent pickups are set against are tabled below. `{note}`

| Input | Type | Unit | Default | Source / meaning |
|---|---|---|---|---|
| `FaultStudy` (Fault study) | Table |  |  | The fault currents the overcurrent pickups are set against: location, fault current at maximum generation, at minimum generation, comment (A4811: 'Fault Study (November 22, 2023)'). |
| `FaultStudyDate` (Fault study date) | Text |  |  | When the fault study was run (A4811: November 22, 2023). |
| `Note_FAULTSTUDY` (Note) | Text |  |  | Your own words on the fault study. |

### Zone 1 (`Z1`, element)

Statement: Set zone 1 at `{input.Zone1Pct}`% of the distance to `{line.RemoteStation}` (Z1% = `{setting.Z1%}`). `{supervision}` `{note}`

| Setting written | Formula |
|---|---|
| `Z1%` | `input.Zone1Pct` |

| Input | Type | Unit | Default | Source / meaning |
|---|---|---|---|---|
| `Used_Z1` (Zone 1 in use) | Boolean |  | Y | Whether zone 1 is used at this position. |
| `Zone1Pct` (Zone 1 reach, % of the line) | Decimal | % | 85 | A4811: 'Set zone 1 at 85% of the distance to St. Andre.'; A0394: 85% to the low-voltage bus, adjusted to 80%. |
| `Trips_Z1` (Zone 1 trips (MTU)) | Boolean |  | Y | Zone 1 phase and ground (Z1P, Z1G) in the unconditional trip mask (A4811 and A0394: yes). |
| `Recloses_Z1` (Zone 1 initiates reclosing (MRI)) | Boolean |  | Y | Z1P, Z1G in the reclose initiate mask (A4811: 'Line reclosing will be initiated by any trip coming from the protection'). |
| `Note_Z1` (Note) | Text |  |  | Your own words on zone 1 (A0394 explains a tap: 'Setting=26.03/(36.07*cos(90-64.55))*100%=80%'). |

### Zone 2 (`Z2`, element)

Statement: Set zone 2 to `{input.Zone2Pct}`% of the distance to `{line.RemoteStation}`, with a time delay of `{input.Zone2DelayPhase}` cycles for phase faults (Z2DP) and `{input.Zone2DelayGround}` cycles for ground faults (Z2DG). `{supervision}` `{note}`

| Setting written | Formula |
|---|---|
| `Z2%` | `input.Zone2Pct` |
| `Z2DP` | `input.Zone2DelayPhase` |
| `Z2DG` | `input.Zone2DelayGround` |

| Input | Type | Unit | Default | Source / meaning |
|---|---|---|---|---|
| `Used_Z2` (Zone 2 in use) | Boolean |  | Y |  |
| `Zone2Pct` (Zone 2 reach, % of the line) | Decimal | % | 130 | A4811: 'Set zone 2 to 130% of the distance to St. Andre'; A0394: 100% to the normally open point. |
| `Zone2DelayPhase` (Zone 2 delay, phase (Z2DP)) | Decimal | cycles | 18 | A4811: 'Use a time delay of 18 cycles (0.3 seconds)'; A0394: 90 cycles to coordinate with fuses. |
| `Zone2DelayGround` (Zone 2 delay, ground (Z2DG)) | Decimal | cycles | 18 | A4811: 18; A0394: 18 ('the ground distance elements can trip much faster'). |
| `Trips_Z2` (Zone 2 trips (MTU)) | Boolean |  | Y | Z2PT, Z2GT in the unconditional trip mask. |
| `Recloses_Z2` (Zone 2 initiates reclosing (MRI)) | Boolean |  | Y |  |
| `Note_Z2` (Note) | Text |  |  |  |

### Zone 3 (`Z3`, element)

Statement: Set zone 3 to `{input.Zone3Pct}`% of the distance to `{line.RemoteStation}`, time delayed `{input.Zone3Delay}` cycles (Z3D). `{supervision}` `{note}`

| Setting written | Formula |
|---|---|
| `Z3%` | `input.Zone3Pct` |
| `Z3D` | `input.Zone3Delay` |

| Input | Type | Unit | Default | Source / meaning |
|---|---|---|---|---|
| `Used_Z3` (Zone 3 in use) | Boolean |  | Y |  |
| `Zone3Pct` (Zone 3 reach, % of the line) | Decimal | % | 130 | A0394: 'Set zone 3 at 130% of the distance to the normally open point'; A4811: 1314% for an abnormal condition, 'normally turned off in the MTU mask'. |
| `Zone3Delay` (Zone 3 delay (Z3D)) | Decimal | cycles | 36 | A4811: Z3D=36; A0394: 156 (2.6 seconds). |
| `Trips_Z3` (Zone 3 time-delayed trips (MTU)) | Boolean |  | Y | Z3T in the unconditional trip mask (A0394: yes; A4811: normally off). |
| `Recloses_Z3` (Zone 3 initiates reclosing (MRI)) | Boolean |  | N |  |
| `Note_Z3` (Note) | Text |  |  |  |

### Supervisory phase overcurrent (50P) (`50P`, element)

Statement: The supervisory overcurrents must be set high enough not to pick up on the line charging current: I charge = `{value.ChargingMva}` MVA/mile × `{line.LengthMiles}` miles × `{value.AmpsPerMva}` A/MVA = `{value.ChargingA}` A primary. Set 50P = `{setting.50P}` A. `{note}`

| Setting written | Formula |
|---|---|
| `50P` | `input.Pickup50P` |

| Value | Formula |
|---|---|
| `value.ChargingMva` | `coalesce(line.ChargingMvaPerMile, input.ChargingMvaPerMile)` |
| `value.AmpsPerMva` | `round(1000000 / (1.7320508 * line.kV * 1000), 2)` |
| `value.ChargingA` | `round(coalesce(line.ChargingMvaPerMile, input.ChargingMvaPerMile) * line.LengthMiles * (1000000 / (1.7320508 * line.kV * 1000)), 2)` |

| Input | Type | Unit | Default | Source / meaning |
|---|---|---|---|---|
| `Used_50P` (50P in use) | Boolean |  | Y |  |
| `ChargingMvaPerMile` (Line charging, MVA per mile) | Decimal |  | 0.128 | Used when the line asset carries none (A4811: '0.128 MVA/mile' at 138 kV; A0394: 0.032 at 69 kV). |
| `Pickup50P` (50P pickup) | Decimal | A | 80 | A4811: 'Set 50NG=80 amps and 50P=80 amps'; A0394: 100. Above the charging current, about 1/3 of the far-end fault level. |
| `Note_50P` (Note) | Text |  |  |  |

### Supervisory residual overcurrent (50NG) (`50NG`, element)

Statement: Set 50NG = `{setting.50NG}` A, above the charging current (`{value.ChargingA}` A) and below the line-ground fault level at the far end. `{note}`

| Setting written | Formula |
|---|---|
| `50NG` | `input.Pickup50NG` |

| Value | Formula |
|---|---|
| `value.ChargingA` | `value.ChargingA` |

| Input | Type | Unit | Default | Source / meaning |
|---|---|---|---|---|
| `Used_50NG` (50NG in use) | Boolean |  | Y |  |
| `Pickup50NG` (50NG pickup) | Decimal | A | 80 | A4811: 80; A0394: 60 ('this will provide good sensitivity for all line faults'). |
| `Note_50NG` (Note) | Text |  |  |  |

### High-set phase overcurrent (50H, switch-onto-fault) (`50H`, element)

Statement: The close-in three-phase fault level is `{input.CloseInFaultA}` A primary; the high-set overcurrent trips through the MTO mask while 52BT is asserted after the breaker closes, so that a line energized onto ground leads trips back out at once. Set 50H = `{setting.50H}` A. `{supervision}` `{note}`

| Setting written | Formula |
|---|---|
| `50H` | `coalesce(input.Pickup50H, round(0.5 * input.CloseInFaultA, 0))` |

| Input | Type | Unit | Default | Source / meaning |
|---|---|---|---|---|
| `Used_50H` (50H in use) | Boolean |  | Y |  |
| `CloseInFaultA` (Close-in three-phase fault, A) | Decimal | A | 7000 | A0394: 'The close in three phase fault level at Eel River is 7000 amps primary'; A4811 did not use it (50H=5000, 'will not be used'). |
| `Pickup50H` (50H pickup (override)) | Decimal | A |  | Leave empty for 50 % of the close-in fault (A0394: 'trip at 50% of the close in fault level ... Set 50H=3500'); or set it. |
| `Trips_50H` (50H trips with the breaker open (MTO)) | Boolean |  | Y | 50H in the trip-with-breaker-open mask (A0394: 'MTO Set to trip on high set overcurrent'; A4811: off). |
| `Note_50H` (Note) | Text |  |  |  |

### Residual time-overcurrent (51N) (`51N`, element)

Statement: The inverse-time residual element picks up at `{setting.51NP}` A on curve `{setting.51NC}` with time dial `{setting.51NTD}`; torque control `{setting.51NTC}`. `{supervision}` `{note}`

| Setting written | Formula |
|---|---|
| `51NP` | `input.Pickup51NP` |
| `51NC` | `input.Curve51NC` |
| `51NTD` | `input.TimeDial51NTD` |
| `51NTC` | `input.TorqueControl51NTC` |

| Input | Type | Unit | Default | Source / meaning |
|---|---|---|---|---|
| `Used_51N` (51N in use) | Boolean |  | Y |  |
| `Pickup51NP` (51NP pickup) | Decimal | A | 160 | A4811: 'pick up at 160A (1ASec)'; A0394: 60 (coordinated with the MCGG on the line). |
| `Curve51NC` (51NC curve) | Text |  | 3 | The relay's curve number (A4811 and A0394: 3, 'SEL Very Inverse'). |
| `TimeDial51NTD` (51NTD time dial) | Decimal |  | 2.01 | A4811: 2.01 ('trip in 0.5s for the fault at St. Andre during maximum generation'); A0394: 3.5. |
| `TorqueControl51NTC` (51NTC torque control) | Text |  | Y | Y = directionally supervised by 32Q (A4811: Y; A0394: N). |
| `Trips_51N` (51NT trips (MTU)) | Boolean |  | Y | 51NT in the unconditional trip mask (A0394 puts 51NT and 67N in MA2 'to allow the neutral trips to be isolated via a blocking switch'). |
| `Recloses_51N` (51NT initiates reclosing (MRI)) | Boolean |  | N |  |
| `Note_51N` (Note) | Text |  |  |  |

### Residual instantaneous overcurrent (67N) (`67N`, element)

Statement: The line-ground fault at `{line.RemoteStation}` is `{input.RemoteFaultA}` A at maximum generation; the instantaneous unit is set at 150% of it: 67NP = `{setting.67NP}` A, torque control `{setting.67NTC}`. `{supervision}` `{note}`

| Setting written | Formula |
|---|---|
| `67NP` | `coalesce(input.Pickup67NP, round(1.5 * input.RemoteFaultA, 0))` |
| `67NTC` | `input.TorqueControl67NTC` |

| Input | Type | Unit | Default | Source / meaning |
|---|---|---|---|---|
| `Used_67N` (67N in use) | Boolean |  | Y |  |
| `RemoteFaultA` (Line-ground fault at the remote terminal, A) | Decimal | A | 824 | A4811: St. Andre 824 A at maximum generation ('Set the instantaneous unit up at 150% of the St. Andre fault level'). |
| `Pickup67NP` (67NP pickup (override)) | Decimal | A |  | Leave empty for 150 % of the remote fault (A4811: 67NP = 1236); or set it (A0394: 800). |
| `TorqueControl67NTC` (67NTC torque control) | Text |  | Y | Y = directionally supervised by 32Q (A4811: Y; A0394: N). |
| `Trips_67N` (67N trips (MTU)) | Boolean |  | Y |  |
| `Recloses_67N` (67N initiates reclosing (MRI)) | Boolean |  | Y |  |
| `Note_67N` (Note) | Text |  |  |  |

### Negative-sequence directional element (32Q) (`32Q`, element)

Statement: The negative-sequence directional element, adjusted by the maximum torque angle MTA = `{setting.MTA}`°, always supervises the distance elements and, with torque control set, the residual overcurrent elements (2-24). `{note}`

| Input | Type | Unit | Default | Source / meaning |
|---|---|---|---|---|
| `Note_32Q` (Note) | Text |  |  |  |

### Loss-of-potential detection (LOP) (`LOP`, element)

Statement: Enable the loss-of-potential logic: LOPE = `{setting.LOPE}`; on loss of potential the distance elements are blocked (2-18). `{note}`

| Setting written | Formula |
|---|---|
| `LOPE` | `input.Lope` |

| Input | Type | Unit | Default | Source / meaning |
|---|---|---|---|---|
| `Used_LOP` (Loss-of-potential logic in use) | Boolean |  | Y |  |
| `Lope` (LOPE) | Text |  | 3 | A4811 and A0394: 'Enable the loss of potential logic. Set LOPE=3.' (Table 2.3, 2-17). |
| `Note_LOP` (Note) | Text |  |  |  |

### Reclosing (79) (`79`, element)

Statement: Reclosing is initiated by the elements marked so below: open interval 79OI = `{setting.79OI}` cycles, reset time 79RS = `{setting.79RS}` cycles. `{note}`

| Setting written | Formula |
|---|---|
| `79OI` | `input.OpenInterval` |
| `79RS` | `input.ResetTime` |

| Input | Type | Unit | Default | Source / meaning |
|---|---|---|---|---|
| `Used_79` (Reclosing in use) | Boolean |  | Y |  |
| `OpenInterval` (79OI open interval) | Decimal | cycles | 30 | A4811: 'The line presently recloses in 0.5 seconds at Grand Falls ... set 79OI=30'; A0394: 60. |
| `ResetTime` (79RS reset time) | Decimal | cycles | 900 | A4811 and A0394: 900 (15 seconds). |
| `Note_79` (Note) | Text |  |  |  |

### Synchronism and voltage checking (25, 27, 59) (`25`, element)

Statement: PSVC = `{setting.PSVC}`; dead below 27VLO = `{setting.27VLO}`, live above 59VHI = `{setting.59VHI}`; difference voltage 25DV = `{setting.25DV}`; sync check on phase `{setting.SYNCP}`; in phase for 25T = `{setting.25T}` cycles; voltages constant for VCT = `{setting.VCT}` cycles. `{note}`

| Setting written | Formula |
|---|---|
| `PSVC` | `input.Psvc` |
| `27VLO` | `input.VoltsLow` |
| `59VHI` | `input.VoltsHigh` |
| `25DV` | `input.DiffVolts` |
| `SYNCP` | `input.SyncPhase` |
| `25T` | `input.SyncTimer` |
| `VCT` | `input.VoltageTimer` |

| Input | Type | Unit | Default | Source / meaning |
|---|---|---|---|---|
| `Used_25` (Sync and voltage checking in use) | Boolean |  | Y |  |
| `Psvc` (PSVC) | Text |  | P | A4811: 'PSVC=P - The polarizing pt's are on the bus (Live Bus/ dead line)'. |
| `VoltsLow` (27VLO dead voltage) | Decimal |  | 40 | A4811: '27VLO=40 - Line will be considered dead at 50% nominal line voltage'; A0394: 20. |
| `VoltsHigh` (59VHI live voltage) | Decimal |  | 64 | A4811: '59VHI=64 - live at 80% nominal'; A0394: 32. |
| `DiffVolts` (25DV difference voltage) | Decimal |  | 40 | A4811: '25DV=40 - Differential voltage will correspond to an angular difference of 30o'; A0394: 20. |
| `SyncPhase` (SYNCP) | Text |  | A | A4811: 'SYNCP=A - Sync pt on phase A'. |
| `SyncTimer` (25T) | Decimal | cycles | 180 | A4811: '25T=180 - Length of time that the system must be in phase'. |
| `VoltageTimer` (VCT) | Decimal | cycles | 30 | A4811: 'VCT=30 - Length of time that the voltages must be constant'. |
| `Note_25` (Note) | Text |  |  |  |

### Remote-end-just-opened (REJO) (`REJO`, element)

Statement: Remote-end-just-opened tripping: REJOE = `{setting.REJOE}`. `{note}`

| Setting written | Formula |
|---|---|
| `REJOE` | `input.Rejoe` |

| Input | Type | Unit | Default | Source / meaning |
|---|---|---|---|---|
| `Rejoe` (REJOE) | Text |  | N | A0394: 'We will not be using the remote end open tripping function. Set REJOE=N.' |
| `Note_REJO` (Note) | Text |  |  |  |

### Switch-onto-fault (52BT) (`SOTF`, element)

Statement: Set 52BT = `{setting.52BT}` cycles, which enables the switch-onto-fault protection (the MTO mask) for that long after the breaker is closed. `{note}`

| Setting written | Formula |
|---|---|
| `52BT` | `input.Bt52` |

| Input | Type | Unit | Default | Source / meaning |
|---|---|---|---|---|
| `Used_SOTF` (Switch-onto-fault in use) | Boolean |  | Y |  |
| `Bt52` (52BT) | Decimal | cycles | 30 | A4811 and A0394: 'Set 52BT=30 cycles which enables the switch onto fault protection for 30 cycles after the breaker is closed.' |
| `Note_SOTF` (Note) | Text |  |  |  |

### Fault locating (`FAULTLOC`, element)

Statement: The fault locator uses the line data above; line length LL = `{setting.LL}` miles. `{note}`

| Setting written | Formula |
|---|---|
| `LL` | `line.LengthMiles` |

| Input | Type | Unit | Default | Source / meaning |
|---|---|---|---|---|
| `Note_FAULTLOC` (Note) | Text |  |  |  |

### Timers (`TIMERS`, group)

Statement: A1TP = `{setting.A1TP}`, A1TD = `{setting.A1TD}`; minimum trip duration TDUR = `{setting.TDUR}` cycles. `{note}`

| Setting written | Formula |
|---|---|
| `A1TP` | `input.A1Pickup` |
| `A1TD` | `input.A1Dropout` |
| `TDUR` | `input.TripDuration` |

| Input | Type | Unit | Default | Source / meaning |
|---|---|---|---|---|
| `A1Pickup` (A1TP) | Decimal | cycles | 0 | A4811: 'Programmable outputs will be used to supervise the reclosing. They will not be time delayed in any way. A1TP=0 and A1TD=0.' |
| `A1Dropout` (A1TD) | Decimal | cycles | 0 |  |
| `TripDuration` (TDUR) | Decimal | cycles | 12 | A4811 summary: TDUR=12. |
| `Note_TIMERS` (Note) | Text |  |  |  |

### Communications (`COMMS`, group)

Statement: TIME1 = `{setting.TIME1}`, TIME2 = `{setting.TIME2}`, AUTO = `{setting.AUTO}`, RINGS = `{setting.RINGS}`. `{note}`

| Setting written | Formula |
|---|---|
| `TIME1` | `input.Time1` |
| `TIME2` | `input.Time2` |
| `AUTO` | `input.Auto` |
| `RINGS` | `input.Rings` |

| Input | Type | Unit | Default | Source / meaning |
|---|---|---|---|---|
| `Time1` (TIME1) | Integer | min | 5 | A4811 and A0394: 'Set Time1=5, time2=0, auto=2, rings=1.' |
| `Time2` (TIME2) | Integer | min | 0 |  |
| `Auto` (AUTO) | Text |  | 2 |  |
| `Rings` (RINGS) | Integer |  | 1 |  |
| `Note_COMMS` (Note) | Text |  |  |  |

### Logic masks (`MASKS`, group)

Statement: MTU, MRI and MTO follow the elements above (the trip, reclose-initiate and trip-with-breaker-open marks); the other masks as set here. `{note}`

| Setting written | Formula |
|---|---|
| `MPT` | `input.MaskMPT` |
| `MTB` | `input.MaskMTB` |
| `MA1` | `input.MaskMA1` |
| `MA2` | `input.MaskMA2` |
| `MA3` | `input.MaskMA3` |
| `MA4` | `input.MaskMA4` |
| `MRC` | `input.MaskMRC` |

| Input | Type | Unit | Default | Source / meaning |
|---|---|---|---|---|
| `MaskMPT` (MPT) | Text |  | 00 00 00 | Trip with permissive-trip asserted (5-33); set as the scheme needs. |
| `MaskMTB` (MTB) | Text |  | 00 00 00 | Trip with block-trip unasserted (5-34). |
| `MaskMA1` (MA1) | Text |  | 00 00 00 | A1 output (5-35). |
| `MaskMA2` (MA2) | Text |  | 00 00 00 | A2 output (5-35); A0394 puts 67N and 51NT here (00 A0 00). |
| `MaskMA3` (MA3) | Text |  | 00 00 00 | A3 output (5-35). |
| `MaskMA4` (MA4) | Text |  | 00 00 00 | A4 output (5-35). |
| `MaskMRC` (MRC) | Text |  | 00 00 00 | Reclose cancel (5-37). |
| `Note_MASKS` (Note) | Text |  |  |  |

## Derived masks

| Mask | From the elements' mark | Outputs per element |
|---|---|---|
| `MTU` | `Trips_<element>` | Z1: Z1P, Z1G; Z2: Z2PT, Z2GT; Z3: Z3T; 51N: 51NT; 67N: 67N |
| `MRI` | `Recloses_<element>` | Z1: Z1P, Z1G; Z2: Z2PT, Z2GT; Z3: Z3T; 51N: 51NT; 67N: 67N |
| `MTO` | `Trips_<element>` | 50H: 50H |

