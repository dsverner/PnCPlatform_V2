<Project Sdk="Microsoft.NET.Sdk">

  <!-- docs/design/API.md §8. The W1 gate: a console program run against a deployed API. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    <InvariantGlobalization>false</InvariantGlobalization>
    <Version>0.2.1</Version>
    <RootNamespace>PnC.Api.Smoke</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Data.SqlClient" Version="6.0.2" />
  </ItemGroup>

  <ItemGroup>
    <!-- W3: the three example documents of docs/design/examples are the gate's fixture, embedded so the exe is self-contained on the OT hosts -->
    <EmbeddedResource Include="..\..\docs\design\examples\*.json" LogicalName="examples/%(Filename)%(Extension)" />
  </ItemGroup>

</Project>
