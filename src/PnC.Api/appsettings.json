{
  "_": "docs/design/API.md §7. No secret ships here. The connection string is the service's own (integrated security, app_execute); DEV overrides it in the gitignored appsettings.Local.json with the tooling's SQL login — the documented DEV-only deviation. Auth:Mode is Windows everywhere; Development, with the X-PnC-Dev-User header, is honoured only when set in the local file AND the environment name is DEV.",
  "Database": {
    "ConnectionString": "Server=10.10.70.25;Database=PnCPlatform_V2_DEV;Integrated Security=true;TrustServerCertificate=true;Encrypt=true;Application Name=PnC.Api"
  },
  "Api": {
    "Schemas": [ "archive", "asset", "audit", "compliance", "config", "connection", "core", "device", "document", "event", "location",
                 "migration", "network", "party", "personnel", "platform", "process", "record", "ref", "scheme", "security", "work" ],
    "MaxTake": 10000,
    "MaterialiseBeforePaging": [ "document.vSettingsRecord", "document.vParsedSettingNamed", "work.vChangeRequestStatus", "location.vFloc", "location.vFlocScheme" ]
  },
  "Engine": {
    "_": "PROCEDURE-ENGINE §4.1 (#54): the scheduled sweep interval in minutes; 0 disables it. The design names a platform setting; none is stored yet, so the host carries it (decision #111).",
    "SweepMinutes": 15,
    "_compliance": "#171: the scheduled compliance pass (every effective obligation rule over its candidate subjects) interval in minutes; 0 disables it.",
    "ComplianceMinutes": 60
  },
  "Auth": {
    "Mode": "Windows",
    "UpnSuffix": "vgsot.internal"
  },
  "Logging": {
    "LogLevel": { "Default": "Information", "Microsoft.AspNetCore": "Warning" },
    "EventLog": { "SourceName": "PnC.Api", "LogName": "Application" }
  },
  "AllowedHosts": "*"
}
